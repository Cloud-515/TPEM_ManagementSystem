# TPEM_ManagementSystem 项目框架说明

本文档面向下一位接手这个项目的人，目标不是逐行解释代码，而是帮助你在最短时间内建立这套程序的整体心智模型，知道：

- 解决方案里有哪些项目
- 上位机的主要模块分别负责什么
- 数据从串口到数据库经历了哪些步骤
- 每个关键文件应该从哪里看起
- 改功能、查问题时该优先看哪些位置

---

# 1. 解决方案整体结构

解决方案文件：

- [MeterAcquisitionPro.sln](MeterAcquisitionPro.sln)

当前实际需要关注的核心项目有两个：

## 1.1 MeterAcquisition

路径：

- [MeterAcquisition/](MeterAcquisition/)

这是主程序，也就是上位机 WinForms 客户端。它负责：

- 连接串口设备
- 读取电表 Modbus 数据
- 在界面上显示实时值、参数、历史查询结果
- 管理仪表盘中的电表卡片
- 将采集到的数据发布到 MQTT
- 将电表档案信息发布到 MQTT

可以把它理解为：

```text
串口采集 + 本地界面 + MQTT 发布端
```

## 1.2 MeterIngestionWorker

路径：

- [MeterIngestionWorker/](MeterIngestionWorker/)

这是后台入库服务。它不负责界面，负责：

- 订阅 MQTT 消息
- 解析遥测数据和档案数据
- 写入 PostgreSQL
- 维护 latest / history / status / alarm 等业务表
- 做离线判断

可以把它理解为：

```text
MQTT 订阅端 + 数据库写入端
```

---

# 2. 整体数据流

这个项目最重要的是先看懂数据是怎么流动的。

## 2.1 主链路

```text
电表设备
  -> Modbus RTU 串口通信
  -> MeterAcquisition（WinForms 上位机）
  -> MQTT Broker
  -> MeterIngestionWorker
  -> PostgreSQL
```

## 2.2 上位机内部链路

```text
MainForm
  -> ModbusService 负责底层串口读写
  -> MeterDataService 负责把寄存器解析成业务数据
  -> MeterManager 负责多电表轮询和内存状态维护
  -> MqttPublisherService 负责发布到 MQTT
```

## 2.3 查询链路

```text
MainForm 查询模块
  -> HistoryQueryService
  -> PostgreSQL 历史表
  -> 图表 / 表格展示
```

---

# 3. MeterAcquisition 上位机结构

## 3.1 程序入口

入口文件：

- [MeterAcquisition/Program.cs](MeterAcquisition/Program.cs)

这里非常简单，只做一件事：启动 `MainForm`。

```text
Application.Run(new MainForm())
```

所以理解上位机时，直接从 [MeterAcquisition/Forms/MainForm.cs](MeterAcquisition/Forms/MainForm.cs) 开始看。

---

## 3.2 MainForm 是整个上位机的中枢

核心文件：

- [MeterAcquisition/Forms/MainForm.cs](MeterAcquisition/Forms/MainForm.cs)
- [MeterAcquisition/Forms/MainForm.Designer.cs](MeterAcquisition/Forms/MainForm.Designer.cs)
- [MeterAcquisition/Forms/MainForm.resx](MeterAcquisition/Forms/MainForm.resx)

其中：

- `Designer.cs` 主要是控件布局
- `MainForm.cs` 主要是业务逻辑

### MainForm 内部主要职责

MainForm 实际上同时承担了下面几类工作：

1. 主串口连接与设备操作
2. 仪表盘串口连接与自动扫描
3. 定时采集与界面刷新
4. MQTT 发布
5. 电表卡片管理
6. 历史查询与对比查询
7. 错误提示与状态栏更新

### MainForm 初始化时创建的关键对象

见 [MainForm.cs:65-109](MeterAcquisition/Forms/MainForm.cs#L65-L109)：

- `_modbusService`：主串口底层通信
- `_modbusService2`：仪表盘串口底层通信
- `_meterDataService`：主串口业务读取
- `_meterDataService2`：仪表盘串口业务读取
- `_meterManager`：管理所有电表对象和轮询
- `_refreshTimer`：1 秒 Tick 的采集刷新定时器
- `_mqttPublisherService`：MQTT 发布器
- `_historyQueryService`：历史查询服务

这几个对象就是上位机的核心骨架。

---

## 3.3 定时采集与发布是程序最核心的运行循环

关键位置：

- [MainForm.cs:1391-1447](MeterAcquisition/Forms/MainForm.cs#L1391-L1447)
- [MainForm.cs:1507-1567](MeterAcquisition/Forms/MainForm.cs#L1507-L1567)

### 运行机制

`_refreshTimer.Interval = 1000`，每秒触发一次 `RefreshTimer_Tick`。

每次 Tick 会做这些事：

1. 根据配置判断这一秒要不要读实时、电能、电能质量
2. 通过 `MeterManager.PollAll(...)` 轮询所有电表
3. 更新界面表格
4. 调用 `PublishAllMetersAsync(...)` 发布 MQTT 消息
5. 更新卡片显示

### 采样频率配置

配置在：

- [MeterAcquisition/App.config](MeterAcquisition/App.config)

关键项：

- `RealTimeIntervalSeconds = 1`
- `EnergyIntervalSeconds = 60`
- `QualityIntervalSeconds = 60`

这意味着：

- 实时数据默认每秒采一次
- 电能数据默认每 60 秒采一次
- 电能质量默认每 60 秒采一次

### 为什么看起来程序一直“每秒都在传”

因为实时数据确实每秒都在发。当前设计就是：

- UI 每秒刷新
- RealTime 每秒读取
- 每秒把包含实时数据的 MQTT 消息发出去

所以如果看 MQTT Explorer 或 `mqtt_message_log`，会看到每秒都有消息。

---

# 4. 上位机各个模块说明

## 4.1 ModbusService：底层串口通信层

文件：

- [MeterAcquisition/Services/ModbusService.cs](MeterAcquisition/Services/ModbusService.cs)

这是最底层的一层，职责是：

- 打开/关闭串口
- 组装 Modbus RTU 帧
- 发送命令
- 接收响应
- CRC 校验
- 读保持寄存器
- 写多个寄存器
- 写单个线圈
- 探测从站地址

### 你应该把它理解成什么

它只关心“怎么和设备通话”，不关心“寄存器代表什么业务意义”。

也就是说：

- `ModbusService` 负责字节级通信
- `MeterDataService` 负责业务字段解析

这是非常关键的分层。

### 关键方法

- `Connect(string portName)`：连接串口
- `Disconnect()`：断开串口
- `ReadHoldingRegisters(...)`：读寄存器
- `WriteMultipleRegisters(...)`：写多个寄存器
- `WriteSingleCoil(...)`：写线圈
- `DiscoverSlaveAddress()`：扫描设备地址

如果串口连不上、读不到回应、CRC 不对，先看这个文件。

---

## 4.2 MeterDataService：业务数据读取与寄存器解析层

文件：

- [MeterAcquisition/Services/MeterDataService.cs](MeterAcquisition/Services/MeterDataService.cs)

它建立在 `ModbusService` 之上，负责把寄存器数据解释成业务对象。

### 主要职责

1. 读取实时数据
2. 读取电能数据
3. 读取电能质量数据
4. 读取/写入通讯参数
5. 读取设备信息
6. 执行控制操作，例如分合闸、清零等

### 典型方法

- `ReadRealTimeData()`
- `ReadEnergyData()`
- `ReadPowerQualityData()`
- `ReadCommunicationConfig()`
- `WriteCommunicationConfig(...)`
- `ReadDeviceInfo()`
- `DO1_On()` / `DO1_Off()` / `DO2_On()` / `DO2_Off()`
- `ClearEnergy()`

### 为什么这个文件重要

如果以后要：

- 增加新的测量项
- 修改寄存器地址
- 改设备协议映射
- 修正某个字段解析错误

优先看这个文件。

这是“协议字段到业务对象”的核心位置。

---

## 4.3 MeterManager：多电表管理层

文件：

- [MeterAcquisition/Domain/MeterManager.cs](MeterAcquisition/Domain/MeterManager.cs)

它负责维护内存中的电表集合和轮询结果。

### 职责

- 管理多个配电箱和其中的电表
- 统一轮询所有电表
- 将采集结果保存回 `MeterInfo`
- 触发 `DataRefreshed` 事件给界面用

### 核心数据结构

在 [MeterAcquisition/Domain/Models.cs](MeterAcquisition/Domain/Models.cs) 中定义：

- `DistributionBox`
- `MeterInfo`
- `MeterPollResult`
- `RealTimeData`
- `EnergyData`
- `PowerQualityData`

### 核心方法

- `AddBox(...)`
- `AddMeterToBox(...)`
- `RemoveMeter(...)`
- `PollAll(...)`
- `ClearData()`

### 它的作用

`MainForm` 并不直接自己管理每一块电表数据，而是把“电表列表 + 最新采样结果”的内存状态集中交给 `MeterManager`。

这样：

- 主界面卡片可以读 `MeterManager`
- 详情窗口也可以读 `MeterManager`
- 采样循环也写 `MeterManager`

它相当于上位机运行时的内存状态中心。

---

## 4.4 MqttPublisherService：MQTT 发布层

文件：

- [MeterAcquisition/Services/MqttPublisherService.cs](MeterAcquisition/Services/MqttPublisherService.cs)

职责：

- 连接 MQTT Broker
- 序列化消息为 JSON
- 发布遥测消息
- 发布档案消息

### 支持两种消息

1. `MeterTelemetryMessage`
   - 遥测数据
   - 定义在 [MeterAcquisition/Domain/MqttContracts.cs](MeterAcquisition/Domain/MqttContracts.cs)

2. `MeterRegistryMessage`
   - 档案/扫描结果/电表列表同步
   - 也定义在 [MeterAcquisition/Domain/MqttContracts.cs](MeterAcquisition/Domain/MqttContracts.cs)

### 关键方法

- `PublishAsync(MeterTelemetryMessage message)`
- `PublishRegistryAsync(MeterRegistryMessage message)`

### 主题格式

配置在 [MeterAcquisition/App.config](MeterAcquisition/App.config)：

- `MqttTopicTemplate = meter/{site}/{box}/{meter}`
- `MqttRegistryTopicTemplate = meter/registry/{site}/{box}`

也就是说：

- 实时/电能/电能质量消息发到 `meter/...`
- 档案同步消息发到 `meter/registry/...`

### 当前特点

- 使用单个 `IMqttClient`
- 发送前自动连接
- 当前没有内建复杂的断线重试队列
- 最近已补了 UI 层错误提示：发布失败会弹窗并保留状态栏错误显示，但程序不会因此退出

如果以后要改 MQTT 地址、主题格式、认证方式、报文结构，优先看这个文件和 `App.config`。

---

## 4.5 HistoryQueryService：历史查询层

文件：

- [MeterAcquisition/Services/HistoryQueryService.cs](MeterAcquisition/Services/HistoryQueryService.cs)

这个模块完全是“查数据库给界面展示”。

### 职责

- 提供可查询指标定义
- 从数据库读电表目录
- 构建站点/配电箱/电表树
- 查询单表历史曲线和明细表
- 查询多表对比曲线
- 在数据量大时做时间桶聚合降采样

### 支持查询的历史表

- `meter_realtime_history`
- `meter_energy_history`
- `meter_power_quality_history`

### 支持查询的指标定义

在 [HistoryQueryService.cs:13-38](MeterAcquisition/Services/HistoryQueryService.cs#L13-L38) 中写死，包括：

- 三相电压
- 三相电流
- 总有功 / 无功 / 视在功率
- 总功率因数
- 频率
- 正反向有功电能
- 正反向无功电能
- 电流/电压 THD
- 不平衡度

### 关键方法

- `GetMetricDefinitions()`
- `GetMetersAsync()`
- `GetMeterTreeAsync()`
- `QueryQuickHistoryAsync(...)`
- `QueryComparisonAsync(...)`

### 注意点

这个服务是“只读数据库”，不会影响采集链路。

如果接手后要新增历史查询字段，通常需要同时改：

1. 入库 Worker 是否把该字段写入历史表
2. `HistoryQueryService` 的指标定义列表
3. 图表/表格展示逻辑

---

## 4.6 MeterDetailsForm：单电表详情窗口

文件：

- [MeterAcquisition/Forms/MeterDetailsForm.cs](MeterAcquisition/Forms/MeterDetailsForm.cs)

职责：

- 展示单块电表的详细实时/电能/电能质量信息
- 监听 `MeterManager.DataRefreshed`
- 跟随主界面采样刷新显示

这个窗体本身不主动采样，只是消费 `MeterManager` 中已经存在的数据。

这也是这个项目比较重要的设计点：

```text
采样只做一份
界面多处共享同一份内存结果
```

---

# 5. MainForm 中不同区域的职责

虽然 `MainForm.cs` 很大，但按职责可以拆成几块来理解。

## 5.1 主串口区

主要负责：

- 连接主串口
- 读单台或外部串口电表数据
- 参数读取/写入
- 设备信息读取
- DO 控制、清零等

相关依赖：

- `_modbusService`
- `_meterDataService`

## 5.2 仪表盘串口区

主要负责：

- 连接第二串口
- 地址范围扫描
- 自动发现电表
- 将电表加入仪表盘区域
- 发布档案同步消息

相关依赖：

- `_modbusService2`
- `_meterDataService2`
- `PublishBoxRegistryAsync(...)`

## 5.3 仪表卡片区

主要负责：

- 动态构建电表卡片
- 显示每个电表的状态、功率、电流
- 删除卡片
- 点击进入详情

内存来源：

- `_meterManager.AllMeters`

## 5.4 查询模块

主要负责：

- 单表快速查询
- 多表对比查询
- 图表悬停提示
- 数据表格分页

依赖：

- `_historyQueryService`

## 5.5 状态与错误提示区

主要负责：

- 串口连接状态
- 通信错误提示
- MQTT 发布失败提示
- 查询模块提示

最近和 MQTT 有关的行为是：

- 发布失败时弹窗一次
- 页面保留 `MQTT 发布失败: ...`
- 后续恢复发布成功时自动清除错误状态

---

# 6. 配置文件说明

## 6.1 上位机配置

文件：

- [MeterAcquisition/App.config](MeterAcquisition/App.config)

关键配置：

### MQTT 相关

- `MqttHost`
- `MqttPort`
- `MqttClientId`
- `MqttTopicTemplate`
- `MqttRegistryTopicTemplate`
- `MqttUsername`
- `MqttPassword`

### 业务标识

- `SiteCode`
- `ToolbarBoxCode`
- `DashboardBoxCode`

### 采样频率

- `RealTimeIntervalSeconds`
- `EnergyIntervalSeconds`
- `QualityIntervalSeconds`

### 数据库连接

- `MeterDb`
- `MeterAcquisition`

注意：虽然上位机本身也需要数据库连接，但主要用途是历史查询，而不是实时入库。

---

## 6.2 入库服务配置

文件：

- [MeterIngestionWorker/App.config](MeterIngestionWorker/App.config)

关键配置：

### MQTT 订阅相关

- `MqttHost`
- `MqttPort`
- `MqttTopicPattern`
- `MqttRegistryTopicPattern`
- `MqttUsername`
- `MqttPassword`

### 入库策略

- `RealtimeHistoryIntervalSeconds`
- `EnergyHistoryIntervalMinutes`
- `QualityHistoryIntervalMinutes`
- `OfflineTimeoutSeconds`

### 变化阈值

- 电压、电流、功率、功率因数、频率的变化阈值

### 报警阈值

- 低电压、高电压、高电流、低功率因数

---

# 7. MeterIngestionWorker 后台入库服务结构

## 7.1 入口与主循环

核心文件：

- [MeterIngestionWorker/Program.cs](MeterIngestionWorker/Program.cs)

这个项目几乎所有逻辑都在 `Program.cs`。

### 启动后会做的事

1. 读取配置
2. 启动 MQTT 订阅客户端
3. 把 MQTT 消息放入内存队列
4. 启动工作线程处理消息
5. 启动离线监控线程

### 主要运行部件

- `MessageQueue`：MQTT 消息队列
- `StartWorkers()`：启动处理线程
- `RunMqttSubscriberAsync(...)`：订阅 MQTT
- `ProcessMessages()`：处理消息
- `MonitorOfflineMetersAsync(...)`：离线状态扫描

---

## 7.2 Worker 处理的两类消息

共享契约定义：

- [MeterIngestionWorker/SharedContracts.cs](MeterIngestionWorker/SharedContracts.cs)

两类：

1. `MeterTelemetryMessage`
   - 实时/电能/电能质量消息
2. `MeterRegistryMessage`
   - 电表档案同步消息

### 遥测消息的处理结果

会更新或写入：

- `mqtt_message_log`
- `meter_realtime_latest`
- `meter_realtime_history`
- `meter_energy_latest`
- `meter_energy_history`
- `meter_power_quality_latest`
- `meter_power_quality_history`
- `meter_status`
- `meter.last_seen_time`
- `alarm_event`

### 档案消息的处理结果

会更新或写入：

- `site`
- `distribution_box`
- `meter`
- `meter_status`
- `meter.last_seen_time`

---

# 8. 数据库写入策略要点

这一部分对接手者非常重要，因为很多“为什么数据库是这样”的问题都在这里。

## 8.1 latest 表

这些表反映当前最新值：

- `meter_realtime_latest`
- `meter_energy_latest`
- `meter_power_quality_latest`

特点：

- 每次收到对应类型消息就 upsert 覆盖最新值
- 不保留完整历史，只保留最后一条

## 8.2 history 表

这些表保留历史：

- `meter_realtime_history`
- `meter_energy_history`
- `meter_power_quality_history`

写入策略：

### 实时历史

- 默认至少间隔 `RealtimeHistoryIntervalSeconds`
- 或者数据变化超过阈值时提前写

### 电能历史

- 按 `EnergyHistoryIntervalMinutes` 控制

### 电能质量历史

- 按 `QualityHistoryIntervalMinutes` 控制

所以：

- 不是所有数据表都每秒写
- 但 MQTT 消息日志和 latest 表会随着实时消息频率而频繁变化

## 8.3 状态与报警表

- `meter_status`：在线状态、最后采集时间、最后错误时间等
- `alarm_event`：报警触发、持续、恢复

### 离线判断

离线监控线程每 5 秒检查一次，如果超过 `OfflineTimeoutSeconds` 没有新采集时间，就标记离线。

---

# 9. 文件级阅读顺序建议

如果是第一次接手，建议按下面顺序看。

## 第一轮：先建立整体印象

1. [MeterAcquisition/Program.cs](MeterAcquisition/Program.cs)
2. [MeterAcquisition/Forms/MainForm.cs](MeterAcquisition/Forms/MainForm.cs)
3. [MeterAcquisition/App.config](MeterAcquisition/App.config)
4. [MeterIngestionWorker/Program.cs](MeterIngestionWorker/Program.cs)
5. [MeterIngestionWorker/App.config](MeterIngestionWorker/App.config)

## 第二轮：理解上位机采集与发布链路

1. [MeterAcquisition/Services/ModbusService.cs](MeterAcquisition/Services/ModbusService.cs)
2. [MeterAcquisition/Services/MeterDataService.cs](MeterAcquisition/Services/MeterDataService.cs)
3. [MeterAcquisition/Domain/MeterManager.cs](MeterAcquisition/Domain/MeterManager.cs)
4. [MeterAcquisition/Services/MqttPublisherService.cs](MeterAcquisition/Services/MqttPublisherService.cs)
5. [MeterAcquisition/Domain/MqttContracts.cs](MeterAcquisition/Domain/MqttContracts.cs)

## 第三轮：理解查询与数据库消费链路

1. [MeterAcquisition/Services/HistoryQueryService.cs](MeterAcquisition/Services/HistoryQueryService.cs)
2. [MeterIngestionWorker/SharedContracts.cs](MeterIngestionWorker/SharedContracts.cs)
3. [MeterIngestionWorker/Program.cs](MeterIngestionWorker/Program.cs)

---

# 10. 常见改动应该看哪里

## 10.1 改寄存器地址、改数据解析

看：

- [MeterAcquisition/Services/MeterDataService.cs](MeterAcquisition/Services/MeterDataService.cs)

## 10.2 改串口通信方式或排查串口异常

看：

- [MeterAcquisition/Services/ModbusService.cs](MeterAcquisition/Services/ModbusService.cs)

## 10.3 改采样周期

看：

- [MeterAcquisition/App.config](MeterAcquisition/App.config)
- [MainForm.cs:1399-1402](MeterAcquisition/Forms/MainForm.cs#L1399-L1402)

## 10.4 改 MQTT 主题、Broker 地址、认证方式

看：

- [MeterAcquisition/App.config](MeterAcquisition/App.config)
- [MeterAcquisition/Services/MqttPublisherService.cs](MeterAcquisition/Services/MqttPublisherService.cs)
- [MeterIngestionWorker/App.config](MeterIngestionWorker/App.config)
- [MeterIngestionWorker/Program.cs](MeterIngestionWorker/Program.cs)

## 10.5 改数据库历史写入频率

看：

- [MeterIngestionWorker/App.config](MeterIngestionWorker/App.config)
- [MeterIngestionWorker/Program.cs](MeterIngestionWorker/Program.cs)
  - `ShouldWriteRealtimeHistory(...)`
  - `ShouldWriteEnergyHistory(...)`
  - `ShouldWriteQualityHistory(...)`

## 10.6 改界面卡片、状态展示、弹窗行为

看：

- [MeterAcquisition/Forms/MainForm.cs](MeterAcquisition/Forms/MainForm.cs)
- [MeterAcquisition/Forms/MainForm.Designer.cs](MeterAcquisition/Forms/MainForm.Designer.cs)
- [MeterAcquisition/Forms/MeterDetailsForm.cs](MeterAcquisition/Forms/MeterDetailsForm.cs)

## 10.7 改查询模块指标或图表来源

看：

- [MeterAcquisition/Services/HistoryQueryService.cs](MeterAcquisition/Services/HistoryQueryService.cs)

---

# 11. 这个项目当前的几个设计特点

## 11.1 上位机职责比较重

`MainForm.cs` 不是一个纯 UI 文件，它实际承担了：

- 控件事件
- 采样调度
- 卡片管理
- MQTT 错误反馈
- 历史查询控制

所以它偏“大而全”。接手时不要期待特别细的 MVC/MVVM 分层。

## 11.2 协议层和业务层是分开的

这部分设计是清晰的：

- `ModbusService`：通信层
- `MeterDataService`：寄存器解释层

这是后续维护最值得保留的边界。

## 11.3 数据入库和 UI 是解耦的

- 上位机不直接负责完整实时入库
- 入库交给 `MeterIngestionWorker`
- 查询模块再直接从数据库读取历史数据

所以整体是“采集 / 入库 / 查询”三段式结构。

## 11.4 MQTT 是中间总线

它的价值在于把：

- 上位机采集
- 数据库入库
- 以后可能的其他订阅方

解耦开来。

如果以后要做网页端、监控端、告警服务，原则上都可以直接订阅同一 MQTT 流。

---

# 12. 接手后建议优先确认的事项

1. MQTT Broker 现在是 Docker 还是本机 Mosquitto
2. PostgreSQL 现在是本机服务还是其他部署方式
3. `App.config` 中端口和实际运行环境是否一致
4. 当前数据库表结构是否和 Worker 代码匹配
5. 是否存在未提交但已经依赖的本地改动
6. 仪表寄存器定义是否和当前设备固件版本一致

---

# 13. 一句话总结整个系统

这套程序可以概括为：

```text
一个 WinForms 上位机通过 Modbus RTU 采集电表数据，实时显示在界面上，同时通过 MQTT 发布遥测与档案消息；一个独立的后台 Worker 订阅这些消息并写入 PostgreSQL，供历史查询、状态管理和报警逻辑使用。
```

如果你只记住一条主线，就记这个。
