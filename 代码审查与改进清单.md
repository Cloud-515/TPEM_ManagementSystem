# TPEM_ManagementSystem 代码审查与改进清单

> **审查日期**：2026-08-26
> **审查范围**：上位机侧 —— `MeterAcquisition`（WinForms 上位机）+ `MeterIngestionWorker`（MQTT 入库服务）
> **待补充**：网页部分（`RuoYi-Vue-master` 前后端）为第二轮审查范围
> **构建基线**：`dotnet build MeterAcquisitionPro.sln` 通过，0 error / 6 × CS8632 / 108 × MSB3277

---

## 使用说明

1. 每完成一项改动，把"进度总表"里对应行的 `[ ]` 改成 `[x]`，并填写**完成日期**。
2. 详细问题描述见"问题明细"章节，小节编号与总表编号一一对应。
3. 新发现的问题追加到总表末尾，编号继续递增，不要复用已废弃的编号。
4. 每批改动完成后，在文末"变更记录"追加一行。

**总体结论**：功能主线（Modbus RTU → MQTT → PostgreSQL → 查询）是通的，架构分层也有可保留的骨架；
但当前代码尚不具备 7×24 无人值守运行的资格 —— 缺日志、缺全局异常兜底、MQTT 断线即丢数、串口无统一仲裁、
合格判定口径分散在三处。第一批改动的目标是"止血"，不改架构。

---

## 一、进度总表

### 第一批：止血（P0 —— 稳定性与现场设备安全）

| 编号 | 完成 | 严重度 | 问题摘要 | 主要位置 | 完成日期 |
|:---:|:---:|:---:|---|---|:---:|
| P0-1 | [x] | 致命 | 无全局异常兜底，`async void` 事件处理器裸奔 | `MeterAcquisition/Program.cs`、`Forms/MainForm.cs` | 2026-08-26 |
| P0-2 | [x] | 致命 | 报文解析长度守卫小于实际消费字节数（3 处越界） | `Services/MeterDataService.cs` | 2026-08-26 |
| P0-3 | [x] | 致命 | 写通讯参数会丢弃校验位、可写入非法波特率索引 | `Services/MeterDataService.cs`、`Forms/MainForm.cs` | 2026-08-26 |
| P0-4 | [x] | 高 | 分合闸遥控无二次确认、无操作审计 | `Forms/MainForm.cs` | 2026-08-26 |
| P0-5 | [x] | 高 | 关窗时先释放资源再调 base，且不等在途轮询 | `Forms/MainForm.cs` | 2026-08-26 |
| P0-6 | [x] | 致命 | Worker MQTT 重连只做一次，失败后永久掉线且无感知 | `MeterIngestionWorker/Program.cs` | 2026-08-26 |
| P0-7 | [x] | 致命 | 入库队列无界（PG 挂即 OOM）；上位机无单实例保护 | `MeterIngestionWorker/Program.cs`、`Program.cs` | 2026-08-26 |
| P0-8 | [x] | 高 | 全项目零日志，14 处空 catch 静默吞异常 | 全局（新增 `Common/AppLogger.cs`） | 2026-08-26 |

> **第一批已全部完成。** 实现细节与验证结果见"第一批实施记录"。

### 第二批：数据可信（P1 —— 数据正确性与判定口径）

| 编号 | 完成 | 严重度 | 问题摘要 | 主要位置 | 完成日期 |
|:---:|:---:|:---:|---|---|:---:|
| P1-1 | [x] | 高 | 合格/不合格判定有三套独立实现，阈值硬编码且不分接线方式 | `Forms/MainForm.cs`、Worker `App.config`、网页端 | 2026-08-26 |
| P1-2 | [x] | 高 | 电表离线不写 `alarm_event`，网页"报警记录"因此只能伪造 | `MeterIngestionWorker/Program.cs` | 2026-08-26 |
| P1-3 | [x] | 高 | `meter_*_latest` 无乱序保护，迟到消息会覆盖新值 | `MeterIngestionWorker/Program.cs` | 2026-08-26 |
| P1-4 | [x] | 高 | 电表消息无 `MessageId`/版本号，幂等仅靠内存 10 秒窗口 | `Domain/MqttContracts.cs`、Worker | 2026-08-26 |
| P1-5 | [x] | 高 | DTO 全为非空 `float`，`0` = 读失败 = 真实零值，三者不可区分 | `Domain/Models.cs` | 2026-08-26 |
| P1-6 | [x] | 高 | 报警无去抖/滞回/带电前置条件，空载与掉电必然误报 | `MeterIngestionWorker/Program.cs` | 2026-08-26 |
| P1-7 | [x] | 中 | 采样节奏用 tick 计数而非时钟，轮询超 1 秒即整体漂移 | `Forms/MainForm.cs` | 2026-08-26 |
| P1-8 | [x] | 中 | 时间基准不统一（固定 +08:00 / `DateTime.Now` 混用） | 全链路 | 2026-08-26 |

> **第二批已完成，遗留一项**：前端 `meter-utils.js` / `meter-simulation.js` 里仍有一份硬编码阈值作为**兜底**
> （服务端返回 `qualityRiskCodes` 时前端优先用服务端结果，兜底路径平时不生效）。
> 彻底收口需要新增阈值查询接口并重新构建 Vue 产物，见"八、第二批实施记录 → 遗留事项"。

### 第三批：架构收口（P2 —— 可维护性）

| 编号 | 完成 | 严重度 | 问题摘要 | 主要位置 | 完成日期 |
|:---:|:---:|:---:|---|---|:---:|
| P2-1 | [x] | 高 | 两种电表协议字节序/量纲各写一套，落进同一批列 | `MeterDataService.cs`、`Amc96lE4MeterDataReader.cs` | 2026-08-26 |
| P2-2 | [ ] | 高 | 串口无统一仲裁器，UI 线程可能被阻塞数秒；无帧间隔保证 | `Services/ModbusService.cs`、热泵共享总线服务 | |
| P2-3 | [x] | 高 | 收帧不校验从站地址/字节数，总线串话可能误收他人回包 | `Services/ModbusService.cs` | 2026-08-26 |
| P2-4 | [x] | 中 | 探测地址在 UI 线程全量扫 1~247，最坏冻结约 2 分钟 | `Forms/MainForm.cs` | 2026-08-26 |
| P2-5 | [ ] | 中 | `MainForm` 3402 行，兼任 UI/调度/协议/MQTT/查询 | `Forms/MainForm.cs` | |
| P2-6 | [x] | 中 | 卡片标签用 `dynamic` 传控件引用，无编译期检查 | `Forms/MainForm.cs` | 2026-08-26 |
| P2-7 | [x] | 中 | 切换电表型号时重建 `MeterManager` 但不重建面板，卡片变僵尸 | `Forms/MainForm.cs` | 2026-08-26 |
| P2-8 | [ ] | 中 | MQTT 契约在两个项目里逐字复制，改字段无编译错误 | `MqttContracts.cs` / `SharedContracts.cs` | |

> **第三批进行中**：P2-1 / P2-3 / P2-4 / P2-6 / P2-7 已完成，并顺带建起了测试工程（P3-7 的一半）。
> 剩余 P2-2（串口仲裁器）、P2-5（`MainForm` 拆分）、P2-8（契约共享库）见"九、第三批实施记录"。

### 第四批：性能与工程化（P3）

| 编号 | 完成 | 严重度 | 问题摘要 | 主要位置 | 完成日期 |
|:---:|:---:|:---:|---|---|:---:|
| P3-1 | [ ] | 高 | MQTT 断线期间数据直接丢；首台发布失败即放弃整轮 | `MqttPublisherService.cs`、`Forms/MainForm.cs` | |
| P3-2 | [ ] | 中 | Worker 每条消息开 3 次连接，并跑全系统最热的历史查询 | `MeterIngestionWorker/Program.cs` | |
| P3-3 | [ ] | 中 | `mqtt_message_log` 无保留策略（10 表 1Hz ≈ 86 万行/天） | 数据库 | |
| P3-4 | [ ] | 中 | `ProcessWorkers>1` 时同表消息乱序处理，判定与写入可能冲突 | `MeterIngestionWorker/Program.cs` | |
| P3-5 | [ ] | 中 | 无头服务引用 ScottPlot/SkiaSharp；MySQL 遗留依赖与过期 SQL | Worker `csproj`、`sql/alarm_event.sql` | |
| P3-6 | [ ] | 中 | HeatPump 用 `?` 注解但未启用 `Nullable`，注解形同虚设 | `MeterAcquisition.csproj` | |
| P3-7 | [ ] | 中 | 无任何测试项目、无 CI | 解决方案级 | |
| P3-8 | [ ] | 低 | `meter_realtime_history` 上有重复索引，拖慢写入 | 数据库 | |

### 并行处理（尽早，与批次无关）

| 编号 | 完成 | 严重度 | 问题摘要 | 主要位置 | 完成日期 |
|:---:|:---:|:---:|---|---|:---:|
| X-1 | [ ] | 高 | 数据库明文口令已进 git；MQTT 无 TLS、无 ACL | `App.config` × 2 | |
| X-2 | [x] | 高 | `RuoYi-Vue-master` 是 gitlink 但无 `.gitmodules`，别人克隆不下来 | 仓库根 | 2026-08-26 |
| X-3 | [ ] | 中 | 提交了 exe/dll 构建产物与个人临时文件，`.git` 已 49MB | `.claude/buildcheck-*`、`程序启动.txt` | |

> **X-3 澄清**：此前曾把 `AMC96L-E4 通讯地址表（完整）.xlsx` 的体积变化列为疑点，
> 已确认该文件当前内容是正常完整的协议地址表，不是被精简，**不属于本条问题**。
| X-4 | [ ] | 中 | 上位机绕过 API 直连 PostgreSQL，与既定架构相悖 | `HistoryQueryService.cs` | |
| X-5 | [x] | 中 | 温控器链路两端都没接通：迁移 002 未执行 + 发布端从未被调用 | `sql/migrations/002_*.sql`、`MqttPublisherService.cs` | 2026-08-26 |
| X-6 | [x] | 中 | 数据库迁移无应用记录，哪个执行过全靠人记（现网 003 已应用而 002 没有） | `sql/migrations/` | 2026-08-26 |
| X-7 | [ ] | 中 | 5 个源文件不在 `csproj` 的 `Compile` 列表里，**根本没被编译**（改了也不生效） | 见下方说明 | |
| X-8 | [ ] | 高 | 仓库根 `skills/` 是 mode `160000` 的 gitlink 但**没有 `.gitmodules`**，且其提交对象不在本仓库 —— clone 拿不到、本地内容也没备份（与 X-2 同类） | 仓库根 | |
| X-9 | [ ] | 中 | 交接文档《项目框架说明》漏掉热泵、温控器、网页端、日志、判定阈值、迁移体系、测试工程与界面规范 | `项目框架说明-上位机与入库服务.md` | |

> **X-7（2026-08-27 新发现）**：`MeterAcquisition.csproj` 是老式工程（显式列 `Compile`），以下文件在磁盘上但没进编译：
> `HeatPump/Forms/ControllerEditorDialog.cs`、`HeatPump/Forms/ModuleEditorDialog.cs`、
> `HeatPump/Forms/ControllerGroupEditorDialog.cs`、`HeatPump/Forms/MainForm.cs`（1079 行）、
> `HeatPump/Forms/MainForm.Designer.cs`（507 行）。
> 发现过程：给界面审计探针加对话框构造时 `CS0246 找不到类型`，回头查 csproj 才确认。
> 真正在用的只有 `HeatPumpDeviceEditorDialog` 和 `HeatPumpModuleGroupEditorDialog`。
> **处理需产品/开发确认**：这批是早期版本残留（删）还是未接线的新实现（补进 csproj）。本批不动。

> **X-8（2026-09-17 新发现）**：仓库根的 `skills/` 在索引里是 mode `160000` 的 gitlink，
> 但**没有 `.gitmodules`**，与当初修掉的 X-2 完全同类 —— 别人 clone 后只会得到空目录，
> `git submodule update` 直接报"No url found for submodule path 'skills'"。
> 且它记录的提交 `690f15c` **不在本仓库对象库里**（`git cat-file -t` 失败），
> 说明这份内容既没被备份、也拿不回来。它实际是 `https://github.com/anthropics/skills.git`
> 的克隆，与 TPEM 业务无关 —— 建议直接移除，或补 `.gitmodules` 并说明为什么要 vendored。

> **X-9（2026-09-17 新发现）**：`项目框架说明-上位机与入库服务.md`（578 行，开篇即写"面向下一位接手这个项目的人"）
> 与当前代码已明显脱节：全文检索 **热泵**、**温控器**、`HeatPump`、`Thermostat`、
> `AppLogger`、`meter_threshold`、`migration`、`TPEM.Tests`、`UiStyle` 的命中数**全部为 0**，
> 网页端（`RuoYi`/`若依`/`前端`/`Vue`）也一次都没提到。
> 按它的"文件级阅读顺序建议"走，会以为这个系统只有"电表 + 入库服务"两部分。
> 后五批的产出（日志、判定阈值、迁移体系、测试工程、界面规范、热泵/温控器/网页端）
> 都需要补进这份文档，否则新接手的人第一步就会走偏。

> **X-3 规模更正（2026-09-17）**：原文只点了 `.claude/buildcheck-adaptive-y/` 一个目录，
> 实测为 **17 个 `buildcheck*` 目录、1153 个跟踪文件、合计 1575 MB**（`.git` 50.3 MB）。
> 全新 clone 会在磁盘上写出 1.5 GB 无用文件。清理需要重写历史，属不可逆操作，建议单独一轮并先评审。

> **X-1 补充（2026-09-17）**：除 PG 与 MQTT 口令外，`sql/migrations/20260730_create_tpem_admin.sql`
> 还硬编码了 `tpem_admin` 的 bcrypt 口令哈希，并授予 `role_id=1`（若依超管角色）——
> 拿到仓库的人可离线爆破。同类问题，纳入 X-1 一起治理。

> **X-5 处理方式**：业务决定**温控器暂不上线**，因此不执行迁移 002，改为把入库服务的温控器订阅
> 做成开关（`SubscribeThermostatTopic`，默认 `false`）并在启动日志里说明链路未接通。
> 这一项因此按"已按暂不上线安全处理"结项，**不代表链路已打通** —— 将来要上线需三件事同时做到：
> 执行迁移 002、补上位机发布端调用、把该配置改为 `true`。

### 第五批：界面布局（UI —— 上位机可用性，2026-08-26 新增）

审查方式：`dotnet build` 后真跑上位机，1400×900 逐页签截图；线控器卡片区用 `artifacts/LayoutProbe`
反射注入 1 台控制器 + 5 个模块，直接读生产代码布局出来的控件 `Bounds`（不改生产代码）。

| 编号 | 完成 | 严重度 | 问题摘要 | 主要位置 | 完成日期 |
|:---:|:---:|:---:|---|---|:---:|
| UI-1 | [x] | 高 | "电表总览"页签构造期 `Insert` 被静默丢弃，整个控件从未显示过 | `Forms/MainForm.cs`、`Forms/MeterOverviewControl.cs` | 2026-08-26 |
| UI-2 | [x] | 高 | 线控器汇总卡片数值与标题叠在一起（4 张卡全中） | `Forms/LineControllerControl.Designer.cs` | 2026-08-26 |
| UI-3 | [x] | 高 | 卡片内 `Dock=Right` 把复选框/按钮拉满卡片高度，遥测正文只剩 139px | `Forms/MainForm.cs` | 2026-08-26 |
| UI-4 | [x] | 中 | 历史分析页摘要行固定 48px、筛选行固定 86px，文字换行即被切一半 | `HeatPump/Forms/HeatPumpMonitorControl.Designer.cs` | 2026-08-26 |
| UI-5 | [x] | 中 | 线控器分组容器宽度写死 `1120`，宽屏留白、窄屏溢出 | `Forms/MainForm.cs` | 2026-08-26 |
| UI-6 | [ ] | 中 | 电表卡片有两套并存实现（"概览仪表盘" / "电表总览"），需决定留哪个 | `Forms/MainForm.cs:2345`、`Forms/MeterOverviewControl.cs:137` | |
| UI-7 | [x] | 中 | 线控器数据页三条工具条 + 汇总卡片吃掉约 590px，卡片区只剩约 110px | `Forms/LineControllerControl.Designer.cs` | 2026-08-27 |
| UI-8 | [ ] | 中 | 卡片区宽度靠手写 `Resize`/`SizeChanged` 钩子同步（两处） | `Forms/MainForm.cs:155`、`Forms/MeterOverviewControl.cs:19` | |
| UI-9 | [ ] | 中 | 窗口最小尺寸 980×680 与页面实际需求不匹配（线控器页需约 700px 纵向） | `Forms/MainForm.Designer.cs:1915` | |
| UI-10 | [x] | 低 | 字体三套混用：`Segoe UI` / `微软雅黑` / 系统默认 | 各 Designer、`UI/UiStyle.cs` | 2026-08-27 |
| UI-11 | [ ] | 中 | 卡片构建逻辑散在 `MainForm`（4071 行）里，应抽成 `UserControl` | `Forms/MainForm.cs` | |
| UI-12 | [ ] | 中 | 无 DPI 感知声明（无 `app.manifest`、配置无 DPI 项），125%/150% 下整窗位图拉伸 | `MeterAcquisition.csproj`、`App.config` | |
| UI-13 | [x] | 中 | 表格布局里仍有一批 `Absolute` 行列（180F / 228F 等），大字号或小屏会切内容 | `HeatPump/Forms/HeatPumpMonitorControl.Designer.cs` | 2026-08-27 |
| UI-14 | [ ] | 低 | 可访问性：全项目仅 1 处 `AccessibleName`，卡片点击无键盘等价操作 | 全 UI | |
| UI-15 | [x] | 高 | 按钮尺寸没有统一口径：38 个按钮 **17 种尺寸、8 种高度**，每个 Designer 各写一套 `MinimumSize`/`Padding` | 全 UI、新增 `UI/UiStyle.cs` | 2026-08-27 |
| UI-16 | [x] | 高 | 按钮被容器拉伸：`Dock=Fill`/四边锚定把"读取设备信息"拉成 1324×38、遥控按钮拉成 160×50 | `Forms/MainForm.Designer.cs` | 2026-08-27 |
| UI-17 | [x] | 高 | "标签 + 输入框"有 4 份各自写死 `Margin.Top` 的假垂直居中实现，15 处同行中线差 4~11px | `Forms/MainForm.cs`、温控器/历史分析各 Designer | 2026-08-27 |
| UI-18 | [x] | 中 | 工具条换行把标签和它的输入框拆到两行（"结束时间"留在上一行、日期框掉到下一行） | `Forms/MainForm.Designer.cs` 等 4 条工具条 | 2026-08-27 |
| UI-19 | [x] | 高 | 5 个代码手写的 `UserControl` 用 `AutoScaleMode.Font` 但没声明 `AutoScaleDimensions`，同一份像素常量在不同页面被放大 1.6 倍 | 温控器三条 Bar、`LineControllerControl`、`MeterOverviewControl`、`HeatPumpMonitorControl` | 2026-08-27 |
| UI-20 | [x] | 中 | 两个编辑对话框是绝对坐标布局，标签压在输入框上（实测重叠 8px）；按钮又是第 5、第 6 种尺寸 | `HeatPump/Forms/HeatPumpDeviceEditorDialog.cs`、`HeatPumpModuleGroupEditorDialog.cs` | 2026-08-27 |
| UI-21 | [ ] | 低 | `ApplyLayoutStandard()` 在构造函数末尾执行，而"电表总览"页签直到 `MainForm_Load` 才 `Insert` —— 界面规范对该页签不生效 | `Forms/MainForm.cs:174,3427` | |

> **UI 第一批（UI-1 ~ UI-5）已完成**，均为"看得见的错位"，实现与实测数据见"十一、界面布局第一批实施记录"。
> **UI 第二批（UI-15 ~ UI-20，顺带收掉 UI-7 / UI-10 / UI-13）已完成**，见"十二、界面布局第二批实施记录"。
> 剩余 6 项（UI-6 / UI-8 / UI-9 / UI-11 / UI-12 / UI-14）属于结构性问题（重复实现、DPI、可访问性），
> 需要和 P2-5（拆分 `MainForm`）一起做。

### 第六批：复查新发现（P4 —— 2026-09-17）

> 新一轮复查（4 路并行：上位机 / 入库服务与数据库 / 若依后端 / 若依前端）在上位机与入库服务侧
> 发现的**清单未记录**的问题。网页端单列为 W 系列（见第七批）。
> 审查方式与验证结果见"十三、第二轮审查实施记录"。

| 编号 | 完成 | 严重度 | 问题摘要 | 主要位置 | 完成日期 |
|:---:|:---:|:---:|---|---|:---:|
| P4-1 | [x] | 高 | 串口状态事件在锁内触发，订阅者随后**同步**等 MQTT（默认 100s 超时）→ 界面冻结且采集停摆 | `Services/ModbusService.cs`、`Forms/MainForm.cs` | 2026-09-17 |
| P4-2 | [x] | 高 | 写通讯参数丢弃用户所选停止位：选 `8N2` 实际写入 `8N1`，表从总线消失 | `Forms/MainForm.cs` | 2026-09-17 |
| P4-3 | [x] | 高 | 写响应不校验从站地址、CRC 按实收长度算 → 分合闸"假成功"/"假失败" | `Services/ModbusService.cs`、`Common/Utils.cs` | 2026-09-17 |
| P4-4 | [x] | 高 | 报警去抖是"累计命中"而非"连续命中"，P1-6 的去抖目标实际未达成 | `MeterIngestionWorker/Program.cs` | 2026-09-17 |
| P4-5 | [x] | 中 | Worker 正常退出必抛未处理 OCE（退出码非 0、汇总与日志 flush 被跳过）；异常路径不取消令牌 → 进程既不干活也不退出 | `MeterIngestionWorker/Program.cs` | 2026-09-17 |
| P4-6 | [x] | 中 | `meter_status.last_collect_time` / `meter.last_seen_time` 缺 P1-3 的单调守卫 → 迟到消息造成假离线告警 | `MeterIngestionWorker/Program.cs` | 2026-09-17 |
| P4-7 | [ ] | 中 | 切型号到 AMC96L-E4 后，参数/设备信息/遥控/清零仍打**原有电表**寄存器 | `Forms/MainForm.cs:133,3132,3758+` | |
| P4-8 | [x] | 中 | `OfflineTimeoutSeconds` 没写进 `App.config`，多表轮询一轮超 15s 即闪"设备掉线" | `Forms/MainForm.cs:151` | 2026-09-17 |
| P4-9 | [ ] | 中 | 关窗不等在途热泵刷新就释放串口（`Dispose` 未取 `_gate`） | `Forms/MainForm.cs`、`HeatPump/Services/ModbusRtuClient.cs` | |
| P4-10 | [x] | 中 | 自动扫描 `finally` 无条件启用"连接"按钮，再点会打断在跑的采集 | `Forms/MainForm.cs` | 2026-09-17 |
| P4-11 | [x] | 低 | 快速查询翻页按钮状态被 `ToggleQuickControls(true)` 覆盖 | `Forms/MainForm.cs` | 2026-09-17 |
| P4-12 | [x] | 中 | 卡片上改从站地址不回写档案、不落盘，平台按旧地址归属历史与告警 | `Forms/MainForm.cs` | 2026-09-17 |
| P4-13 | [ ] | 中 | 热泵/温控器链路 5 处空 catch，把"串口坏了"显示成"没扫到设备" | `HeatPump/Services/*`、`Thermostat/Services/*` | |
| P4-14 | [ ] | 中 | 热泵故障码两端都是死链路：发布端恒发空串，入库端把"解析失败"当"无故障"（将来会反向恢复告警） | `Forms/MainForm.cs:771`、Worker `Program.cs:990` | |
| P4-15 | [ ] | 中 | 入库端无重试/死信回流，一次瞬时数据库错误即永久丢一条消息 | `MeterIngestionWorker/Program.cs` | |
| P4-16 | [x] | 高 | `sql/public.sql` 是旧 dump 且**缺 `uq_alarm_event_active`**，重放会让告警入库整体失败（42P10 → 整事务回滚） | `sql/public.sql` | 2026-09-17 |
| P4-17 | [ ] | 低 | 4 个报警阈值 `App.config` 键已废弃仍被读取，运维在此调阈值不生效 | Worker `App.config` | |
| P4-18 | [x] | 低 | `MarkLogFailed` 吞异常，`mqtt_message_log` 行永远停在 `pending` | `MeterIngestionWorker/Program.cs` | 2026-09-17 |
| P4-19 | [ ] | 低 | `meter_realtime_history` 还有第二对重复索引（`collect_time DESC`） | `sql/public.sql` | |
| P4-20 | [ ] | 中 | 档案同步：空 `meters` 会把整箱表标离线；`uk_box_slave` 冲突回滚整批 | `MeterIngestionWorker/Program.cs` | |
| P4-21 | [ ] | 低 | `sql/migrations` 混入一份 MySQL 迁移，`apply.sh` 的 glob 永不列出它（004 的不变量对它是假的） | `sql/migrations/` | |
| P4-22 | [ ] | 低 | 阈值解析每条消息重建对象；`RecentMessages` 清理每条消息全表扫描 | Worker `Program.cs`、`Common/MeterThresholds.cs` | |
| P4-23 | [ ] | 低 | 跨线程读写 `DateTimeOffset?` 非原子；UI 清空仪表盘与后台轮询存在集合并发 | `Domain/MeterManager.cs`、`Forms/MainForm.cs` | |
| P4-24 | [ ] | 低 | `ModbusService.IsConnected` 在锁外访问 `_serialPort`，与 `Dispose`/`Connect` 有竞态窗口 | `Services/ModbusService.cs` | |

### 第七批：网页端第一轮（W —— 第二轮审查，2026-09-17）

> 网页端（`RuoYi-Vue-master`）此前从未被系统审查，本轮首次覆盖。
> 结论：**功能仍在但多处带病**——质量历史链路两端都是坏的、阈值统一很可能没生效、
> 新接口基本没有权限控制。详见"十三、第二轮审查实施记录"。

| 编号 | 完成 | 严重度 | 问题摘要 | 主要位置 | 完成日期 |
|:---:|:---:|:---:|---|---|:---:|
| W-1 | [x] | 致命 | 质量历史 SQL 引用未 join 的别名 `ms` → 该接口恒定 500，且 SQL 原文被回给浏览器 | `MeterCardMapper.xml:342` | 2026-09-17 |
| W-2 | [x] | 致命 | 历史追溯"电能质量"Tab 必崩（`categoryInfo` 缺 `quality` → `undefined.title`） | `meter/history/index.vue` | 2026-09-17 |
| W-3 | [x] | 高 | "设备告警状态"页累计电能字段名不存在（`forwardActiveEnergyKwh`）→ 永远显示 `--` | `meter/alarm-record/index.vue` | 2026-09-17 |
| W-4 | [x] | 中 | `reactivePowerKvar` 从不被赋值，详情抽屉与告警页"无功功率"恒 `--` | `MeterCardServiceImpl.java` | 2026-09-17 |
| W-5 | [x] | 中 | 严重数值异常复用 `FAULT` → 电压/频率问题显示成"通信故障"；PF 用 0.5、电压用 180~260，与阈值表 0.85/198~242 并存，页面自相矛盾 | `MeterCardServiceImpl.java` | 2026-09-17 |
| W-6 | [ ] | 高 | 电表各接口基本无 `@PreAuthorize`，且全链路无站点级数据隔离 | `MeterCardController.java`、`SecurityConfig.java:108` | |
| W-7 | [ ] | 高 | 网页端**很可能读不到** `meter_threshold`（Java 读 MySQL 从库，DDL 只存在于 PG 脚本），静默退回硬编码并缓存 5 分钟 —— P1-1 在网页端等于没做。**已做**：失败不再静默（见十四），可看出根因；表要在哪建仍待现场定。**2026-09-23 已定论**：`ma.meter_threshold` **不存在**（实测 `Table 'ma.meter_threshold' doesn't exist`；`ma` 库共 13 张表，无此表），所以网页端目前**只能**走硬编码兜底 —— 要么在 MySQL `ma` 建表并把阈值同步过去，要么把阈值读取改到 PG 数据源 | `MeterCardServiceImpl.java:31,466`、`sql/migrations/003` | |
| W-8 | [x] | 高 | 阈值表的语义字段 Java 侧未实现：`is_alarm_enabled` 无任何读取处 | `MeterCardMapper.xml:23`、`MeterThreshold.java` | 2026-09-17 |
| W-9 | [ ] | 中 | 仪表盘能耗趋势把 30 天读数全量拉进 JVM，分桶 O(读数×桶数) 且每桶 `Calendar.clone()` | `MeterCardServiceImpl.java:337`、`MeterCardMapper.xml:372` | |
| W-10 | [x] | 中 | `/quality/page` 手写分页整型溢出（`(pageNum-1)*pageSize`），`pageSize` 无上限 | `MeterCardController.java:84` | 2026-09-17 |
| W-11 | [x] | 中 | 全局异常处理器把 `e.getMessage()`（含 SQL 片段与列名）直接返回浏览器 | `GlobalExceptionHandler.java:96,107` | 2026-09-17 |
| W-12 | [ ] | 中 | 拓扑保存"全删全插" + 毫秒时间戳当区域主键，并发保存会互相破坏 | `MeterTopologyServiceImpl.java:89,130` | |
| W-13 | [ ] | 中 | 嵌套 `@DataSource` 用的是普通 `ThreadLocal`（非栈），内层返回即清空上下文，后续查库静默走错库 | `DynamicDataSourceContextHolder.java:19` | |
| W-14 | [ ] | 低 | 历史行也被 `applyStatus` 判成"设备掉线"（历史查询不取 `meter_status` 字段） | `MeterCardServiceImpl.java:641` | |
| W-15 | [ ] | 低 | 趋势分桶与标签用 JVM 默认时区，服务器时区非 GMT+8 时 x 轴整体偏移 | `MeterCardServiceImpl.java:301+` | |
| W-16 | [ ] | 低 | 热泵 `alarm=0` 被忽略（返回全部）；`/alarms` 无条数上限 | `HeatPumpMonitorController.java:66,90` | |
| W-17 | [ ] | 低 | 拓扑 SQL 非幂等且带 `DROP TABLE`、`parent_id` 硬编码 2000；演示数据脚本是 MySQL 方言 | `RuoYi-Vue-master/sql/` | |
| W-18 | [ ] | 低 | `MeterTelemetryRepository` 是死代码且配置前缀不一致，但它的质量历史 SQL 反而 join 正确（典型的"改了 A 忘了 B"） | `repository/MeterTelemetryRepository.java:101` | |
| W-19 | [ ] | 低 | `getRealtimeDetail` 查不到返回 200 + `data=null`，且不过滤 `is_enabled` | `MeterCardController.java:136` | |
| W-20 | [x] | 高 | 模拟数据模式在页面上没有任何标识，开着开关看首页会把演示读数当成现场数据 | `layout/components/AppMain.vue` | 2026-09-17 |
| W-21 | [x] | 中 | 首页与实时页定时器在 keep-alive 下泄漏（只在 `beforeDestroy` 清理）→ 离开页面后仍每 15s 请求 | `views/index.vue:185`、`meter/realtime/index.vue:79` | 2026-09-17 |
| W-22 | [x] | 中 | 拓扑页 `loadTopology` 无 catch，接口失败伪装成"尚未创建拓扑区域"空态，此时保存会用空配置覆盖现有区域 | `meter/topology/index.vue:103` | 2026-09-17 |
| W-23 | [x] | 中 | 详情抽屉无请求竞态保护，快速切换设备会显示另一台设备的读数 | `meter/components/realtime-detail.vue:62` | 2026-09-17 |
| W-24 | [ ] | 中 | 质量页 `Promise.all` 耦合：统计接口失败会连列表一起清空 | `meter/quality/index.vue:56` | |
| W-25 | [x] | 低 | 能耗页切换设备重复发起同一次分析请求（手动调用 + 路由 watch 各一次） | `meter/energy/index.vue:104` | 2026-09-17 |
| W-26 | [ ] | 中 | 首页"负荷排行"点正常设备也进"设备告警状态"页（《任务说明》第 2 条的可复现入口） | `views/index.vue:266,293` | |
| W-27 | [x] | 低 | 实时页自动刷新会用"未点搜索的输入值"过滤列表（输入框直绑 `queryParams`） | `meter/realtime/index.vue:13,81` | 2026-09-17 |
| W-28 | [ ] | 低 | 前端仍各存一份硬编码判定阈值（兜底路径），与后端口径可能漂移 | `meter-utils.js:41`、`meter-simulation.js:56` | |
| W-29 | [ ] | 低 | 电表菜单硬编码进 `constantRoutes`，与 `sys_menu` 权限体系并存，现场可能重复/失配 | `router/index.js:77` | |
| W-30 | [ ] | 低 | 多处请求失败静默清空表格，用户无法区分"无数据"与"请求失败" | 实时/质量/历史页 | |

### 第八批：网页端第三轮（W3 —— 2026-09-23）

> 起因：修拓扑页悬浮球"收起后再点无反应"时，顺带把网页端又过了一遍（页面级 + 组件/共享层两路并行审计）。
> 结论：**数据源本身可能已经脱节（见 W-31）**，另有 1 处会覆盖新数据的竞态、一批"看着有反应其实没有"的交互，
> 以及跨页重复实现。**W-32 ~ W-45 已于 2026-09-23 当日修复并逐项实测**（见文末"第八批修复与验证记录"）；
> W-6 / W-31 / W-46 未动（都需要先定方向），W-40 只修了前端误导、后端筛选仍未支持。

| 编号 | 完成 | 严重度 | 问题摘要 | 主要位置 | 完成日期 |
|:---:|:---:|:---:|---|---|:---:|
| W-31 | [ ] | 高 | 网页端读的 MySQL `ma` 数据**停在 2026-07-30**（实测 `meter.last_seen_time`、`meter_realtime_latest.collect_time`、`meter_energy_latest.collect_time`、`meter_realtime_history.collect_time` 四者最大值同为 `2026-07-30 00:08:14`），而入库服务写的是 PostgreSQL → 页面上"实时"数据实为两个月前的快照。**待定方向**：哪份库是网页端的权威数据源 | `ma` 库 vs `MeterIngestionWorker` | |
| W-32 | [x] | 高 | 历史追溯页明细表无请求序号守卫（同页趋势有 `trendRequestId`）→ 快速切设备/时间段时旧响应覆盖新数据 | `meter/history/index.vue:181` | 2026-09-23 |
| W-33 | [x] | 中 | 电能质量页「重置」按钮空转：三个 `el-form-item` 缺 `prop`，`resetForm()` 不重置任何字段 | `meter/quality/index.vue:6-8,78` | 2026-09-23 |
| W-34 | [x] | 中 | 异常设备页严重度排序把最严重的"通信故障"排到最后：`severity[code] \|\| 99`，而 `FAULT: 0` 是 falsy。**顺带发现**首页与异常页各存一张严重度表且 NODATA/VOLTAGE_BAD/PF_LOW 先后互相矛盾，已合并为 `getMeterSeverityRank` | `meter/exception-devices/index.vue`、`meter-utils.js` | 2026-09-23 |
| W-35 | [x] | 中 | 能耗分析把 `null` 当 0：`Number(null) === 0` 能穿过 `filter(Number.isFinite)`，缺失采样点被计入均值/峰值并画成 0 kW，KPI 显示 `0.00 kWh`。首页负荷排行与柱状图同一毛病，一并修 | `meter/energy/index.vue`、`views/index.vue` | 2026-09-23 |
| W-36 | [x] | 中 | 热泵监控 `refresh` 只有 `try/finally` 没有 `catch` → 每 15 秒一次未处理的 Promise 拒绝，界面无任何失败提示；定时器也缺 `activated/deactivated` | `heatpump/monitor/index.vue` | 2026-09-23 |
| W-37 | [x] | 中 | 异常设备页只读 `/cards` 的 `dashboard` 分组，`toolbar`（外部串口）设备永远不出现，页面却写"展示全部设备" | `meter/exception-devices/index.vue` | 2026-09-23 |
| W-38 | [x] | 中 | 电能质量页点「详情」固定发两次详情请求：`showDetail` 直接 `open()`，同时又改 query 触发 watcher 再 `open()` 一次 | `meter/quality/index.vue` | 2026-09-23 |
| W-39 | [x] | 中 | 能耗页切换时间预设会顺带重拉一次全量设备卡片；该请求失败时 `meterId` 被置空，改时间范围却把已选设备清掉 | `meter/energy/index.vue` | 2026-09-23 |
| W-40 | [x] | 中 | 热泵「活动告警」忽略站点/状态筛选（`/alarms` 不接收参数），筛选器看着生效实际不生效。**前端已修**：筛选表单只在「实时状态」页签显示，告警页签给出说明；**后端按站点筛选仍未支持**（可选增强） | `heatpump/monitor/index.vue` | 2026-09-23 |
| W-41 | [x] | 低 | 告警记录页数字保留 3 位小数、其余页面 2 位 → 同一读数两页显示不同；页内还各写一份 `formatNumber`/`statusType` | `meter/alarm-record/index.vue` | 2026-09-23 |
| W-42 | [x] | 低 | 同一含义多套实现：位置文本三种写法、占位符 `--` 与 `----` 混用、`/cards` 三种消费方式、`meterId` 在 String/Number 间混用。**已收敛**到 `formatMeterLocation` / `formatMeterAddress` / `toMeterNumber` / `getMeterSeverityRank`；**状态文案仍与后端 `statusText` 不一致**（前端「通信故障」vs 后端「通信异常」），属措辞决定，未擅改 | `meter-utils.js`、各页面 | 2026-09-23 |
| W-43 | [x] | 低 | 死代码：`getEnergyTrend` 与 `getDashboardEnergyTrend` 完全重复（前者无调用点，已删）；`formatMeterDateTime` 名不符实且无人调用（已删） | `api/system/meter.js`、`meter-utils.js` | 2026-09-23 |
| W-44 | [x] | 低 | 热泵"控制器"列在 `controller_name` 为空时显示"地址 undefined" | `heatpump/monitor/index.vue` | 2026-09-23 |
| W-45 | [x] | 低 | 空串未并入"缺失"判定（`Number('') === 0`）：相电压/电流会显示 `0.0`，空串 PF 被判低功率因数。已抽出 `hasMeterValue` 统一判空 | `meter-utils.js`、`meter-device.vue` | 2026-09-23 |
| W-46 | [ ] | 低 | 拓扑页交互缺口：列表视图只能改**区域**不能改**配电箱**；"未分配设备"是移出区域的唯一出口。属新增功能，未做 | `meter/topology/index.vue` | |
| W-47 | [x] | 低 | 首页 15 秒轮询直接调 `loadSnapshot`，而它一进来就置整表 `loading` → 每 15 秒闪一次遮罩。**注意**：这是真实但次要的问题，**不是**"首页一直转圈"的根因（根因见 W-48）；已改为轮询走静默模式，失败时也不再清空卡片 | `views/index.vue` | 2026-09-23 |
| W-48 | [x] | 致命 | **本轮自己引入的回归**：首页模板里用了 `formatMeterLocation`，但只 import 了、**没挂到 `methods`** → 表格一有数据渲染就抛 `TypeError`，Vue 中断 patch、组件停在上一帧 → 页面永远挂着 loading 遮罩（用户报的"首页一直在转圈"）。已补 `methods`，并写了 `artifacts/db-cleanup/check-template-methods.py` 全量扫描（先去掉 import 再判断，避免多行 import 被误判成已暴露） | `views/index.vue:311` | 2026-09-23 |

---

## 二、问题明细

### P0-1 无全局异常兜底，`async void` 事件处理器裸奔

**现象**：`Program.Main` 只有 `Application.Run(new MainForm())`，没有 `Application.ThreadException`、
`AppDomain.CurrentDomain.UnhandledException`、`TaskScheduler.UnobservedTaskException`，也没有
`Application.SetUnhandledExceptionMode`。配合满地的 `async void` 事件处理器，任何漏网异常都会弹出系统默认
未处理异常框，用户点错就退出，而且不留任何痕迹。

**可复现路径**：
- `MainForm.BtnAutoScan_Click` 里两处 `await PublishBoxRegistryAsync(...)` 完全没有 try/catch
  （同文件 `DeleteMeterCard_Click` 反而包了，说明是遗漏而非设计）。Broker 不可达时点"自动扫描"即触发。
- `RefreshTimer_Tick` 是 `async void`，`Task.Run` 里的解析异常会顺着 await 冒出来。

**改进方案**：`Program.cs` 装三道兜底并把堆栈落盘；所有 `async void` 处理器用 try/catch 包住整个方法体。

---

### P0-2 报文解析长度守卫小于实际消费字节数（3 处越界）

`Services/MeterDataService.cs` 三个读取方法的守卫全部偏小：

| 方法 | 请求量 | 实际消费 | 原守卫 | 触发结果 |
|---|---|---|---|---|
| `ReadRealTimeData` | 58 寄存器 = 116B | 116B（`Frequency` 读 112~115） | `< 112` | 112~115B 时越界 |
| `ReadEnergyData` | 12 寄存器 = 24B | 24B | `< 16` | 16~23B 时越界 |
| `ReadPowerQualityData` | 58 寄存器 = 116B | 116B | `< 40` | 40~115B 时越界（最松） |

只要设备固件返回的寄存器数与预期不一致（换型号、换固件、协议表对错）就会命中。而
`MeterManager.PollAll` 有两个空 `catch` 把它静默吞掉（仪表盘路径 → 数据无声丢失），
主串口路径没有 catch（→ 走 P0-1 的崩溃）。

**附带问题**：`ReadPowerQualityData` 读完 `VoltageTHDC` 后没有 `offset += 4` 就直接 `offset += 40`，
与其他字段写法不一致，需对照协议表核对（已加 TODO 标注，归入 P2-1 一并处理）。

**改进方案**：守卫改成"本方法实际消费的最大字节数"；空 catch 全部替换为记日志 + 失败计数。
更根本的做法是表驱动寄存器映射（P2-1）。

---

### P0-3 写通讯参数会丢弃校验位、可写入非法波特率索引

这是本次审查中**唯一会造成物理损失**的一类缺陷 —— 参数写错后表就从总线上消失，需现场逐台恢复。

- `MeterDataService.GetParityIndex` 无条件 `return 2`：用户在界面选的校验位被完全丢弃，永远写 8E1。
- `MeterDataService.GetBaudRateIndex` 未命中返回 `-1`，被截成 `0xFFFF` 写进波特率寄存器。
- `MainForm.BtnWriteParams_Click` 用 `byte.Parse` / `int.Parse` 而非 `TryParse`；且没有二次确认
  （反而"清除电能"有确认框）。

**改进方案**：补全 parity 双向映射；索引未命中直接拒绝下发并返回明确原因；写参数前二次确认
（显示"从 X 改为 Y"），写后回读比对，不一致立即告警。

---

### P0-4 分合闸遥控无二次确认、无操作审计

`MainForm.ExecuteControlAction` 对 DO1/DO2 合闸分闸直接下发，点一下就动。清零有确认框，
切断供电的操作反而没有。也没有"谁在什么时候对哪台表做了什么"的记录。

**改进方案**：分合闸加含设备名 + 从站地址的二次确认；所有控制动作写审计日志
（操作时间、操作机器/账号、目标从站、动作名、结果）；后续接入角色权限。

---

### P0-5 关窗时先释放资源再调 base，且不等在途轮询

`MainForm.OnFormClosing` 在 `base.OnFormClosing(e)` **之前**就把串口和 MQTT 全 Dispose 了，
也不检查 `e.Cancel` —— 一旦关闭被取消，程序会带着已释放的串口继续运行。
同时 `_refreshTimer.Stop()` 不等待 `_isRefreshing` 落地，线程池里正在读串口的 `Task.Run`
会撞上已 Dispose 的 `SerialPort`。

**改进方案**：`OnFormClosing` 只负责停定时器 + 判 `e.Cancel`；资源释放搬到 `OnFormClosed`，
并在释放前等待在途采样结束（有超时上限，避免卡死退出）。

---

### P0-6 Worker MQTT 重连只做一次，失败后永久掉线且无感知

`MeterIngestionWorker.RunMqttSubscriberAsync` 的 `DisconnectedAsync` 里 `Task.Delay(5s)` 后只重试
**一次**；如果那次 `ConnectAsync` 仍失败，异常在事件回调里消失，就没有下一次了 ——
服务进程活着、控制台没输出、数据全丢，运维完全看不出来。

**改进方案**：改成带指数退避（5s → 10s → … → 上限 60s）的重连循环；每次尝试都记日志；
定期打印连接状态心跳，让"活着但掉线"变成可观测状态。

---

### P0-7 入库队列无界；上位机无单实例保护

- `MeterIngestionWorker` 的 `BlockingCollection` 没有容量上限，PG 一挂就无限堆积到 OOM。
- `App.config` 的 `MqttClientId` 是写死常量，且上位机没有单实例互斥。开两个实例 →
  同 ClientId 互相顶下线 → 无限重连风暴 + 争抢同一个 COM 口。

**改进方案**：队列设上限并定义满时策略（丢最旧 + 计数告警）+ 定期输出队列深度；
上位机加 `Mutex` 单实例；ClientId 追加机器名后缀，保证多机部署天然不冲突。

---

### P0-8 全项目零日志，14 处空 catch 静默吞异常

《任务说明》第一条要求"要有完整的运行过程日志"，实测未落地：上位机全项目 **0 处**日志框架调用
（仅 `HeatPumpWorkspaceConfigStore` 用 `StreamWriter` 写配置文件），14 个空 catch；
Worker 只有 `Console.WriteLine`，关掉窗口就什么都不剩，而且它是控制台程序、没有服务化、
PC 重启后不会自启。

**改进方案**：引入统一日志（滚动文件 + 分级 + 结构化字段 `meterId`/`slaveAddress`/`registerAddress`）。
必须覆盖：串口收发结果、每轮轮询成功/失败台数、MQTT 连接与发布结果、入库成功/失败、
报警状态跃变、所有异常堆栈、以及 P0-4 的控制审计。Worker 后续用 Windows 服务或 NSSM 托管 + 开机自启。

**实现说明**：本项目为 .NET Framework 4.8 + packages.config，当前环境无 VS/nuget.exe，
`dotnet build` 不会还原 packages.config 依赖，因此**未引入 NLog/Serilog**，而是自建零依赖的
`AppLogger`（滚动文件、按级别过滤、线程安全、异步落盘）。接口刻意做成与主流日志库同形
（`Info/Warn/Error/Debug` + 结构化上下文），后续如需替换为 NLog 只改 `AppLogger` 内部实现即可。

---

### P1-1 合格/不合格判定有三套独立实现

> **2026-08-26 补充（合并仓库后发现）**：实际是**四套**，而且网页端那套才是唯一有工程依据的。
> 网页端仓库里已存在一份 [`RuoYi-Vue-master/docs/仪表监测判定标准.md`](RuoYi-Vue-master/docs/仪表监测判定标准.md)，
> 写明了判定通则、状态码语义、数值阈值与边界示例。三端阈值实测对比：
>
> | 判定项 | 上位机 `DetermineMeterStatus` | Worker `App.config` | 网页端判定标准文档 |
> |---|---|---|---|
> | 相电压合格区间 | 180 ~ 260 V | 180 / 260 V | **198 ~ 242 V**（220V ±10%） |
> | 总功率因数下限 | 0.5 | 0.5 | **0.85** |
> | 电压 THD | 未判定 | 未判定 | **≤ 5 %** |
> | 电流 THD | 未判定 | 未判定 | **≤ 8 %** |
> | 电压不平衡度 | 未判定 | 未判定 | **≤ 2 %** |
> | 电流不平衡度 | 未判定 | 未判定 | **≤ 3 %** |
> | 频率 | 40 ~ 70 Hz | 未判定 | 未判定 |
> | 缺数据（null）处理 | 非空 float，0 视为真实值 | 同左 | **null 不判风险，交由状态码决定** |
>
> 也就是说：同一台 210 V、PF 0.7 的表，在上位机和 Worker 眼里"正常"，在网页端眼里是"电压越限 + 功率因数偏低"。
> 而网页端那份文档里"null 不参与数值判定"的规则，正是 P1-5 应该向其对齐的做法。
>
> **因此 P1-1 的任务从"制定标准"变成"把网页端这份标准提升为三端唯一口径"**：
> 以该文档为基准建 `meter_threshold` 表（站点/配电箱/单表三级覆盖），上位机与 Worker 改为读库，
> 该 md 文档同步移到仓库根 `docs/` 并声明为唯一权威口径。

**原始记录**：

- 上位机 `MainForm.DetermineMeterStatus`：硬编码 50V / 40~70Hz / 180~260V / PF<0.5
- Worker：走 `App.config` 的 `AlarmVoltageLow/High`、`AlarmCurrentHigh`、`AlarmPowerFactorLow`
- 网页端：又是一套

同一台表在三个界面上可以给出三种结论。阈值还是全局的，不区分接线方式和电压等级 ——
`BasicConfig.WiringMode` 定义了却从未使用，CT/PT 变比也没有进入判定。

**改进方案**：阈值下沉到数据库（`meter_threshold`，支持站点/配电箱/单表三级覆盖），三端统一读同一份；
同时按《任务说明》产出一份《判定标准.md》作为唯一口径文档，代码与文档互为校验。
**这条是最优先的架构性修正**，因为它同时解掉网页端多个遗留问题（风险统计不准、首页指标不一致）。

---

### P1-2 电表离线不写 `alarm_event`

`MonitorOfflineMetersAsync` 里，热泵那段会写 `heat_pump_alarm_event`，电表那段只
`UPDATE meter_status.is_online`，既不写 `alarm_event`、也没有恢复事件。

这正是《任务说明》里"'报警记录'本质上不是历史报警事件，而是根据当前设备状态临时生成的一条状态说明"
的根因 —— 不是前端页面命名问题，是后端根本没有产生这类事件。

**改进方案**：电表离线/恢复成对写入 `alarm_event`，网页端才可能有真实报警历史。

---

### P1-3 `meter_*_latest` 无乱序保护

热泵和温控器的 upsert 都带单调守卫 `WHERE ... last_collect_time <= EXCLUDED.last_collect_time`，
但电表的 `UpsertRealtimeLatest` / `UpsertEnergyLatest` / `UpsertPowerQualityLatest` 都没有 ——
迟到消息会把新值覆盖回旧值。`ProcessWorkers > 1` 时概率明显上升。

**改进方案**：三张 latest 表补同样的时间守卫。

---

### P1-4 电表消息无 `MessageId` / 版本号

热泵、温控器有 `MessageId` + 数据库唯一约束去重（`TryInsertHeatPumpMessageLog`），
电表却只有 `IsDuplicateEnvelope` 的内存 10 秒窗口 —— 重启即失效、多实例失效。
且指纹只算 payload 不含 topic，若同时开启 `PublishLegacyTopic` / `SubscribeLegacyTopic`，
同一 payload 的第二份会被误判为重复丢弃。电表消息**完全没有版本字段**，将来改结构没有兼容手段。

**改进方案**：`MeterTelemetryMessage` 补 `MessageType = "meter.telemetry.v1"` + `MessageId`
（热泵那套已经是好范本，照抄即可），改走库级去重。

---

### P1-5 / P1-6 `0` 值不可区分 + 报警无去抖

`Domain/Models.cs` 的 DTO 全是非空 `float`。任何部分失败都落成 0，而 Worker 的
`EvaluateRealtimeAlarms` 直接拿裸值判：0V 触发"电压过低"，空载时 PF≈0 触发"功率因数过低"。
**没有去抖、没有滞回、没有带电/带载前置条件** —— 会持续产生误报，也是前端告警信噪比差的源头。

**改进方案**：DTO 改 `float?`，缺数据写 NULL；报警加前置条件（判低压前先确认已带电）、
连续 N 次满足才置位、恢复用不同阈值做迟滞。

---

### P1-7 采样节奏用 tick 计数而非时钟

`RefreshTimer_Tick` 用 `_refreshTickCount % _energyIntervalSeconds` 判断该不该采，
但 `_isRefreshing` 跳过的 tick 不累加计数 —— 一轮轮询超过 1 秒（多台表 × 最长 500ms 超时，很容易），
`EnergyIntervalSeconds=60` 实际就变成 120、180 秒。配置值和真实行为对不上。

**改进方案**：三类采样各自记录 `_lastRealtimeAt` / `_lastEnergyAt` / `_lastQualityAt`，按时钟判到期。

---

### P1-8 时间基准不统一

遥测用 `GetBeijingNow()`（固定 +08:00），档案消息用 `DateTime.Now`，界面掉线判定用
`DateTime.Now - LastSuccessfulReadTime`（本地时间）。机器时区一旦不是 +08:00，三者互相错位。

**改进方案**：全链路统一 `DateTimeOffset`，存储用 UTC，只在展示层转 +08:00。

---

### P2-1 两种电表协议字节序/量纲各写一套

> ### ⚠️ 2026-08-26 更正：本条原始描述过重，需要纠正
>
> 初稿写的是"两种协议字节序/量纲各写一套，落进同一批列"，容易被读成**两者算出来的值不一致**。
> 第三批实施时补了等价性测试（`TPEM.Tests/ModbusCodecEquivalenceTests.cs`），
> 用 500 组随机字节 + 13 个典型值逐一比对，结论是：
>
> - **字节序结论相同**：Legacy 的 `GetReversedBytes` 反转 4 字节，AMC96L 的 `(reg[i]<<16)|reg[i+1]`
>   再 `BitConverter`，**两种写法完全等价，都等于大端 ABCD**。不存在字节序分歧。
> - **量纲结论也相同**：缩放系数不同（Legacy 比率 ×100、AMC96L 整数 ×0.01；功率 AMC96L ×1000）
>   是因为两种表的**原始编码本来就不同**，换算后的业务值单位一致。
>   `RegisterMapTests.两种协议对同一物理量给出相同单位的结果` 就是在钉这一点。
>
> 所以此前把网页端"电能质量风险统计不准"部分归因到这里是**不成立的**，那条应重新定位
> （更可能是 P1-1 的阈值分歧与统计只覆盖当前分页，前者已在第二批解决）。
>
> 本条真正的问题是**可维护性**，仍然值得做，也已经做了：同一件事写两遍、都没有测试、
> 都没有边界检查，偏移与缩放全是散落的裸字面量 —— 任何一次协议表改动都得靠人眼数字节。

**原始记录**：

- Legacy 用 `GetReversedBytes`（4 字节全反转），AMC96L 用 `ParseFloat`（高低字组合）—— 两套写法，
  没有共用、也没有测试。
- 缩放散落：THD 是 `float × 100` vs `raw / 100`；不平衡度 `× 100` vs `/ 10`；
  功率 AMC96L `× 1000` 转 W、Legacy 直接用。

**改进方案（已实施）**：抽 `ModbusCodec` 统一字节序解码；把寄存器映射做成
`RegisterField<T>` 表（偏移、类型、字节序、缩放、单位都是数据）；长度守卫由映射表自动推导，
不会再与解析代码脱节；补 34 条测试锁定行为。

---

### P2-2 / P2-3 串口无统一仲裁器 + 收帧不健壮

`ModbusService` 只有一把 `lock`。抢这把锁的有三方：线程池上的轮询、UI 线程上的手动按钮、
以及 `SharedBusHeatPumpDataService` 里同样 `Task.Run` 的热泵读取。后果是 UI 线程可能被阻塞数秒，
且 Modbus RTU 要求的 3.5 字符帧间隔完全没有保证。

收帧本身也不健壮：
- `ReadResponse` 超时后只要收到 >0 字节就返回，CRC 对整段算 —— 残帧/串话直接判失败且不做帧重同步；
  每次读还固定多睡 10ms。
- `ReadHoldingRegisters` **不校验 `response[0]` 从站地址**，也不校验 `response[2]` 字节数与请求量匹配。
  总线串话时有机会把别的从站的回包当成自己的数据接受。
- 返回值统一是 `null`，"未连接 / 超时 / CRC 错 / Modbus 异常码"四种情况无法区分 → 无法做通信质量诊断。

**附带浪费**：`SharedBusHeatPumpDataService.ReadTelemetryAsync` 对每个模块（0~15）都重读一次控制器
设置块，一次全扫 32 次事务而不是 17 次 —— 共享总线时对电表轮询的挤占是双倍的。

**改进方案**：每个 COM 口一个串行请求队列（`Channel<ModbusRequest>` + 单消费者），
带优先级（手动控制 > 定时轮询）、超时预算、帧间隔、重试次数、每设备失败计数与熔断；
返回 `ModbusResult { Status, Data }` 并统计成功率/平均响应时间/CRC 错误数/超时数。

> **前置确认（2026-08-26 已确认）**：《任务说明》里"热泵和电表是否共用同一条 RS485"——
> 答复是**不共用**，热泵走独立串口（`App.config` 的 `HeatPumpPortName`）。
> 因此：
> - 本条**保持 P2，不升为 P0**；
> - `SharedBusHeatPumpDataService`（共享总线模式）不是现网在用的路径，
>   它那个"每个模块都重读一次控制器设置块、全扫 32 次事务而不是 17 次"的浪费也就不紧急了；
> - 仍需仲裁器的原因缩小为**同一条电表总线内部的争抢**：定时轮询在线程池、
>   手动按钮（读参数/写参数/分合闸/探测）在 UI 线程，两者抢 `ModbusService` 那一把锁，
>   UI 线程可能被阻塞到一整轮轮询结束；以及 Modbus RTU 要求的 3.5 字符帧间隔仍无保证。

---

### P2-4 ~ P2-8 其余可维护性问题

- **P2-4**：`BtnDiscover_Click` → `DiscoverMeterAddress` 在 UI 线程全量扫 1~247，单次探测最长 500ms，
  最坏约 2 分钟界面完全无响应。同文件的 `BtnAutoScan_Click` 已用 `Task.Run` 做对了，照抄并补进度/取消即可。
  它结尾还无条件 `Disconnect()` 并置"未连接" —— 轮询正在跑时探测一次就把采集打断了。
- **P2-5**：`MainForm` 3402 行。建议切分为 `AcquisitionScheduler`（采样节奏）、`SerialBusArbiter`（串口仲裁）、
  `TelemetryPublisher`（含离线缓冲）、`MeterRepository`（查询），`MainForm` 只留 UI 绑定。
- **P2-6**：`UpdateMeterCardLabels(dynamic tags, ...)` 用 `dynamic` 传控件引用 —— 无编译期检查、
  走 DLR 有开销、控件结构一改就是运行时 `RuntimeBinderException`。换强类型 `MeterCardTags`。
- **P2-7**：`btnMeterProtocol_Click` 切换型号时 `new MeterManager(...)` 直接换掉管理器但不重建面板，
  已有卡片指向不存在的表，`UpdateAllCardLabels` 里 `Find` 返回 null 后静默 `continue`，
  界面停在旧值上不刷新也不报错。
- **P2-8**：`MqttContracts.cs` 与 `SharedContracts.cs` 是逐字复制（后者缩进还是错乱的），只有命名空间不同。
  任何一端改字段，另一端不会有编译错误，只会在运行时静默丢字段。应建 `TPEM.Contracts` 类库两端引用，
  并统一 Worker 的 `LangVersion 7.3` 与主项目的 `latest`。

---

### P3-1 MQTT 断线期间数据直接丢

`MqttPublisherService` 没有离线缓冲、没有遗嘱消息（LWT），`WithCleanSession()` + QoS 1 组合等于
放弃会话补投。更糟的是 `PublishAllMetersAsync`：**第一台表发布失败就 `return`**，
同一轮剩下所有表的数据一并丢弃。而且发布是在 timer tick 里逐台 `await`，网络一卡整个采样周期就被拖长。

**改进方案**：改成"采样入队 + 后台单消费者发送"；断线时写本地环形缓冲或 SQLite 待补发；
加遗嘱消息（上位机掉线平台立刻可知，比 15 秒超时判断快得多）；ClientId 唯一化后改用持久会话。

---

### P3-2 ~ P3-4 Worker 性能与并发

- **P3-2**：`HandleTelemetryEnvelope` 为一条消息开三次连接（写日志、事务、标记已处理）。
  更关键的是 `ShouldWriteRealtimeHistory` **每条消息**都对历史表跑
  `ORDER BY collect_time DESC LIMIT 1` —— 1Hz × N 台，这是全系统最热的查询，查的又是增长最快的表。
  改进：三步合并到同一连接同一事务；`ShouldWrite*` 改内存缓存每表最后写入时间（启动时预热），DB 只作兜底。
- **P3-3**：`mqtt_message_log` 每条消息一行，10 表 1Hz ≈ 86 万行/天，无保留策略。
  改进：只记失败 + 按比例采样成功，或加分区 + 定时清理。
- **P3-4**：`ProcessWorkers > 1` 时同一台表的消息被不同 worker 并行处理，而 `ShouldWrite*` 是读库判定的 ——
  两条消息可能同时判定"该写"，或互相覆盖。默认值 1 所以未暴露，但配置项摆着就是陷阱。
  改进：按 `meterId` 哈希分区到固定 worker 保证单表串行；或删掉该配置项并注明原因。

---

### P3-5 ~ P3-8 工程化

- **P3-5**：Worker 引用了 `ScottPlot` / `SkiaSharp` / `HarfBuzzSharp` —— 无头控制台入库服务不需要绘图库，
  108 条 MSB3277 版本冲突警告基本都在这个项目。同时引用 `MySql.Data` 和 `Npgsql`；
  `sql/alarm_event.sql` 还是 MySQL 反引号方言，与实际 PG 库不一致；Worker 里存 PG 连接串的字段
  叫 `_mysqlConnectionString`。这些遗留会让下一个接手的人判断错运行环境。
- **P3-6**：HeatPump 模块用了 `?` 可空注解，但 csproj 没开 `<Nullable>`，6 条 CS8632 说明
  **这些注解完全不生效**，一点 null 安全都没提供。
- **P3-7**：没有任何测试项目、没有 CI（`.github/` 下只剩几个无关的 java-upgrade 钩子脚本）。
  建议建 `TPEM.Tests`，第一批覆盖报文解析、字节序、量纲换算、阈值判定、`ShouldWrite*` 这五处 ——
  都是纯函数，好测且正是当前 bug 密度最高的地方。
- **P3-8**：`meter_realtime_history` 上有 4 个索引，其中 `idx_meter_realtime_history_meter_time` 与
  `uq_meter_realtime_history_meter_time` 是同一组列的重复索引，写入路径要多维护一份。

---

### X-1 ~ X-4 并行处理项

- **X-1**：`App.config` 的 `Password=12345` 已提交进仓库（`git ls-files` 确认被跟踪）；
  MQTT 用户名口令同样明文、无 TLS、无 ACL。改进：连接串移出仓库（环境变量 / `configSource` 指向
  未跟踪文件 / DPAPI 加密节），**并轮换现有口令**（已泄露到 git 历史）；MQTT 启用 TLS +
  每客户端独立账号 + 主题级 ACL。
- **X-2**（已于 2026-08-26 解决，方式与原建议不同）：原状况是 `RuoYi-Vue-master` 在索引里为 mode `160000`
  的 gitlink，但仓库根没有 `.gitmodules`，别人 clone 后只会得到空目录，`git submodule update` 直接报
  "No url found for submodule path"；且记录的 `2f29fde` 已落后网页端实际进度 4 个提交
  （这就是 `git status` 里那个 ` M RuoYi-Vue-master` 的真实原因，并非工作区脏）。
  **实际处理**：没有补 `.gitmodules`，而是把两个仓库合并成了单仓库 —— 见"七、仓库结构调整"。
  现在 `RuoYi-Vue-master/` 是普通跟踪目录（682 个文件），一次 `git clone` 即可拿到全部代码。
- **X-3**：`.claude/buildcheck-adaptive-y/` 提交了 `MeterAcquisition.exe` 和一批 dll；
  仓库跟踪 5.9MB 的 PDF 协议手册（`.git` 已 49MB）；`程序启动.txt` 混着无关的采购催办邮件草稿。
  协议地址表 xlsx 属于必要的工程资料（P2-1 的 TODO 需要对照它核对），**不在清理范围内**。
- **X-4**：上位机绕过 API 直连 PostgreSQL 做历史查询，与《任务说明》"客户端调用 api 取数据"的架构相悖，
  意味着每台产线 PC 都得持有数据库账号。

### X-5 温控器链路两端都没接通（2026-08-26 新发现，同日修正严重度）

**第一次发现（第二批实施期间）**：`sql/migrations/002_thermostat_telemetry.sql` 里的四张表
（`thermostat_device`、`thermostat_device_state`、`thermostat_telemetry_history`、`thermostat_message_log`）
在现网 `ma` 库中一张都不存在（`information_schema` 命中数 0），
而 `MeterIngestionWorker.HandleThermostatTelemetryEnvelope` 已经在向它们写入。
当时判为"高"，理由是只要来一条温控器消息，入库就会抛
`relation "thermostat_message_log" does not exist`。

**进一步核查后修正为"中"**：全仓库检索 `PublishThermostatTelemetryAsync`，
**除定义处外零调用** —— 上位机的温控器模块（`ThermostatMonitorControl` +
`ThermostatWorkspaceService` + `ModbusThermostatDataService`）只做"串口读取 → 本地表格显示 →
配置持久化"，从来没有接进 MQTT 发布链路。

所以现状是：

| 环节 | 状态 |
|---|---|
| 上位机采集温控器（串口） | ✅ 已实现，界面可用 |
| 上位机发布到 MQTT | ❌ **方法写好了但没有任何调用点** |
| 入库服务订阅与处理 | ✅ 已实现（含 MessageId 幂等、主题身份校验、四张表写入） |
| 数据库四张表 | ❌ 迁移 002 未执行 |

链路在**两端**都断着，中间那段反而是完整的。因此风险是**潜伏的、不是活跃的**：
现在不会有消息到达，也就不会真的报错。这也解释了为什么此前一直没人发现 ——
改造前没有日志，而且根本没有消息触发过这条路径。

**要不要执行迁移 002，取决于温控器的规划：**

- **若温控器要进平台**（网页端看历史/状态）：执行 002，**并且还要补上发布端调用** ——
  只建表不接发布端，链路依然不通。缺的那一环是"在温控器刷新定时器里调用
  `PublishThermostatTelemetryAsync`"，与热泵的 `PublishHeatPumpTelemetryAsync` 对称。
- **若温控器暂时只在上位机本地看**：不执行也不会出问题。但建议把入库服务的温控器订阅
  做成可开关（`SubscribeThermostatTopic`，默认关），并在文档里写明这条链路尚未接通，
  避免下一个接手的人以为它是可用的。

**执行风险评估（若决定执行）**：极低。全文只有 `CREATE TABLE IF NOT EXISTS` 与
`CREATE INDEX IF NOT EXISTS`，没有 `ALTER`、没有 `DROP`、不触碰任何现有表与数据，可重复执行；
回滚就是 `DROP TABLE` 那四张。同类操作的迁移 001（热泵五张表）已在现网执行过。

> 本次未擅自执行：它超出第二/三批范围，且"要不要上线温控器"是业务决定而非技术决定。


---

### UI-1 "电表总览"页签从未显示过（已修）

**现象**：`MeterOverviewControl`（378 行）连同它的页签，运行至今一次都没在界面上出现过。

**根因**：`InitializeMeterOverviewTab()` 在**构造函数**里调 `tabControlMain.TabPages.Insert(...)`。
`TabControl` 句柄未创建时，`Insert` 会把页签**静默丢弃** —— 不抛异常、`TabPages.Count` 里也不含它。
同一批代码里线控器、历史分析、温控器三个页签用的是 `Add`，`Add` 不受这个限制，所以只有这一个页签消失。

**实测**：修前 `TabPages.Count = 11`，修后 `= 12`，且位置正确（"概览仪表盘"之后第 2 个）。

**顺带修**：页签能显示之后暴露出第二个问题 —— 空态提示"暂无电表设备"高度为 0，页面纯白。
`ResizeSections()` 对 `_sectionsPanel` 的**每个**子控件都套 `MinimumSize = MaximumSize = (宽, 0)`，
而这个提示是 `AutoSize=false` 的 `Label`，拿不到首选高度就被约束成 0。现在只对分区（`TableLayoutPanel`）
做宽度绑定，空态标签只同步宽度。实测 `1319×0` → `1319×90`。

**尚未决定**：这个页签和"概览仪表盘"的卡片区是两套并行实现，见 UI-6。本批只修"显示不出来"这个 bug，
没有合并，两个页签现在都能看到同一批卡片。

---

### UI-2 线控器汇总卡片两行字重叠（已修）

`LineControllerControl.Designer.cs` 的 `CreateSummaryValue` 把数值标签建成 `AutoSize=true` 且**不设 Dock**，
`AddSummaryCard` 里另一个标题标签是 `Dock=Top`。不设 Dock 的控件停在 padding 区左上角 (0,0)，
`Dock=Top` 的标题也从顶部开始 —— 两行字直接压在一起。

**实测**（4 张卡片全中）：

| 卡片 | 修前 | 修后 |
|---|---|---|
| 连接状态 | 数值 `(0,0,75,28)` vs 标题 `(15,17,78,23)` → 重叠 | 标题 `(15,17,298,34)` / 数值 `(15,51,298,69)` |
| 控制器数量 / 模块数量 / 最近刷新 | 同样重叠 | 同样分开 |

**修法**：数值改 `Dock=Fill` + `AutoSize=false` + 垂直居中；标题保持 `Dock=Top` 固定 24px。
同时删掉 `MainForm` 里那份写得正确但**从未被调用**的重复 `AddSummaryCard`（37 行死代码）。

---

### UI-3 卡片内 `Dock=Right` 把控件拉满高度（已修）

`CreateLineControllerCard` 里"选择模块"复选框和"配置"按钮都是 `Dock=Right`。
停靠到左右两侧的控件必然被拉满可用高度，于是在 340×208 的卡片里：

| 控件 | 修前 | 修后 |
|---|---|---|
| 选择模块（CheckBox） | 100 × **154** | 进标题行，单行高 |
| 配置（Button） | 75 × **154** | 进标题行，单行高 |
| 遥测正文（FlowLayoutPanel） | **139** × 130 | **314** × 125 |

遥测正文只剩 139px 宽，出水/回水温度这类稍长的行直接被挤出可视区。
**修法**：标题行改成 3 列表格（名称 100% / 复选框 AutoSize / 按钮 AutoSize），两个控件用 `Anchor=Right`。

分组标题栏（"配置控制器" / "编辑分组"）原来是同一个坑，只是被 `Panel { Height = 28 }` 兜着；
UI-5 把高度交给内容自适应后按钮立刻被拉成 90px 高（截图已复现），因此一并改成同样的 3 列表格。

---

### UI-4 固定行高把报错文字切一半（已修）

`HeatPumpMonitorControl` 的 `_analysisPanel` 里，摘要行 `Absolute 48F`、筛选行 `Absolute 86F`。
本机实测：数据库缺 `controller_code` 字段时摘要文字是两行
（`加载模块失败: 42703: 字段 "controller_code" 不存在` + `POSITION: 24`），48px 只够显示第一行加第二行的上半截。
筛选条是 `WrapContents=true`，窄窗口一换行同样被 86px 切掉。

**修法**：两行都改 `AutoSize`，`_summaryLabel` 补 `AutoSize=true` + `MinimumSize=(0,40)`（空文本时高度不跳）。
实测修后两行报错完整可见。同一个表格里还有 `Absolute 180F`（告警事件）和 `Absolute 228F`（对比列），
本批未动，归到 UI-13。

---

### UI-5 分组容器宽度写死 1120px（已修）

`AddLineControllerGroup` 建的分组是 `Panel { Width = 1120 }`。原因是外层 `_cardsPanel` 是
`FlowLayoutPanel(TopDown)`，而 FlowLayout **不认 `Dock`**，分组只能自己写死一个宽度。
后果：1400 宽窗口右侧留白、每行固定 3 张卡；窗口缩到 980（`MinimumSize`）时横向溢出。

**修法**：`_cardsPanel` 改成单列 `TableLayoutPanel`（列 100% + `GrowStyle=AddRows` + `AutoScroll`），
分组 `Dock=Fill` + `AutoSize`，宽度由列给。分组内部也改成 2 行表格（标题行 / 卡片流），
`Controls.Add(section)` 的调用方写法不变，顺序也不变（`AddRows` 按添加顺序落行）。

**实测**：分组容器宽度 `1120`（写死）→ `1299`（= 父容器可用宽度，随窗口变化）。

---

### UI-6 ~ UI-14 结构性界面问题（未改）

- **UI-6**：电表卡片有两套实现 —— `MainForm.CreateDashboardSection`（320×220，卡内可改电表名）和
  `MeterOverviewControl.BuildCard`（300×190，只读，带分区标题和空态）。UI-1 修完两个页签都能看到同一批
  电表，**留哪个是产品决定**：留"概览仪表盘"则删掉 `MeterOverviewControl`（378 行）和它的页签；
  留"电表总览"则要把改名入口挪过去，并把仪表盘页签收敛成纯串口/扫描工具条。
- **UI-7**：1400×900 下线控器数据页纵向分配是：顶部串口条 140 + 页签 26 + 端口工具条区 150
  （其中约 110px 是空白 —— `_connectionBarHost` 是 `Panel`，内部 `AutoSize` 工具条撑不满它）+
  控制条 85 + 汇总卡片 150 + 状态行 40，卡片区只剩约 110px，而卡片本身 208px 高，
  永远只能看到上半截。建议：工具条折叠进一行、汇总卡片降到 60px 或做成一行文字。
- **UI-8**：卡片区宽度同步靠两处手写钩子（`flpDashboard.Resize` 和 `_sectionsPanel.SizeChanged`）。
  UI-5 已证明单列 `TableLayoutPanel` 能让宽度自动跟随，这两处可以按同样方式去掉。
- **UI-9**：`MainForm.MinimumSize = (980, 680)`，但按 UI-7 的分配，线控器页要看到一整行卡片需要约
  700px 纵向可用高度，980×680 下这一页不可用。要么提高最小尺寸，要么按 UI-7 压缩固定区域。
- **UI-10**：`LineControllerControl` 用 `Segoe UI`，`MainForm` 卡片用 `微软雅黑`，其余控件用系统默认。
  中文在 `Segoe UI` 下靠字体回退渲染，同一页面字形不一致。
- **UI-11**：`MainForm.cs` 已 4071 行（P2-5 记录的 3402 行已过时），卡片构建、分组构建、汇总卡片
  刷新混在其中。UI-2/UI-3 这类 bug 的根源就是同类控件有多份手写实现。建议抽
  `MeterCardControl` / `LineControllerCardControl` 两个 `UserControl`，和 P2-5 一起做。
- **UI-12**：项目没有 `app.manifest`，配置里也没有 DPI 声明，.NET Framework 4.8 默认 DPI 不感知，
  125%/150% 缩放下整窗由系统位图拉伸（发虚）。现场用笔记本或 4K 屏时明显。
  修法：加 `app.manifest` 声明 PerMonitorV2，然后逐页核对 —— 因为代码里 `AutoScaleMode.Font` 与
  写死像素（卡片 340×208、列宽 228F）混用，开了 DPI 感知反而会暴露新的错位，需要成批验证。
- **UI-13**：`Absolute 180F`（告警事件表）、`Absolute 228F`（对比列）在系统字号放大到 125% 时会切内容。
  UI-4 已把会切文字的两行换成 `AutoSize`，剩下这两处属于"够用但不自适应"。
- **UI-14**：全项目只有 1 处 `AccessibleName`（正好是 UI-3 那个复选框）；卡片整块可点击但没有键盘等价
  操作，纯键盘无法进详情页。产线扫码枪/工控机常配简易键盘，这一项建议在抽 `UserControl` 时一起补。

---

### UI-15 ~ UI-20 界面规范缺失（2026-08-27 新增，已改）

这一组的共同点是：**没有一处定义"一个按钮该多大、一行该多高"**，于是每个 Designer 各写一套。
审计探针（`artifacts/UiAudit`，1400×900，读生产代码真实 `Bounds`）量到的修前状态：

| 指标 | 修前 |
|---|---|
| 按钮数量 / 尺寸种类 | 38 个 / **17 种** |
| 按钮高度种类 | **8 种**：29 / 33 / 38 / 41 / 42 / 45 / 50 / 52 |
| 字体 | 2 种（微软雅黑 9.75 + 宋体 9） |
| 工具条换行 | 4 处，其中 3 处把标签和输入框拆到两行 |
| 同行控件中线偏差 | 15 处，偏差 4 ~ 11.5px |
| 子控件超出父容器 | 5 处 |

- **UI-15 按钮尺寸各写一套**：现存的按钮尺寸写法有 `MinimumSize` 92×26 / 82×26 / 76×32 / 88×30 / 68×32 /
  58×28 / 72×28、`Width = 75`、Designer 里写死的 `Size` 78×45 / 112×45 / 160×50 / 133×38，
  再加一批"什么都不写只 `AutoSize`"。同一行里 2 字按钮和 4 字按钮因此差 30~40px。
- **UI-16 按钮被容器拉伸**：`btnReadInfo` 是 `Dock=Bottom`，被拉成 **1324×38** 的一整条；
  遥控页 5 个按钮四边锚定，被拉成 160×50；参数页两个 133×38。按钮宽度由容器决定，不由内容决定。
- **UI-17 假垂直居中**：`AddHeatPumpField`、温控器 `CreateField`（两份）、历史分析 `CreateFilterLabel`
  都用 `Margin = new Padding(0, 8, 2, 0)` 这类写死上边距来"凑"标签与输入框对齐，四处数字还不一样
  （8/9/2/3）。字体或 DPI 一变就歪，实测 15 行全部偏。
- **UI-18 换行拆散字段**：工具条是扁平的 `FlowLayoutPanel`，标签和输入框是两个独立子控件，
  换行点落在它们之间 —— "开始时间"后面跟着的日期框被甩到下一行（用户截图里的现象）。
- **UI-19 缩放基准不统一（本组的根因）**：`ContainerControl.AutoScaleMode = Font` 时，
  缩放系数 = 当前字体度量 / `AutoScaleDimensions`。5 个手写 `UserControl` 只写了前者、没声明后者，
  于是拿默认基准 6×13 去比环境字体（微软雅黑 9.75 实测 10×21），**所有像素常量被放大约 1.6 倍**；
  而 `MainForm` 自己声明了 `AutoScaleDimensions = 8×15`，系数正好 1。
  同一份 `MinimumSize(76,32)` 因此在温控器页画成 95×52、在 `MainForm` 里画成 76×32 ——
  这就是"同样的代码、不同页面尺寸不一样"的来源。
- **UI-20 对话框绝对坐标**：`HeatPumpDeviceEditorDialog` 的标签写死 `Location=(16,·)`、输入框写死 `(90,·)`，
  "控制器站号"这种 5 字标签一宽就压到文本框上（探针实测重叠 8px）；按钮又是第 5、第 6 种尺寸（75×23）。

---

### P4-1 串口锁内触发事件，订阅者同步等 MQTT（已修）

`ModbusService.Connect`/`Disconnect` 原来在 `lock (_lockObj)` **内部**调 `OnConnectionStateChanged`，
而订阅者 `MainForm.UpdateConnectionUI` 在"已连接"分支调 `ReplaceToolbarMeter` → 发布档案消息，
在"未连接"分支也发一次；这两处原本都用 `PublishBoxRegistryAsync(...).GetAwaiter().GetResult()`
**在 UI 线程同步等网络**。MQTTnet 4.3.7 的 `MqttClientOptions.Timeout` 默认 **100 秒**，而调用方没设超时。

后果（三个叠加）：① 点"连接/断开"时 Broker 不可达 → 界面冻结最长约 100 秒；
② 这段时间**串口锁一直在 UI 线程手里**，定时采集的 `Task.Run` 全部堵在 `ReadHoldingRegisters` 上，整段数据缺失；
③ 关窗时 `WaitForRefreshToSettle` 会因为 `_isRefreshing` 结束不了而空等。

**修法**：`Connect`/`Disconnect` 先在锁内完成开/关串口，**出锁后**才触发事件；
`MainForm` 两处改走已有的 `RunGuardedAsync`（后台发送 + 兜底记录），不再同步阻塞。
档案消息只是平台侧的设备清单，发布失败不影响本地采集。

---

### P4-2 写通讯参数丢弃用户所选停止位（已修）

`cmbParamParity` 的选项是 `8N2 / 8O1 / 8E1 / 8N1 / 8O2 / 8E2`（`MainForm.Designer.cs:846-852`），
**停止位是选项的一部分**；但 `BtnWriteParams_Click` 构造配置时 `StopBits = _meterCommunicationConfig.StopBits`
（取自 `App.config`），`ParseParityFromString` 也只解析校验位。
于是用户选 `8N2` 实际写入的是 `8N1` —— 与 P0-3 修掉的校验位问题是**同一个后果**：
电表按另一种帧格式通讯，从总线上消失，只能到现场逐台恢复。P0-3 当时只修了校验位那一半。

**修法**：新增 `ParseStopBitsFromString`（选项最后一位即停止位），写入前用它；
确认框补充显示下拉项原文（`即下拉项 8N2`），让操作者能核对实际要写的帧格式。

---

### P4-3 写响应校验缺从站地址、CRC 按实收长度算（已修）

`WriteMultipleRegisters` / `WriteSingleCoil` 原来只判 `Crc16Helper.Validate(response, response.Length)`
和功能码，对照读路径（P2-3 已改）缺少三项校验。两个方向都会出错：

| 方向 | 触发 | 后果 |
|---|---|---|
| 假成功 | 总线上别的从站回包（或错位帧）恰好功能码是 `0x10/0x05` | 分/合闸被判为成功，操作者以为断路器已动作 |
| 假失败 | 一次 Read 连带读到多帧（廉价 USB-RS485 的 TX 回显最常见），`response.Length > 8`，CRC 覆盖多余字节必然失败 | 明明写成功却报"设备未确认"，界面提示"命令可能已到达…请重试" → **诱导重复分/合闸** |

**修法**：新增 `ValidateWriteResponse`，在缓冲区里按 **8 字节定长**找一帧，
从站地址、功能码、地址与写入值回显、CRC 全部相符才算成功；为此给 `Crc16Helper`
补了带偏移的 `Calculate`/`Validate` 重载（原来只能从下标 0 算）。
失败按原因分别计入"身份/长度不符"或"CRC 错"，不再统一退化成 `false`。

---

### P4-4 报警去抖是"累计命中"而不是"连续命中"（已修）

`EvaluateRealtimeAlarms` 只对"本次命中"的项 `AlarmDebounce.AddOrUpdate(key, 1, (k,v) => v+1)`，
而全代码唯一的移除点在 `ResolveRecoveredAlarms` 里，且仅在"该告警已 active 且越过滞回带"时执行。
某次采样**未命中**且当前无 active 告警时，计数器既不归零也不递减。

于是计数器语义变成"任意时间跨度内命中的总次数"：PF 阈值附近若平均每 10 分钟蹭一次越限，
约 50 分钟后照样置位一条告警，而这 5 次从来不是连续的 —— **P1-6 想消除的误报只被削弱、没有消除**，
且去抖窗口随采样节奏漂移。

**修法**：新增 `ResetDebounceCountersOfUnhitItems`，每轮评估后把本表**本轮未命中**的计数器清零，
把"连续"真正实现为"未命中即清零"。这也是 P1-6 `require_energized`（未带电整项跳过）能如期生效的前提。

---

### P4-5 Worker 退出路径（已修）

两处独立缺陷，都在 `MeterIngestionWorker/Program.cs`：

1. **正常退出必抛未处理异常**：`await Task.WhenAll(workers + offlineMonitor + heartbeat)` 在任何 try 之外，
   而 `MonitorOfflineMetersAsync` 的 `catch (OperationCanceledException) { throw; }` 会把取消异常抛出来
   （对照 `ReportHeartbeatAsync` 是吞掉的，说明是遗漏）。结果：Ctrl+C 停止 → 异常冒到 Main 之外 →
   走 `AppDomain.UnhandledException` 记成"服务崩溃"、**退出码非 0**（作为服务托管会被判定为崩溃），
   并且下面的"累计处理 N 条"汇总与 `AppLogger.Shutdown()` 一并被跳过。
2. **异常路径从不取消令牌**：`catch (Exception)` 只置 `exitCode = 1`，不调 `cts.Cancel()`，
   而两个后台循环的退出条件都是 `!cancellationToken.IsCancellationRequested` ——
   进程会停在"既不订阅也不退出"的状态，只能手工杀掉，正是 P0-6 想消除的那种状态。

**修法**：`Task.WhenAll` 包一层 `catch (OperationCanceledException)`；异常分支补 `cts.Cancel()`。

---

### P4-6 `meter_status` / `meter.last_seen_time` 缺单调守卫（已修）

P1-3 给三张 `meter_*_latest` 补了 `WHERE 表.collect_time <= EXCLUDED.collect_time`，
但 `meter_status` 与 `meter` 被漏掉了：

- `UpsertMeterStatus` 用 `last_collect_time = COALESCE(EXCLUDED.last_collect_time, meter_status.last_collect_time)`
  —— `COALESCE` 只防 NULL，**不防"更旧"**；
- `UpdateMeterLastSeen` 是 `SET last_seen_time = @last_seen_time` 无条件覆盖。

离线判定用的是 `meter_status.last_collect_time < @deadline`，所以一条乱序消息就能把时间戳往回拨 →
15 秒后离线监控把通讯正常的表标成离线并写一条 critical 级 `alarm_event`，下一条消息又恢复，
产生成对的短命告警。当前 `ProcessWorkers=1` 时概率低，但配置项摆着就是陷阱（P3-4）。

**修法**：两处都改成"只接受不更旧的值"（`last_collect_time` / `last_publish_time` 用 CASE 保留较大者，
`last_seen_time` 加 `AND (last_seen_time IS NULL OR last_seen_time <= @last_seen_time)`）。

---

### P4-7 ~ P4-24 其余复查发现（未改）

- **P4-7 切型号后仍打原有电表寄存器**：`MainForm.cs:133/3132` 无条件
  `new MeterDataService(_modbusService)`，参数/设备信息/遥控/清零全部用它
  （`:3758/3834/3890/3908`），切到 AMC96L-E4 只改了按钮文字。读回的垃圾参数还会被当"当前参数"
  放进二次确认框。**需现场确认 AMC96L-E4 是否支持这些地址**，再决定按协议分派还是直接禁用。
- **P4-8 离线超时不在配置里**：`MainForm.cs:151` 读 `OfflineTimeoutSeconds`，`App.config` 里没有这个键，
  只能吃默认 15 秒。默认 1 秒采样、一轮最坏 `台数 × 3 × 500ms`，上十几台表必然超 15 秒 →
  排在后面的卡片周期性闪"设备掉线"，与 P1-1 的三端统一口径冲突。
- **P4-9 关窗不等热泵刷新**：`WaitForRefreshToSettle` 只等 `_isRefreshing`（电表），
  随后 `_heatPumpWorkspaceService.Dispose()` → `ModbusRtuClient.Dispose` 未取 `_gate` 就关闭并释放串口。
- **P4-10 扫描后错误启用"连接"按钮**：`finally` 无条件 `btnSecondConnect.Enabled = true`，
  而扫描期间该口是已连接状态；用户再点会 `Connect()` → `Close()`+`Open()`，打断在跑的采集。
- **P4-11 / P4-12 / P4-23（低）**：翻页按钮状态被 `ToggleQuickControls(true)` 覆盖；
  卡片改从站地址不回写档案也不落盘（平台按旧地址归属历史与告警，刷新后读数跳回）；
  跨线程读写 `DateTimeOffset?` 非原子、UI 清空仪表盘与后台轮询存在集合并发。
- **P4-13 空 catch 掩盖串口故障**：热泵与温控器链路 5 处空 catch，
  端口被占用/线缆掉了与"没扫到设备"显示成同一句话，现场排查方向被带偏。
- **P4-14 热泵故障码两端死链路**：发布端 `MainForm.cs:771` 恒写 `FaultCode = string.Empty`；
  入库端 `ParseHeatPumpCode` 对空串/非数字一律返回 0，于是 `faultCode > 0` 不成立 → 走"恢复 fault 告警"分支。
  现状只是功能失效，**将来发布端填入真实故障码（如 `"E05"`）会被解析为 0，反而把已激活的故障告警恢复掉**。
- **P4-15 无重试/死信回流**：`HandleTelemetryEnvelope` 失败即 `throw`，只计数不重投；
  `WithCleanSession()` 无会话补投，`mqtt_message_log` 里的 `failed` 行没有任何进程会消费。
  （注：P1-4 把幂等标记放在业务事务内，正好为将来重投留好了口子。）
- **P4-16 `sql/public.sql` 会破坏现网**：它是 2026-05-28 的全库 dump，会
  `DROP TABLE IF EXISTS alarm_event` 后重建，而重建语句**没有 `uq_alarm_event_active`**
  （该索引只存在于迁移 003）。Worker 依赖它做 `ON CONFLICT (meter_id, alarm_code) WHERE status='active'`，
  一旦有人用这份 dump 恢复库，每条带遥测的事务都会因 42P10 整体回滚 → 入库全面失败。
  **在任何恢复/比对之前先给它加警告，长期应废弃只保留迁移。**
- **P4-17 / P4-18 / P4-19 / P4-21 / P4-22（低）**：4 个报警阈值配置键已废弃仍被读取（改了不起作用）；
  `MarkLogFailed` 吞异常导致日志行永远停在 `pending`；`meter_realtime_history` 除 P3-8 那对外
  还有一对重复索引（`collect_time DESC`）；`sql/migrations` 里混入一份 MySQL 迁移，
  `apply.sh` 的 `[0-9][0-9][0-9]_*.sql` 永不匹配它，004 的"不在表里=没应用"对它不成立；
  阈值解析每条消息重建对象、`RecentMessages` 清理每条消息全表扫描。
- **P4-20 档案同步的两个陷阱**：`MarkMissingMetersOffline` 无条件执行，
  而 `registry.Meters` 允许为空列表（发布端存在"先 `ClearDashboardMeters()` 再发布"的路径），
  空列表会把整箱表标成离线；`ON CONFLICT (site_id, meter_code)` 也兜不住
  `uk_box_slave (box_id, slave_address)` 冲突 —— 表换箱且地址被占用时整批同步回滚。
- **P4-24（低）**：`ModbusService.IsConnected` 在锁外访问 `_serialPort`，与 `Dispose`/`Connect` 有竞态窗口
  （表现为偶发 `NullReferenceException`，被上层兜住，不致命）。

---

### W-1 / W-2 电能质量历史：后端 500 + 前端必崩（已修）

这两个缺陷叠在同一条链路上，用户点一次就能同时命中：

- **后端**：`selectQualityHistory` 的 SELECT 引用 `ms.last_collect_time / ms.last_publish_time /
  ms.status_code / ms.is_online`，但 FROM 只 join 了 `meter`、`distribution_box`、`site`，
  **没有 `meter_status ms`** → MySQL 报 `Unknown column 'ms.last_collect_time'` →
  经 `GlobalExceptionHandler` 把原始 SQL 错误**直接返回浏览器**。
  同项目另一份实现（`MeterTelemetryRepository.java:101`）反而是 join 对的，典型"改了 A 忘了 B"（见 W-18）。
- **前端**：`history/index.vue` 的 `categoryInfo` 只有 `realtime`/`energy` 两个 key，
  `trendTitle` 却按 `activeCategory` 取值 → 切到"电能质量"或从详情抽屉点"质量历史"（`?category=quality`）
  直接 `TypeError: Cannot read property 'title' of undefined`，整页渲染失败。
  也就是说《任务说明》第 4 条"补齐质量历史 Tab"当时交付的是一个**必崩**页面。

**修法**：SQL 补 `LEFT JOIN meter_status ms ON ms.meter_id = m.id`；
`categoryInfo` 补 `quality` 条目，getter 再包一层空值兜底（未知分类显示"趋势"而不是崩）。
顺带修掉同文件的两处小问题：`data()` 里重复的 `trendRequestId` 键；
以及**该页被 keep-alive 缓存、却只在 `created` 读一次 `$route.query`**，
导致"从别的页面带参数跳进来不刷新"——新增 `$route.query` watch（比较 meter/category/时间范围三项，
相同则直接返回，不会与 `syncQuery` 的 `$router.replace` 形成回环）。

---

### W-3 / W-4 网页端恒显示 `--` 的字段（已修）

两处"字段名对不上，页面永远显示 `--`"：

| 位置 | 读的字段 | 实际字段 | 修法 |
|---|---|---|---|
| `meter/alarm-record/index.vue:48-49` | `forwardActiveEnergyKwh` / `reverseActiveEnergyKwh` | `forwardActiveEnergy` / `reverseActiveEnergy` | 改前端字段名（`realtime-detail.vue` 用的是正确名字，只有这一页写错） |
| `realtime-detail.vue:18`、`alarm-record/index.vue:43` | `reactivePowerKvar` | 领域对象有这个字段，但**全代码没有任何一处给它赋值** | 在 `enrichCard` 里按有功功率同样的方式补上 `setReactivePowerKvar(toKilovars(...))`，一处改动同时修好两个页面 |

---

### W-5 状态判定复用 `FAULT` 且与阈值表不一致（已修）

`MeterCardServiceImpl.applyStatus` 有三个问题叠在一起，共同表现是**页面自相矛盾、并把运维带向错误方向**：

1. `isSevereAnomaly`（频率不在 40~70、或三相电压全低于 50V）也返回 `FAULT` ——
   而 `FAULT` 在前端映射成**"通信故障"**。于是一台电压严重异常的表显示成"通信故障"，
   修的人会去查通信，实际是配电问题。
2. 状态判定里的功率因数下限是 **0.5**、电压区间是 **180~260**，而同一张 `meter_threshold` 表
   （以及本页的风险列）用的是 **0.85 / 198~242**。一台 PF 0.6 的表会同时显示
   "运行正常"（状态列）和"功率因数偏低"（风险列）。
3. 这些数字全是硬编码，`meter_threshold` 的站点/配电箱/单表三级覆盖对状态列无效。

**修法**：`applyStatus` 改为接收该表合并后的阈值（`resolveThresholds(card)`），
PF 下限、电压区间、频率上下限全部走 `minOf/maxOf(...)`，缺行时退回与种子一致的默认值；
"严重数值异常"改用新的状态码 **`ABNORMAL` / "数值异常"**，把 `FAULT` 让回通信故障专用；
三相同时低于 50V 的语义单独抽成 `DEENERGIZED_VOLTAGE` 常量（表示未带电，而不是"电压越限"）。
前端 5 处状态元数据同步补 `ABNORMAL`：`meter-utils.js`、`meter-simulation.js`、
`views/index.vue`（图例 / severity 排序）、`exception-devices/index.vue`（severity 排序）。

> **行为变化，需现场确认**：原先显示"通信故障"的电压/频率异常设备，现在会显示"数值异常"。
> 统计口径不变（都属于异常设备），但按状态码筛选时结果会变。

---

### W-6 ~ W-19 网页端后端其余问题（未改）

- **W-6 权限与隔离（高）**：`MeterCardController` 里只有 `/topology` 的 GET/PUT 有 `@PreAuthorize`，
  `/cards`、`/realtime/page`、`/quality/page`、`/history/*`、`/realtime/detail/{meterId}` 等全部没有；
  框架兜底只有 `anyRequest().authenticated()`（`SecurityConfig.java:108`）。
  而 `sys_menu` 里也没有电表各页对应的权限位（前端菜单是硬编码的 `constantRoutes`），
  所以**补注解必须同时补菜单种子数据**，否则非管理员账号会整块看不到——这是产品决定，本轮未动。
- **W-7 网页端阈值很可能没生效（高）**：Java 侧 `@DataSource(SLAVE)` 指向 **MySQL** `ma` 库，
  而 `meter_threshold` 的建表与种子只存在于 **PostgreSQL** 迁移 003，全仓库没有 MySQL 版 DDL。
  读不到时 `catch` 里静默 `loaded = new LinkedHashMap<>()`，退回硬编码并缓存 5 分钟 ——
  P1-1 "三端统一"在网页端等于没做，而界面上完全看不出来。
  **现场一条 SQL 即可定论**：`SELECT * FROM ma.meter_threshold LIMIT 1`。
- **W-8 阈值表的语义字段没人执行（高）**：`is_alarm_enabled` 映射进了实体但全代码无读取处；
  `require_energized` / `debounce_count` / `recover_margin` 连 mapper 都没查。
  于是停电空载时上位机按 `require_energized` 跳过、网页端照样判"电压越限" ——
  **数字统一了，规则没统一**。
- **W-9 仪表盘能耗趋势（中）**：`selectEnergyReadings` 无 meter 过滤、无 limit，
  一次把 24h/7d/30d 全量读数拉进 JVM；`getBucketIndex` 对**每条读数**从桶 0 起循环
  （30 天 = 30 次/条），每桶还 `Calendar.clone()`。建议分桶下推到 SQL。
- **W-10 / W-11 / W-12 / W-13（中）**：`/quality/page` 手写分页 `(pageNum-1)*pageSize` 会整型溢出
  且 `pageSize` 无上限；异常处理器把 `e.getMessage()` 原文返回浏览器；
  拓扑保存"全删全插"+ 毫秒时间戳当主键（并发保存互相破坏）；
  嵌套 `@DataSource` 用的是普通 `ThreadLocal` 非栈，内层返回即清空上下文（目前靠调用顺序侥幸不出错）。
- **W-14 ~ W-19（低）**：历史行也被 `applyStatus` 判成"设备掉线"（历史查询不取 `meter_status` 字段，
  界面暂未渲染该列所以还看不见）；趋势分桶用 JVM 默认时区；热泵 `alarm=0` 被忽略、`/alarms` 无上限；
  拓扑 SQL 非幂等且带 `DROP TABLE`、`parent_id` 硬编码 2000，演示数据脚本是 MySQL 方言；
  `MeterTelemetryRepository` 是死代码且配置前缀与 `application-druid.yml` 不一致，
  但它的质量历史 SQL 反而是对的（准备启用 PG 数据源时会立刻踩坑）；
  `getRealtimeDetail` 查不到返回 200 + `data=null`，且不过滤 `is_enabled`。

---

### W-20 ~ W-30 网页前端其余问题（未改）

- **W-20 模拟数据模式无任何标识（高）**：`api/system/meter.js:18-25` 命中 `simulated()` 就返回伪造数据。
  **更正一处审查结论**：开关本身已限制为超管可见（`views/system/user/profile/index.vue:34` 的
  `v-if="isSuperAdmin"`），并不是"任一登录用户"都能打开；真正的风险是**打开之后界面上毫无痕迹** ——
  同一台浏览器上的任何人（包括非管理员）看到的都是演示数据，却与真实数据长得一模一样。
  已修：在布局层加常驻横幅（见十四），进入每个页面时重读开关状态。
  另外 localStorage 仍可被手工设置，本项定位是"让模拟态无法隐身"，不是"阻止开关被打开"。
- **W-21 定时器泄漏（中）**：`views/index.vue:185` 与 `meter/realtime/index.vue:79` 的
  `setInterval(…, 15000)` 只在 `beforeDestroy` 清理，而这两个页面都被 `cachedViews` 缓存
  （`AppMain.vue:24-26`）→ 离开页面后仍持续请求，多页签时成倍。
- **W-22 ~ W-25（中）**：拓扑页 `loadTopology` 无 catch，接口失败伪装成"尚未创建拓扑区域"空态；
  详情抽屉无请求竞态保护（快速切设备会显示另一台的读数）；
  质量页 `Promise.all` 耦合导致统计接口失败时列表也一起消失；
  能耗页切换设备重复发起同一次分析请求。
- **W-26 首页入口职责仍混乱（中）**：负荷排行图点击**正常设备**也会跳
  `MeterAlarmRecord`（`views/index.vue:266 → 293-294`），进去显示"该设备当前没有报警或故障" ——
  这正是《任务说明》第 2 条要修的错误层级，改名字（"设备告警状态"）没有解决入口问题。
- **W-27 ~ W-30（低）**：实时页自动刷新会用"未点搜索的输入值"过滤列表；
  前端 `meter-utils.js` / `meter-simulation.js` 仍各存一份硬编码阈值作为兜底；
  电表菜单硬编码进 `constantRoutes` 与 `sys_menu` 权限体系并存（现场可能重复/失配，需现场核对）；
  多处请求失败静默清空表格，用户无法区分"无数据"与"请求失败"。

---

### W3-31 ~ W3-46 网页端第三轮明细（2026-09-23）

**先说两条实测定论（不是推断，可以直接据此决策）**

1. **W-7 的现场问题有答案了**：`SELECT * FROM ma.meter_threshold` → `ERROR 1146: Table 'ma.meter_threshold' doesn't exist`。
   `ma` 库只有 13 张表（`alarm_event` / `distribution_box` / `meter` / `meter_comm_config` / `meter_energy_history` /
   `meter_energy_latest` / `meter_power_quality_history` / `meter_power_quality_latest` / `meter_realtime_history` /
   `meter_realtime_latest` / `meter_status` / `mqtt_message_log` / `site`），确实没有阈值表。
   所以 Java 侧每次读阈值都会走 WARN 兜底分支，**P1-1 的三端统一在网页端等于没做**。
2. **W-31 数据源可能已经脱节**：`ma` 库里四个时间列的最大值全是 `2026-07-30 00:08:14`
   （`meter.last_seen_time`、`meter_realtime_latest.collect_time`、`meter_energy_latest.collect_time`、
   `meter_realtime_history.collect_time`），而入库服务写的是 PostgreSQL。
   也就是说网页端现在展示的"实时运行/能耗/质量/历史"全是两个月前的一份快照，
   这与《两份库的归属待确认》是同一件事，但它比阈值问题更前置 —— **先定哪份库是网页端的权威数据源，再谈界面优化**。

**会咬人的（可复现，不是观感问题）**

- **W-32**：`history/index.vue:181` 的 `loadDetails` 没有任何请求归属校验，而同文件 `loadTrend`（:165-178）有
  `const requestId = ++this.trendRequestId` + `if (requestId !== this.trendRequestId) return`。
  快速切设备/时间段/翻页时，先发的旧请求后到就覆盖新结果，而表头与 `total` 已按新条件更新 —— 用户无从察觉。
- **W-33**：`quality/index.vue:6-8` 三个 `el-form-item` 没有 `prop`，而 `:78` 的
  `resetQuery() { this.resetForm('queryForm'); this.handleQuery() }` 依赖 `prop` 才能重置（Element 的 `resetFields` 只处理带 `prop` 的项）。
  现象：填了"站点=A"点重置，输入框原样保留，并且立刻按旧条件又查一遍。对比 `realtime/index.vue:12-21` 是带 `prop` 的，所以只有质量页坏。
- **W-34**：`exception-devices/index.vue:41` 定义 `FAULT: 0`，`:55` 排序却写 `(severity[code] || 99)`；
  `0 || 99` 求值为 `99`，于是**最该先看的通信故障被排到最后**，且与"未知状态码"同权。
- **W-35**：`energy/index.vue:65` 用 `Number(item.activePowerTotal) / 1000` 再 `.filter(Number.isFinite)`，
  `:67-68` 用 `Number(summary.startForwardActiveEnergy)`；`Number(null) === 0` 且 `isFinite(0)` 为真，
  所以缺失采样会以 0 参与均值/峰值、在趋势图上画成 0 kW 的点，KPI 显示 `0.00 kWh`。
  这与 `meter-utils.js:38-42` 特意修过的口径（"不能让 `Number(null) === 0` 把'没有数据'显示成 0.00"）自相矛盾 —— 同仓库两套口径。
- **W-36**：`heatpump/monitor/index.vue:84` 起 15 秒轮询 `refresh`，`:92-100` 与 `:114-117` 都是 `try/finally` **没有 `catch`**；
  `request.js` 失败时 `Promise.reject`，于是每次失败都产生一次未处理的 Promise 拒绝（控制台每 15 秒一条），
  而界面既不提示也不标记数据陈旧。该页也缺 `activated/deactivated`（W-21 当时只改了 `views/index.vue` 与 `meter/realtime`）。
- **W-38**：`quality/index.vue:79-83` 的 `showDetail` 既直接 `this.$refs.detail.open(...)`，又改 `$route.query.meterId`，
  而 `:50` 的 watcher 收到变化后再 `openRouteMeter` → 同一台设备固定发两次 `/realtime/detail/{id}`。
  这正是 W-25 在能耗页修掉的同类问题，质量页漏改。
- **W-39**：`energy/index.vue:97-102` 的 `applyPreset`（只该改时间范围）里调了 `loadMeters()`，
  而 `:112-118` 的 `loadMeters` 拉的是全量 `/system/meter/cards`，且失败分支会把 `meterId` 置空 —— 改时间范围反而丢掉已选设备。
- **W-40**：`heatpump/monitor/index.vue:93-96` 里 `listHeatPumpAlarms()` 没接 `params`（后端 `/alarms` 也不收参数），
  所以站点/状态筛选与"仅显示告警"对告警标签页无效，而界面上筛选器看着是生效的。

**状态与口径**

- **W-37**：`exception-devices/index.vue:6` 文案写"展示当前采集状态异常的全部设备"，`:54` 只取 `dashboard` 分组。
  后端 `MeterCardServiceImpl.java:72-92` 明确把卡片分成互斥两组（`isToolbar` → 外部串口 / 其余 → 仪表盘串口），
  所以外部串口的设备**永远不会**出现在这个页面，而它在能耗页的设备下拉里是可选的。
- **W-41**：`alarm-record/index.vue:99` 的本地 `formatNumber` 用 `maximumFractionDigits: 3`，其余页面用 `formatMeterNumber(value, digits = 2)` →
  同一个 `activePowerKw` 在两个页面显示不同位数；该页还把 `getMeterStatusLabel` 包了一层同名方法。
- **W-42**：位置文本至少有三种写法（`{{ siteName || '--' }} / {{ boxName || '--' }}`、`[a,b].filter(Boolean).join(' / ') || '--'`）；
  占位符 `--` 与 `----` 混用；状态文案前端是「通信故障/无数据/等待采集」，后端 `MeterCard.statusText` 写的是「通信异常/设备掉线/等待数据」；
  同一个 `/cards` 有三种消费方式（`energy` 合并两组不去重、`history` 用 Map 去重并排序、`exception-devices` 只取 dashboard）；
  `meterId` 在 `String` / `Number` / 原样 之间混用，一旦后端返回字符串型 ID，严格相等匹配会静默失败。
- **W-45**：`meter-utils.js:63-65` 的 `isBelow/isAbove/isOutside` 只挡 `null`/`undefined`，
  `Number('') === 0` 会让空串 PF 判成低功率因数；`meter-device.vue:112-135` 的 `hasValue` 同样放行空串，显示成 `0.0`。
  **待确认**：上游网关/序列化是否会把缺失读数输出为空串（后端 `Float` 缺失时是 `null`，所以这是防御性问题）。

**清理项**

- **W-43**：`api/system/meter.js:32-40` 的 `getEnergyTrend` 与 `getDashboardEnergyTrend` URL、参数完全一样，前者全仓无调用点；
  `meter-utils.js:44` 的 `formatMeterDateTime(value) { return value || '--' }` 不做任何格式化且无人调用 —— 名字会误导后来者。
- **W-44**：`heatpump/monitor/index.vue:108-110` 的 `row.controller_name || ('地址 ' + row.controller_address)`，
  两个字段都为空时列显示"地址 undefined"。

**交互缺口（与悬浮球同一类：看着有、用起来不是那么回事）**

- **W-46**：拓扑页列表视图只支持改**区域**，不支持改**配电箱**；而跨箱移动恰恰是唯一受 `uk_box_slave(box_id, slave_address)` 约束的操作，
  批量流程覆盖不到它。另外"未分配设备"是移出区域的唯一出口，2026-09-23 把它挪到页面最下方之后，
  从靠上的区域往外拖必须依赖边缘自动滚动/滚轮 —— 若现场觉得别扭，可给每个区域加"移出本区域"的投放条或行内按钮。

**本轮复核确认（旧编号，仍存在）**

- **W-6** 仍成立：`router/index.js:77-127` 的 `/meter` 及其 7 个子路由只有 `meta`，**没有 `roles`/`permissions`**；
  `MeterCardController` 里只有 `/topology` 的 GET/PUT 带 `@PreAuthorize`，`/cards`、`/realtime/page`、`/energy/page`、
  `/quality/page`、`/quality/stats`、`/history/*/trend`、`/energy/analysis`、`/realtime/detail/{meterId}` 全部没有。
  即：**任何能登录的账号，地址栏直接进 `/meter/realtime` 就能看到现场数据**。
  注意与 2026-09-23 的清库有关：`sys_role_menu` 已清空、`普通角色` 无任何菜单授权，将来新建的操作员账号会
  "看得到电表模块、看不到框架菜单"，W-6 的产品决定因此更紧迫。
- **W-24** 仍成立（`quality/index.vue:59-67` 的 `Promise.all` + `catch` 清空 `meterList`/`stats`，
  统计接口失败会让页面显示"当前没有识别到质量风险设备"+ 四个 KPI 全 0）。
- **W-28** 仍成立，但**要更正一处说法**：当时写的"与后端口径可能漂移"在当前环境下不成立 ——
  后端因为 W-7 读不到表，也在用同一批硬编码值兜底，所以两边数字**暂时一致**。
  真正的风险是"两份独立硬编码副本"，将来只在一边调整阈值就会静默不一致。
- **W-30** 仍成立，本轮把位置补齐了四处：`realtime/index.vue:118-121`（还会把 `total` 清 0 导致分页器 `v-show` 消失、15 秒后又自己回来）、
  `quality/index.vue:63-67`、`history/index.vue:181`（明细）、`realtime-detail.vue:74`（`detail = {}` → 抽屉变成"全是 `--` 的设备"）。
  另外 `realtime/index.vue:102` 的 15 秒轮询本身也没有请求序号守卫，与手动翻页/搜索并发时旧响应会覆盖新页。

**待确认（未臆测，需要信息才能定论）**

1. 后端日期字段（`MeterCard.lastCollectTime` / `dataCollectTime`，`java.util.Date`）的序列化格式：
   表格里直接输出原始值，而 `history/index.vue:187` 的 x 轴 formatter 又写 `value.replace('T', ' ')`。
   若 Jackson 未配 `date-format`，表格显示的是未本地化的 ISO 串；若已配则只是 formatter 多写了。
2. 热泵页是否被 `cachedViews` 缓存（组件名 `HeatPumpMonitor` 与后端菜单路由名是否匹配）—— 决定 W-36 的定时器泄漏是否已经发生。
3. 采集网关/接口层是否会把缺失读数序列化成空字符串（决定 W-45 是不是现实风险）。

---

不全是问题，有几处是明确的资产，重构时应主动守住：

1. **协议层与业务层的边界很清晰**（`ModbusService` 只管字节、`MeterDataService` 只管字段），
   这条边界是后续所有重构都应该保留的。
2. **热泵 / 温控器链路的工程质量明显高于电表链路**：`MessageId` 幂等、topic 与消息身份一致性校验、
   `latest` 表单调守卫、离线自动产生告警事件。**电表链路照着它改就行**，不需要重新设计。
3. `HistoryQueryService` 用白名单指标定义拼表名列名 + 参数化传值，避开了 SQL 注入；
   `GetBucketSeconds` 的分桶降采样（>1500 点自动聚合）设计合理。
4. 采样单份、多处界面共享同一份内存结果（`MeterManager` + `MeterDetailsForm`）的设计正确，
   避免了重复占用总线。

---

## 四、变更记录

| 日期 | 批次 | 内容 | 验证方式 |
|:---:|:---:|---|---|
| 2026-08-26 | —— | 建立本文档，完成上位机侧全量审查（28 项 + 4 项并行） | `dotnet build` 基线：0 error |
| 2026-08-26 | 第一批 | P0-1 ~ P0-8 全部落地，新增 `Common/AppLogger.cs` | 见下方"第一批实施记录" |
| 2026-08-26 | 并行项 | X-2 解决：两个仓库合并为单仓库，网页端从 gitlink 改为真实子目录 | 见"七、仓库结构调整" |
| 2026-08-26 | 第二批 | P1-1 ~ P1-8 全部落地，新增迁移 003 与 `Common/MeterThresholds.cs`；新发现 X-5 | 见"八、第二批实施记录" |
| 2026-08-26 | 第三批 | P2-1/P2-3/P2-4/P2-6/P2-7 落地，建起 `TPEM.Tests`（34 条）；**更正 P2-1 原始结论** | 见"九、第三批实施记录" |
| 2026-08-26 | UI 批 | 新增 UI-1 ~ UI-14；UI-1 ~ UI-5 落地（页签丢失、卡片重叠/被压、固定行高、写死宽度） | 见"十一、界面布局第一批实施记录" |
| 2026-08-27 | UI 批 | 新增 UI-15 ~ UI-20 并落地；新增 `UI/UiStyle.cs` 界面规范 + 10 条护栏测试；顺带收掉 UI-7 / UI-10 / UI-13；新发现 X-7 | 见"十二、界面布局第二批实施记录" |
| 2026-09-17 | 复查批 | 4 路并行复查；新增 P4-1 ~ P4-24、X-8 / X-9、UI-21；P4-1 ~ P4-6 已落地 | 见"十三、第二轮审查实施记录" |
| 2026-09-17 | 网页批 | 网页端首次审查；新增 W-1 ~ W-30；W-1 ~ W-5 已落地 | 见"十三、第二轮审查实施记录" |
| 2026-09-17 | 复查批二 | W-8 / W-10 / W-11 / W-20 ~ W-23 / W-25 / W-27 与 P4-8 / P4-10 ~ P4-12 / P4-16 / P4-18 落地；W-7 由静默改为可见 | 见"十四、第二批修复实施记录" |
| 2026-09-23 | 网页批三 | 两路并行审计 + 真库抽查；新增 W-31 ~ W-46；**W-7 定论**（`ma.meter_threshold` 不存在）；复核确认 W-6 / W-24 / W-28 ~ W-30 仍存在并更正 W-28 一处说法；新发现 W-31（`ma` 数据停在 2026-07-30） | 见"二、问题明细"W3 小节 |
| 2026-09-23 | 拓扑页 | ①"未分配设备"从页面最上方移到所有区域之后（列表视图本就是最后，两边一致）；②悬浮球"收起后再次点击无反应"修复：`onFabAction('collapse')` 由只置 `true` 改为取反，并给球加随状态切换的 `title` | 8779 预览页实测：DOM 纵坐标顺序 74 / 455 / 1172；组件状态 `barCollapsed` 收起后再点回到 `fabOpen: true` 且 `editing` 不变 |
| 2026-09-23 | 网页批三修复 | **W-24、W-28 ~ W-45 落地**：重置按钮空转、明细请求竞态、`null` 当 0、严重度排序（含合并两张矛盾表）、热泵轮询无 catch 与筛选器误导、精度与判空收敛到共享层、删两处死代码；W-30 把四处"失败伪装成无数据"分开 | 见"十五、网页端第三批实施记录"（构建三次通过 + 预览页逐项实测） |
| 2026-09-23 | 首页转圈 | **W-47 首页轮询不再盖整页遮罩**（真实但次要）；**W-48 修掉本轮自己引入的回归**——模板用了未挂 `methods` 的 `formatMeterLocation`，渲染抛错使组件冻在 loading 帧，这才是"首页一直在转圈"的根因。**同时更正 W-47 最初的判断**（把根因记成了轮询遮罩）。另新增模板方法暴露检查脚本 | 预览页读真实 DOM 前后对比：表格 0 行→4 行、遮罩可见→`display:none`、根类名复原；`check-template-methods.py` 全量扫描 0 处 |

---

## 五、第一批实施记录

### 新增文件

| 文件 | 说明 |
|---|---|
| `MeterAcquisition/Common/AppLogger.cs` | 零依赖滚动文件日志器。两个项目共用同一份源码（Worker 通过 `<Compile Link>` 引用，不复制文件） |

### 逐项改动

**P0-8 日志**（先做，后面几项都依赖它）
- 分级（Debug/Info/Warn/Error/Off）、按天 + 按 10MB 滚动、保留 30 天、后台线程异步落盘、队列满丢弃并计数。
- 日志目录优先程序目录 `logs/`，不可写时退回 `%LOCALAPPDATA%\TPEM_ManagementSystem\logs`。
- 另有 `AppLogger.Audit(...)`：控制类操作**同步**写独立 `audit/audit-yyyyMM.log`，保证遥控命令下发后即使进程立刻崩溃记录也不丢。
- 配置项：两个 `App.config` 均新增 `LogLevel`。
- 未引入 NLog/Serilog 的原因见 P0-8 明细（packages.config + 当前环境无法还原新包）。

**P0-1 异常兜底**
- `MeterAcquisition/Program.cs` 重写：装 `Application.ThreadException`、`AppDomain.UnhandledException`、
  `TaskScheduler.UnobservedTaskException` 三道兜底，UI 线程异常改为"记日志 + 提示 + 继续运行"。
- `MainForm` 新增 `HandleHandlerException` / `RunGuardedAsync`，已加固：
  `RefreshTimer_Tick`、`BtnAutoScan_Click`、`DeleteMeterCard_Click`、`MainForm_Shown`、
  `HeatPumpRefreshTimer_Tick`，以及热泵连接/断开/扫描三个 `async` lambda。
- 顺带修掉一个按钮状态泄漏：`BtnAutoScan_Click` 原来在正常路径上重新启用按钮，中途抛异常会让三个按钮永久禁用；现在放到 `finally`。
- Worker 侧：配置加载失败改为返回退出码 2 并打印原因，另装 `AppDomain` / `TaskScheduler` 两道兜底。
- 查询模块的 `Quick*` / `Compare*` 处理器本身已有完整 try/catch/finally，未重复包装。

**P0-2 解析越界**
- 三个守卫改为按"实际消费字节数"命名常量：116 / 24 / 116（原 112 / 16 / 40）。
- 新增 `IsResponseUsable(...)`：长度不足记 Warn 并说明"实收 N 字节、需要 M 字节"，无响应记 Debug。
- `MeterManager.PollAll` 两个空 catch 改为分类记录（串口不可用 → Debug，其他 → Error 带堆栈），
  并新增 `MeterInfo.ConsecutiveFailureCount`，恢复通讯时输出一条 Info。
- `ReadPowerQualityData` 中 `VoltageTHDC` 后缺少 `offset += 4` 的问题**未改动**，只加了 `TODO(P2-1)`
  —— 需先对照协议地址表核对，否则会静默改变已入库数据的语义。

**P0-3 写通讯参数**
- `GetParityIndex` 改为 `GetParityIndex(Parity, StopBits)` 的完整逆映射（寄存器同时编码校验位与停止位），
  无法映射返回 -1；`GetBaudRateIndex` 未命中同样返回 -1。
- `WriteCommunicationConfig(config, out error)`：任何一项无法映射就**拒绝下发**并给出原因，不再让 `-1` 变成 `0xFFFF` 写进设备。
- `BtnWriteParams_Click`：`TryParse` 校验 + "从 X 改为 Y"的二次确认 + 写后回读比对 + 审计。
  回读无响应时提示"设备可能已切到新参数，请按新参数重连确认"，不误报为失败。
- 新增 `ParseStopBits`，`ReadCommunicationConfig` 现在也会回填停止位。

**P0-4 遥控确认与审计**
- `ExecuteControlAction(actionName, riskNote, action)`：所有会改变现场状态的动作统一走二次确认 + 审计。
- 分/合闸的确认框显示目标从站与串口，并明确"可能切断下游设备供电"。
- `BtnClearEnergy_Click` 并入同一入口（原来只有它有确认框）。
- 设备未确认时的提示改为"命令可能已到达但应答丢失，请核对实际状态"，避免误判为未执行。

**P0-5 关闭流程**
- `OnFormClosing` 只停定时器并在 `e.Cancel` 时**恢复采集**（原来取消关闭会留下"定时器停了但程序还在"的半死状态）。
- 资源释放移到 `OnFormClosed`，先 `WaitForRefreshToSettle(3s)` 等在途采样落地再逐个 `SafeDispose`。
- `RefreshTimer_Tick` 新增 `_isShuttingDown` 短路，并单独捕获 `ObjectDisposedException`。
- `ModbusService.Dispose` 改为"先 Close 再 Dispose"并加锁（直接 Dispose 打开中的 SerialPort 在部分 USB 转串口驱动上会抛异常）；
  `UpdateConfig` 加锁并允许在已释放后安全调用。
- 权衡说明：`WaitForRefreshToSettle` 用 `Application.DoEvents()` 让采样的 UI 续体得以完成，
  这是在不重构定时器的前提下的最小修法；彻底的做法是把定时器改成 `CancellationToken` 驱动的异步循环，归入 P2-5。

**P0-6 Worker 重连**
- 删掉 `DisconnectedAsync` 里的"延时 5 秒重连一次"，改为 `MaintainConnectionAsync` 退避循环：
  5s → 10s → 20s → 40s → 上限 `MqttReconnectMaxDelaySeconds`（默认 60s），连上后重置。
- `DisconnectedAsync` 只在 `ClientWasConnected` 时记录 —— MQTTnet 在 `ConnectAsync` 失败时也会触发该回调，
  否则一次失败会输出两份完整堆栈。
- 同一轮故障只记一次完整堆栈，后续重试记一行摘要（含最内层根因），避免 Broker 长时间不可用时日志被刷爆。
- 新增 `ReportHeartbeatAsync`：默认每 300 秒输出队列深度与处理/失败/丢弃计数，让"进程活着但没干活"可被发现。

**P0-7 队列上限与单实例**
- `MessageQueue` 改为有界（`MaxQueueLength`，默认 50000），满时**丢弃最旧**再放入最新
  （实时遥测价值随时间衰减），首次及每累计 1000 条丢弃输出一次 Warn。
- 退出时汇总累计处理/失败/丢弃条数。
- 上位机加 `Global\TPEM_MeterAcquisition_SingleInstance` 互斥体，第二个实例直接拒绝启动并说明原因；
  互斥体不可用（受限账户）时只告警不阻断。
- `MqttPublisherService` 的 ClientId 追加机器名后缀（超长时退回稳定哈希），多机部署天然不冲突。

### 验证结果

| 项 | 方式 | 结果 |
|---|---|---|
| 编译 | `dotnet build -t:Rebuild` | 0 error；警告仍为改动前的 6 × CS8632 + MSB3277，**未新增** |
| 日志落盘与编码 | 运行 Worker，检查 `logs/ingestion-*.log` | 文件生成，UTF-8 中文正常，控制台同步镜像 |
| P0-6 退避重连 | 把 `MqttPort` 临时改成 18999（无监听）运行 45 秒 | 进程不退出；日志可见 5s→10s→20s→40s 退避；首次带堆栈、后续单行摘要 |
| P0-7 单实例 | 连续启动两个 `MeterAcquisition.exe` | 第 1 个记录"上位机启动"；第 2 个记录"已有一个上位机实例在运行，本次启动被拒绝" |
| 目录自建 | 检查 `bin/Debug` | `logs/`、`audit/` 自动创建 |

### 仍需真机验证（本环境无法覆盖）

1. 接真实电表后确认 `IsResponseUsable` 不会误拒正常响应（重点看是否出现"响应长度不足"Warn，
   若出现说明设备返回的寄存器数与协议表不符，需按 P2-1 修正映射而不是放宽守卫）。
2. 写通讯参数的完整流程：改波特率后按新参数重连并"读取参数"确认。
3. 分/合闸确认框与审计文件在现场账号下的可写性。
4. 关窗时机：连接状态下关闭窗口，确认无 `ObjectDisposedException` 且日志有完整的"开始释放资源 / 资源释放完成"。

<!-- 新的变更记录请追加到上面的表格末尾 -->
<!-- ANCHOR-CHANGELOG -->

---

## 六、网页部分（第二轮，2026-09-17 已完成）

> **本节已于 2026-09-17 执行完毕**：`RuoYi-Vue-master` 前后端已完成首轮系统审查，
> 结果单列为 **W 系列（W-1 ~ W-30）**，见"一、进度总表 → 第七批"与"十三、第二轮审查实施记录"。
> 下表是审查前记录的 7 条线索，现按代码证据逐条给出结论（不再照抄旧描述）：

| 原线索 | 结论 |
|---|---|
| 1 子页之间缺少带设备上下文的跳转 | **大部分已修**：详情抽屉已有 4 个带 `meterId` 的可点入口；列表页本身仍只有"详情" |
| 2 "异常设备"与"报警记录"职责混乱 | **仍存在**：改名为"设备告警状态"但内容仍是当前状态说明，`occurredAt` 直接用采集时间冒充报警发生时间；且正常设备也能进来（见 W-26） |
| 3 隐藏页 `activeMenu` / `$router.back()` | **已修**：改为 `/meter/realtime` 与具名路由返回 |
| 4 历史追溯缺"电能质量"Tab | **部分修 + 引入致命崩溃**，见 W-1 / W-2 |
| 5 电能质量页风险统计只基于当前分页 | **已修**：`/quality/stats` 服务端全量 + 服务端 `riskOnly` 过滤后再分页 |
| 6 能耗分析与实时监测无上下文联动 | **部分**：能耗页支持 `?meterId=`，实时页完全不读 `meterId` |
| 7 选择时间后图表不跟随 | **仍存在**，见下方根因 |

> **第 7 条的根因定位**（两处，哪条主导需现场确认）：
> **(a)** 后端趋势 SQL 对原始采样硬编码 `LIMIT 240` 且不做时间分桶
> （`MeterCardMapper.xml:206-223`，同类还有 237/286 行），前端直接把返回点当 x 轴 ——
> 采样密度足够时"近 24 小时"与"近 30 天"返回的是**同一批最近 240 个点**（5 分钟采样≈20 小时），
> 宽范围之间图表几乎逐点相同；
> **(b)** 前端 `handleRangeChange` 在 `isValidDateRange` 为假时直接 `return`
> （`history/index.vue:142-149`），而该校验含秒级 `end <= Date.now()`、`disabledDate` 却只到"天"粒度，
> 选到今天的未来时刻时图表静默不刷新。
>
> **现场确认方法**：选"近 24 小时"截图，再改"近 7 天"截图，比较 x 轴首尾时间是否变化；
> 同时核对 `meter_realtime_history` 近 30 天的行数与采集间隔。本轮未改（属另一条链路，见下）。

---

## 七、仓库结构调整（2026-08-26）

### 调整前的问题

项目被拆在两个 GitHub 仓库里，且**仓库名与实际内容错位**，是本次最容易误判的一处：

| 远端仓库 | 实际内容 | 对应本地路径 |
|---|---|---|
| `MeterAcquisitionPro.git` | 上位机侧（C# WinForms + 入库服务），682 个网页文件一个都没有 | 仓库根目录 |
| `TPEM_ManagementSystem.git` | 网页端（RuoYi-Vue，Java + Vue），682 个文件，**零个 C# 文件** | 根目录下的 `RuoYi-Vue-master/` |

仓库名 `TPEM_ManagementSystem` 与本地根目录同名，但它对应的其实是根目录下的**子目录**。
再加上父仓库把 `RuoYi-Vue-master` 记成一个没有 `.gitmodules` 配套的 gitlink，
就形成了"看起来 TPEM_ManagementSystem 是整个项目的仓库，实际只有一半"的错觉。

两个仓库**没有共同祖先**，是完全独立的两条历史。

### 调整后

合并为单仓库，`origin` = `TPEM_ManagementSystem.git`，目录结构保持上位机在根、网页端落子目录：

```text
TPEM_ManagementSystem/            ← origin: TPEM_ManagementSystem.git
├─ MeterAcquisitionPro.sln
├─ MeterAcquisition/              上位机 WinForms
├─ MeterIngestionWorker/          MQTT 入库服务
├─ RuoYi-Vue-master/              网页端（原 gitlink，现为真实目录，682 文件）
├─ sql/                           上位机侧库脚本
├─ 代码审查与改进清单.md
└─ 项目框架说明-上位机与入库服务.md
```

### 做法与校验

采用 Git 官方的 subtree 合并流程，**两边历史完整保留、无 rebase、无 force push、无提交丢失**：

```bash
git merge -s ours --no-commit --allow-unrelated-histories tpem/master
git rm --cached RuoYi-Vue-master                    # 移除 mode 160000 的 gitlink
git read-tree --prefix=RuoYi-Vue-master/ tpem/master
git commit
```

合并提交 `7d2b6cc` 有两个父提交：`70bc200`（上位机）与 `b07be3a`（网页端）。
推送到 `TPEM_ManagementSystem.git` 是**快进推送**，远端原有 12 个提交的历史未被改写。

| 校验项 | 结果 |
|---|---|
| 网页端内容完整性 | `RuoYi-Vue-master/` 子树哈希 = `92e0d690…` = `tpem/master` 根树哈希，**逐字节一致** |
| 是否有文件被 .gitignore 遮蔽 | 682 个文件 `git check-ignore` 命中数 **0** |
| 跟踪方式 | 682 个文件为普通 `100644`，不再是 gitlink |
| 解决方案可构建 | `dotnet build -t:Rebuild` 通过，0 error，警告数与合并前一致 |
| 合并后规模 | 23 个提交，1965 个跟踪文件 |

### 收益

第二批的 P1-1（判定口径三端统一）与 P1-2（电表离线写 `alarm_event`）都需要
上位机、入库服务、网页端**同时**改动。合并后可在同一个提交里完成，
不会再出现前后端版本对不上的中间状态，也不需要手工同步 gitlink 指针。

### 遗留事项

- `MeterAcquisitionPro.git` 已转为归档（本地远端名改为 `archive`），停留在 `70bc200`，不再接收新提交。
  建议在 GitHub 仓库设置里正式 Archive，并在其 README 注明已迁移到 `TPEM_ManagementSystem`。
- 合并期间为规避目录占用，曾把 `RuoYi-Vue-master/.git` 临时改名后删除
  （删除前已确认：仅有 `master` 与 `origin/master` 两个引用、均指向 `b07be3a`、无 stash、
  且全部提交已进入父仓库对象库）。网页端已不再是独立仓库，后续直接在本仓库提交即可。










---

## 八、第二批实施记录（2026-08-26）

### 新增文件

| 文件 | 说明 |
|---|---|
| `sql/migrations/003_threshold_idempotency_alarm.sql` | 判定阈值表 + 种子数据、电表消息幂等表、告警部分唯一索引。幂等，可重复执行，全部为新增对象 |
| `MeterAcquisition/Common/MeterThresholds.cs` | 阈值模型 + 提供者 + **共用判定引擎**。上位机与入库服务引用同一份源码（Worker 走 `<Compile Link>`） |
| `RuoYi-Vue-master/.../domain/MeterThreshold.java` | Java 侧阈值实体 |

### P1-1 判定口径统一

审查时以为是三套，合并仓库后核对发现是**五套**：上位机、入库服务、Java 服务端、
前端 `meter-utils.js`、前端 `meter-simulation.js`。做法：

1. 建 `meter_threshold` 表，作用域 `meter > box > site > global` 四级覆盖，
   种子值取自 `RuoYi-Vue-master/docs/仪表监测判定标准.md`（相电压 198~242V、PF 0.85、
   THD ≤5%/≤8%、不平衡 ≤2%/≤3%）—— 这是项目里唯一有工程依据的一份标准。
2. 抽出 `MeterQualityEvaluator` 作为唯一判定实现：严格比较（等于阈值算合格）、
   null 不参与数值判定、三相取最不利相。上位机与入库服务各自把 DTO 映射成同一个
   `MeterSampleView` 再喂给它，**比较逻辑也只有一份**，不只是阈值统一。
3. 上位机 `DetermineMeterStatus` 删掉硬编码 180~260V / PF 0.5 / 40~70Hz，改走引擎。
4. 入库服务 `EvaluateRealtimeAlarms` 同样改走引擎，并新增 THD、不平衡度四项判定
   （此前入库服务完全没有评估过这四项）。
5. Java 侧 `MeterCardServiceImpl.resolveQualityRiskCodes` 改为查 `meter_threshold`（带 5 分钟缓存），
   读不到时退回与种子一致的内置默认值。
6. 两端启动时都把生效阈值与来源打进日志，排查"为什么这台表被判异常"第一步就能看到。

> **行为变化，需现场确认**：相电压合格区间从 180~260V 收窄到 198~242V、功率因数下限从 0.5 提到 0.85，
> 并新增了 THD 与不平衡度告警。这会让"异常"设备数量上升 —— 这是向权威标准对齐的预期结果，
> 但请确认现场配电实际电压水平能满足 198~242V，否则应在 `meter_threshold` 里按站点/单表覆盖放宽。

### P1-6 报警去抖与滞回

改造前从现网数据拿到的实证：`alarm_event` 共 263 条，其中 259 条是 `power_factor_low`，
**全部来自同一台表**，报警值密集落在 0.499x（阈值 0.5 附近），
**238 条持续时间不足 5 秒** —— 一个在阈值附近抖动的量产生了 259 条独立事件。

现在：
- 连续命中 `debounce_count` 次才置位（PF 配 5 次，其余 3 次）；
- 恢复要越过滞回带（低限需回升到 `min + recover_margin`，高限需回落到 `max - recover_margin`）；
- 取不到值时**不宣布恢复**，避免"数据断了"被当成"故障消失了"；
- `require_energized` 为真的项在未带电时整项跳过，停电不再被误报成"电压过低 + 功率因数过低"。

### P1-2 电表离线产生告警事件

`MonitorOfflineMetersAsync` 改成 CTE：`UPDATE meter_status ... RETURNING` 出新离线的表，
直接 `INSERT INTO alarm_event`（`alarm_code='offline'`，级别 critical，`start_time` 取最后采集时间）。
收到遥测时在同一事务内 `ClearAlarm(..., "offline", ...)` 成对恢复，`duration_seconds` 自动算出。

这是网页端"报警记录其实不是历史事件、只是当前状态说明"的根因修复：**后端此前从未为电表离线写过事件**。

### P1-3 / P1-4 / P1-5 / P1-7 / P1-8

- **P1-3**：三张 `meter_*_latest` 的 upsert 补 `WHERE 表名.collect_time <= EXCLUDED.collect_time`，与热泵/温控器链路对齐。
- **P1-4**：`MeterTelemetryMessage` 补 `MessageType = "meter.telemetry.v1"` + `MessageId`；
  新增 `meter_message_log` 做库级去重，**插入放在业务事务内** —— 若入库失败回滚，去重标记一并回滚，
  消息才有机会重投；放事务外会让一条失败消息被永久标记为已处理。
  兼容老上位机（`MessageId` 为空时退回内存指纹去重）。心跳任务按 `MessageLogRetentionHours`（默认 48h）清理。
- **P1-5**：三个 DTO 共 26 个字段 × 两个项目改为 `float?`；界面缺数据显示 `--` 而不是 0；
  入库写 `DBNull`；`ReadFloat` 不再把 NULL 读成 0f；变化检测把"有值↔无值"的切换本身当作显著变化。
- **P1-7**：采样到期改为按 UTC 时钟判断（`_lastRealtimeSampleUtc` 等三个字段），
  不再用 `_refreshTickCount % interval` —— 后者会因 `_isRefreshing` 跳过的 tick 不计数而整体漂移。
- **P1-8**：`MeterRegistryMessage.ScanTime` 与 `MeterInfo.LastSuccessfulReadTime` 改为 `DateTimeOffset`，
  掉线判定改用 `DateTimeOffset.UtcNow`。显示与日志时间戳保持本地时间（那是正确的）。
  热泵模块的 `Telemetry.LastUpdatedAt` 仍是 `DateTime.Now`，不在电表链路上，归入后续。

### 验证结果

| 项 | 方式 | 结果 |
|---|---|---|
| C# 编译 | `dotnet build -t:Rebuild` | 0 error；警告仍为既有 6 × CS8632 + MSB3277，**未新增** |
| Java 编译 | `mvn -o -pl ruoyi-system -am compile` | BUILD SUCCESS；`MeterThreshold.class` 与 mapper XML 均已产出 |
| 迁移 003 | 对现网库执行两次 | 第一次建表 + 9 条种子；第二次全部跳过，**幂等成立** |
| P1-3 乱序保护 | 现网库事务内实测后 ROLLBACK | 先写 10:00/220V，再投 09:00/111V → `INSERT 0 0`，值仍为 220，**迟到消息被拦住** |
| P1-4 幂等 | 同上 | 同一 `message_id` 第一次 `INSERT 0 1`、第二次 `INSERT 0 0`，**判定为重复** |
| 告警唯一约束 | 同上 | 第一次 `inserted=t`、第二次 `inserted=f`，active 条数恒为 1，**并发下不会插重** |
| P1-2 离线事件 | 同上 | CTE 产生 `offline` active 告警；恢复后 `duration_seconds=3600` 计算正确 |
| 阈值读取（Worker） | 启动实跑 | 日志：`已从 meter_threshold 载入判定阈值，global 9 条` + `相电压 198~242 V，总功率因数下限 0.85` |
| 阈值读取（上位机） | 启动实跑 | 同上，两端读到的是同一份 |
| 断库兜底 | 把连接串端口改成 11999 实跑 | 日志：`读取 meter_threshold 失败，暂时使用内置兜底阈值继续判定`，且兜底值与库内种子**完全一致**，进程不退出 |
| 现网数据 | 全程只读 + 事务 ROLLBACK | `alarm_event` 仍 263 条、`meter_message_log` 0 条，**未改动任何业务数据** |

### 遗留事项

1. **前端兜底阈值未收口**：`ruoyi-ui/src/views/meter/components/meter-utils.js` 与
   `ruoyi-ui/src/utils/meter-simulation.js` 仍各有一份硬编码阈值。
   好在 `getMeterQualityRiskCodes` 会优先使用服务端返回的 `qualityRiskCodes`，兜底路径平时不生效。
   彻底收口需要：新增一个阈值查询接口 → 前端启动时拉取 → 删掉硬编码 → 重新 `npm run build`。
   建议与第三批一起做，避免为此单独发一次前端。
2. **`meter_threshold` 目前只有 global 一层**，站点/配电箱/单表级覆盖能力已实现但没有数据。
   现场若有不同电压等级的回路，应在这里加覆盖行而不是改代码。
3. **X-5**：迁移 002 未执行，温控器入库链路不通（详见 X-5 明细）。
4. 阈值变更后最长 5 分钟生效（三端都是 `ThresholdRefreshSeconds` / `THRESHOLD_CACHE_TTL_MILLIS`），
   现场调阈值时需要知道这个延迟。

---

## 九、第三批实施记录（2026-08-26，进行中）

### 新增文件

| 文件 | 说明 |
|---|---|
| `TPEM.Tests/TPEM.Tests.csproj` | 首个测试工程（xunit，net48，SDK 式）。按源码链接被测文件，不引 WinForms 工程 |
| `TPEM.Tests/ModbusCodecEquivalenceTests.cs` | 字节序等价性回归：逐字复刻改造前两套解码实现，断言新旧结果一致 |
| `TPEM.Tests/RegisterMapTests.cs` | 映射表回归：偏移、缩放、单位、长度守卫、跨协议量纲一致性 |
| `MeterAcquisition/Common/ModbusCodec.cs` | 唯一一份 32/16 位解码实现，支持 4 种字节序，越界与 NaN/无穷返回 null |
| `MeterAcquisition/Common/MeterRegisterMap.cs` | `RegisterField<T>` 描述 + `RegisterMapReader`，长度守卫由映射表推导 |
| `MeterAcquisition/Forms/MeterCardTags.cs` | 卡片控件引用的强类型载体，取代匿名类型 + `dynamic` |

### 先建测试工程，再动协议层

第三批第一件事不是改代码，而是**先让改动可证明**。`TPEM.Tests` 里的
`ModbusCodecEquivalenceTests` 逐字复刻了改造前两个读取器的私有解码逻辑，
再断言 `ModbusCodec` 与它们产出完全相同的值 —— 这样"抽出公共实现"是可证明的等价替换，
而不是凭感觉重写电力数据的解析。

**这套测试立刻纠正了审查初稿的一个结论**：两种协议的字节序与量纲其实是一致的，
只是写法重复、缺测试、缺边界检查。详见 P2-1 明细里的更正说明。

工程配置上有两点值得留意：
- 测试工程是 SDK 式（`PackageReference`），另两个是 packages.config 老式工程。
  两者共存没问题，但**不能**从 SDK 式工程 `ProjectReference` 到 packages.config 工程
  （传递依赖不会被正确还原），所以这里按源码 `<Compile Link>` 引入被测文件。
- 断言的是生产代码里真正在用的那两张映射表（通过 `internal` 暴露），不是测试里另抄一份。

### P2-1 表驱动寄存器映射

`RegisterField<T>` 把"偏移 + 原始类型 + 字节序 + 缩放系数 + 单位 + 赋值目标"变成数据。
两个读取器的三/四张映射表现在是可读、可断言的表格，取代了原来一长串
`offset += 4` / `// 跳过平均相电压` / 行尾 `* 100`。

顺带解决的：
- **长度守卫不再靠人算**。`RegisterMapReader.RequiredLength(map)` 直接给出映射表消费到的最大字节数，
  这正是 P0-2 那三处守卫写小了的根因（当时是人工推导 112/16/40）。测试断言它等于"请求寄存器数 × 2"。
- **单字段越界不再拖垮整份数据**。越界字段留 null，其余字段照常可用（P1-5 语义）。
- **AMC96L 的谐波与不平衡度是两次独立读取**，改造前任一块失败就整体返回 null；
  现在只让失败那块的字段留 null。
- `VoltageUnbalance` / `CurrentUnbalance` 偏移 108/112 的可疑点（改造前 `VoltageTHDC` 后缺一次
  `offset += 4`）从"藏在算术里"变成"写在表上"，TODO(P2-1) 仍挂着等协议表核对 —— 未核对前不改动。

### P2-3 收帧校验

改造前只校验 CRC。RS485 是共享总线，收到**别的从站**的回包是常态，
而那种回包 CRC 是合法的，于是会被当成本次请求的数据采信 ——
现场表现是某台表偶尔出现另一台表的读数，且完全无从察觉。

现在按序校验：长度 → 从站地址 → 异常码 → 功能码 → 声明字节数 → CRC，
每种失败都记具体原因，并计入六类通信质量统计（`GetStatisticsSummary()`）。
CRC 只对"这一帧应有的长度"计算，上次超时残留的尾字节不再把本次好帧判成坏帧。

探测失败的弹窗现在会直接带上这份统计，"接线不对"和"型号/波特率不对"从此可以区分。

### P2-4 / P2-6 / P2-7

- **P2-4**：探测地址搬到后台线程，带进度（`正在探测... n/247`）与取消；
  正在采集时拒绝执行（原来点一下就把采集打断）；
  只关闭"为探测而临时打开"的串口，不再无条件 Disconnect 并把状态写成"未连接"。
- **P2-6**：`Control.Tag` 里的匿名类型 + `dynamic` 换成 `MeterCardTags`。
  原写法没有编译期检查、每次访问走 DLR，而这段代码每秒对每张卡片都执行一次。
- **P2-7**：切换电表型号后同步 `SetupDefaultBoxes` + 重建面板 + 刷新总览 + 关闭详情窗口，
  并在有已扫描设备时二次确认。原来只换 `MeterManager` 不重建界面，
  留下的卡片指向已不存在的 `MeterInfo`，`Find` 返回 null 后静默 continue ——
  卡片停在旧读数上，既不刷新也不报错，看起来像"采集卡住了"。

### 验证结果

| 项 | 方式 | 结果 |
|---|---|---|
| 单元测试 | `dotnet test` | **34 条全通过**（等价性 23 + 映射表 11），耗时 ~100ms |
| 字节序等价性 | 500 组随机字节 + 13 个典型值 | 新实现与改造前两套实现结果完全一致 |
| 跨协议量纲 | 同一物理量按各自原始编码造帧 | 两种协议解出的业务值相同（3.2% THD、45500W） |
| 长度守卫 | 由映射表推导后断言 | 三张表分别 116 / 24 / 116 字节，等于请求寄存器数 × 2 |
| 报文偏短 | 只给 64 字节的实时帧 | 前段字段正常，偏移 108/112 的两个字段为 null，不抛异常 |
| C# 编译 | `dotnet build -t:Rebuild` | 0 error；警告仍为既有 6 × CS8632 + MSB3277，**未新增** |
| 上位机启动 | 实跑 12 秒 | 正常启动，日志无 ERROR / WARN，阈值仍正确读自 meter_threshold |

### 本批剩余

| 编号 | 为什么留到后面 |
|---|---|
| P2-2 串口仲裁器 | 改动面最大（要把 `ModbusService` 的一把锁换成每口一个请求队列 + 优先级 + 帧间隔 + 熔断），且需要先确认"热泵与电表是否共用同一条 RS485"—— 若共用，本条应提升为 P0 |
| P2-5 `MainForm` 拆分 | 收益是长期可维护性，风险却最高；建议在 P2-2 落地、串口访问已收口到仲裁器之后再拆，那时切分边界会自然清楚 |
| P2-8 契约共享库 | 需要新建 `Tpem.Contracts` 工程并把 DTO 移出两个命名空间，会牵动较多引用；单独一批做更安全 |
| P3-7 CI | 测试工程这一半已完成；GitHub Actions 工作流未加 |

### 过程中的一个教训（已记录以免再犯）

用 PowerShell `Get-Content -Raw` + `Set-Content` 改 `MeterAcquisition.csproj` 时，
PowerShell 5.1 对无 BOM 的 UTF-8 文件按 ANSI 代码页读取，导致文件里的中文变成乱码、
`</ErrorText>` 结束标签被破坏，工程无法加载。已用 `git checkout` 恢复并改用编辑器工具完成。
**结论：不要用 PowerShell 5.1 做 UTF-8 文本文件的读写往返。**

---

## 十、迁移记录表与温控器开关（2026-08-26）

### X-6 数据库迁移应用记录（新增）

**问题**：现网出现了「003 已应用、002 没应用」的状态，而没有任何地方记录过哪个执行过 —— 全靠人记。
第二批实施时之所以能发现，是靠查 `information_schema` 里有没有对应的表反推出来的；
这种反推只在「迁移恰好会建表」时才成立，一旦出现纯 `ALTER` 或纯 `INSERT` 的迁移就失效。

**新增文件**：

| 文件 | 说明 |
|---|---|
| `sql/migrations/004_schema_migration.sql` | 迁移记录表 + 按标志性对象是否存在回填历史状态 |
| `sql/migrations/apply.sh` | 查看/应用/校验脚本，**默认只看不改库** |

**设计要点**：

- 不变量是「**不在 `schema_migration` 表里 = 没应用**」。回填时不凭空写「已应用」，
  而是按各迁移的标志性对象判断（001→`heat_pump_device`、002→`thermostat_device`、003→`meter_threshold`），
  判定为未应用的**不写入**，以保持这条不变量成立。
- 记 `checksum`（sha256）：迁移文件应视为不可变，应用后被改动可通过 `--verify` 发现。
  回填的三条没有 checksum（无从追溯当时的文件内容），`--verify` 会明确标为「回填记录，跳过」而不是误报。
- `apply.sh` **默认列出状态、不改库**；要应用必须显式 `--apply <版本号>`。
  这是刻意的：现在 002 就是「存在但不该被应用」的迁移，任何"自动应用全部待办迁移"的脚本都会把它带上线。

**用法**：

```bash
cd sql/migrations
export PGPASSWORD=...        # 或用 ~/.pgpass
./apply.sh                   # 列出全部迁移及应用状态
./apply.sh --verify          # 校验已应用迁移的文件是否被改动过
./apply.sh --apply 002       # 显式应用某一个
```

**当前状态（实跑结果）**：

```text
版本  状态      文件
----  --------  --------------------------------------------
001   已应用    001_heat_pump_telemetry.sql
002   未应用    002_thermostat_telemetry.sql
003   已应用    003_threshold_idempotency_alarm.sql
004   已应用    004_schema_migration.sql
```

> **踩到的坑（已在脚本里处理）**：Windows 上 psql 在输出被重定向时会退回 ANSI 代码页（中文环境 GBK），
> 读 UTF-8 迁移文件里的中文注释会报「字符 0x.. 在编码 UTF8 没有相应值」。脚本已显式钉住
> `PGCLIENTENCODING=UTF8`。另外 psql 输出是 CRLF 行尾，直接读会让字段末尾多一个回车符 ——
> 曾导致空 checksum 被读成非空、于是回填记录被误报成「文件已被改动」，脚本里统一用 `tr -d` 剥掉。
> 手工用 psql 跑迁移时也要记得设 `PGCLIENTENCODING=UTF8`（pgAdmin 不受影响）。

### X-5 温控器按「暂不上线」处理

业务决定温控器暂不上线，因此**不执行迁移 002**，改为：

- 入库服务新增 `SubscribeThermostatTopic` 配置，**默认 `false`**：不订阅温控器主题。
  订阅了也收不到消息（发布端没有调用点），一旦收到反而会因四张表不存在而每条都失败。
- `HandleEnvelope` 里加防御分支：开关关闭时即便收到温控器消息也直接忽略并记 Debug，
  不去写不存在的表。
- 启动日志明确输出链路状态，避免下一个接手的人看到 Worker 里那一大段完整的温控器入库代码
  就以为它可用：

  ```text
  [INFO] [Mqtt] 已订阅主题: meter/+/+/+ , meter/registry/+/+ , tpem/+/heatpump/+/telemetry
  [INFO] [Mqtt] 温控器主题未订阅（SubscribeThermostatTopic=false）。该链路尚未接通：
                上位机未接入发布、迁移 002 未执行。要启用需三件事同时做到：
                执行迁移 002、补上位机发布端调用、把本配置改为 true。
  ```

**将来要上线温控器，三件事必须同时做到**（缺一件链路都不通）：

1. `./apply.sh --apply 002` 建四张表；
2. 在上位机温控器刷新定时器里调用 `PublishThermostatTelemetryAsync`
   （与热泵的 `PublishHeatPumpTelemetryAsync` 对称，那条链路可直接参照）；
3. 把 `SubscribeThermostatTopic` 改为 `true`。

### 验证结果

| 项 | 方式 | 结果 |
|---|---|---|
| 迁移记录表 | 实跑 `apply.sh` 两次 | 第一次自动建表并回填 001/003/004；第二次输出一致，**幂等** |
| 未应用识别 | `apply.sh` 状态列表 | 002 正确显示「未应用」，其余「已应用」 |
| checksum 校验 | `apply.sh --verify` | 三条回填记录正确标为「无 checksum，跳过」，不误报 |
| 温控器开关 | 入库服务实跑 12 秒 | 订阅列表只有 3 个主题（无温控器），并输出链路未接通说明 |
| 编译 | `dotnet build` | 0 error |

---

## 十一、界面布局第一批实施记录（2026-08-26）

### 审查与验证手段

本批全程有实测，不靠读代码推断：

| 手段 | 说明 |
|---|---|
| 逐页签截图 | 启动上位机 → 置 1400×900 → 用 `TCM_SETCURFOCUS` 逐个切页签 → `PrintWindow` 截图，共 12 张 |
| 布局探针 | `artifacts/LayoutProbe`（SDK 式工程，引用已构建的 `MeterAcquisition.exe`）反射拿到 `_heatPumpWorkspaceService`，塞进 1 台控制器 + 5 个模块，调真正的 `RefreshLineControllerPage()`，再读控件 `Bounds` |
| 数据来源 | 探针注入的是 `HeatPumpDashboardManager.ReplaceControllers`（公开方法），**没有为了测试改动生产代码** |

> 为什么需要探针：线控器卡片只有扫到设备才渲染，本机没有热泵硬件；
> 而 `heatpump-workspace.json` 里的持久化配置只"装饰"已扫到的控制器，不能凭空造出卡片
> （`HeatPumpWorkspaceService.ApplyConfiguration` 只对 `Dashboard.Controllers` 里已有的项生效）。

### 逐项改动

| 编号 | 文件 | 改动 |
|---|---|---|
| UI-1 | `Forms/MainForm.cs` | `InitializeMeterOverviewTab` 只建页签；新增 `InsertMeterOverviewTab()`，在 `MainForm_Load` 里插入 |
| UI-1 | `Forms/MeterOverviewControl.cs` | `ResizeSections` 只对 `TableLayoutPanel` 分区套 `MinimumSize/MaximumSize`，空态标签只同步宽度 |
| UI-2 | `Forms/LineControllerControl.Designer.cs` | `CreateSummaryValue` 改 `Dock=Fill` + `AutoSize=false` + 垂直居中；标题标签固定 24px |
| UI-2 | `Forms/MainForm.cs` | 删掉从未被调用的重复 `AddSummaryCard`（37 行） |
| UI-3 | `Forms/MainForm.cs` | `CreateLineControllerCard`：复选框/按钮从 `Dock=Right` 改为标题行 3 列表格 + `Anchor=Right` |
| UI-3 | `Forms/MainForm.cs` | `AddLineControllerGroup` 的分组标题栏同样改 3 列表格（原 `Dock=Right` 按钮在高度自适应后被拉成 90px） |
| UI-4 | `HeatPump/Forms/HeatPumpMonitorControl.Designer.cs` | 筛选行 `Absolute 86F`、摘要行 `Absolute 48F` → `AutoSize`；`_summaryLabel` 补 `AutoSize` + `MinimumSize=(0,40)` |
| UI-5 | `Forms/LineControllerControl.Designer.cs` | `_cardsPanel`：`FlowLayoutPanel(TopDown)` → 单列 `TableLayoutPanel`（列 100% / `AddRows` / `AutoScroll`） |
| UI-5 | `Forms/LineControllerControl.cs` | `CardsPanel` 属性类型跟着改 |
| UI-5 | `Forms/MainForm.cs` | `_flpLineControllerCards` 字段类型跟着改；分组去掉 `Width = 1120`，改 2 行表格 + `Dock=Fill` + `AutoSize` |

### 验证结果

| 项 | 方式 | 修前 | 修后 |
|---|---|---|---|
| 页签数量 | 探针读 `TabPages`，真机截图核对 | 11（无"电表总览"） | **12**，位置在"概览仪表盘"之后 |
| 空态提示 | 探针读控件 `Bounds` | `1319×0`（页面纯白） | `1319×90`，截图可见"暂无电表设备" |
| 汇总卡片 | 探针判 `Rectangle.IntersectsWith` | 4/4 **重叠** | 4/4 不重叠 |
| 卡片按钮 | 探针读 `Bounds` | CheckBox `100×154`、Button `75×154` | 标题行整体 `314×33` |
| 卡片正文 | 探针读 `Bounds` | `139×130` | `314×125` |
| 分组宽度 | 探针读 `Bounds` | `1120`（写死） | `1299` = 父容器可用宽度 |
| 两行报错 | 真机截图（数据库缺字段场景） | 第二行被切一半 | 两行完整 |
| 编译 | `dotnet build MeterAcquisitionPro.sln` | —— | 0 error，警告数与基线一致（6 × CS8632 + MSB3277） |
| 回归 | 12 个页签逐个切换截图 | —— | 无异常、无崩溃，其余页签外观不变 |

### 遗留与注意

1. **"概览仪表盘"和"电表总览"现在都能看到同一批电表卡片**。本批只修"页签显示不出来"这个 bug，
   合并/删除属于产品决定，记在 UI-6。
2. 线控器数据页卡片区可见高度仍只有约 110px（卡片 208px 高），要真正好用得先做 UI-7 的空间重排。
3. `artifacts/LayoutProbe` 留在仓库外目录（`artifacts/` 未被跟踪），后续做 UI-6 ~ UI-14 可以继续用它验证。
4. 验证期间临时给 `heatpump-workspace.json` 塞过测试控制器，**已还原为原始内容**
   （空 `Controllers`，备份在 `artifacts/heatpump-workspace.json.bak`）。

---

## 十二、界面布局第二批实施记录（2026-08-27）

本批的题目是用户反馈的"组件布局不规范、按钮大小错乱不一"，对应 UI-15 ~ UI-20，
顺带把 UI-7 / UI-10 / UI-13 一起收掉。

### 审查与验证手段

| 手段 | 说明 |
|---|---|
| 界面审计探针 | 新增 `artifacts/UiAudit`（引用已构建的 `MeterAcquisition.exe`）。遍历全部 12 个页签（含嵌套页签），读每个按钮/输入框的真实 `Bounds`，输出：按钮尺寸分布、字体分布、工具条换行位置、同行控件中线偏差、子控件超出父容器、对话框同级控件重叠 |
| 逐页签截图 | 复用 `artifacts/ui_tabs2.ps1`，1400×900 逐页签 `PrintWindow` |
| 卡片区 | `artifacts/LayoutProbe`（线控器卡片）+ 探针里新增的电表卡片播种（走公开的 `MeterManager.AddMeterToBox`，不改生产代码） |
| 护栏测试 | `TPEM.Tests/UiStyleTests.cs` 10 条，断言的是生产代码里那一份 `UiStyle`（按源码链接进测试工程） |

> 探针的 `App.config` 从主工程抄了一份（`artifacts/` 不进版本库）——
> `MainForm` 构造函数会读 `connectionStrings`，缺了直接抛 `ConfigurationErrorsException`。
> 顺手也给 `LayoutProbe` 补上：它自上一批以来已经跑不起来了（`HistoryQueryService` 后来加了连接串守卫）。

### 新增文件

| 文件 | 说明 |
|---|---|
| `MeterAcquisition/UI/UiStyle.cs` | **界面规范**：字体、行高、按钮尺寸、间距只在这里定义一次，另附构建助手（`Apply` / `EqualizeWidths` / `Cell` / `Field` / `RebuildToolbar` / `CreateFormGrid` / `DialogButtonRow` / `WrapInToolbar`） |
| `TPEM.Tests/UiStyleTests.cs` | 10 条护栏测试 |
| `artifacts/UiAudit/` | 界面审计探针（只读，不进版本库） |

规范本身（数值都在 `UiStyle` 里，改一处全项目生效）：

- 一行标准高度 `RowHeight = 34`，按钮和"字段组"都是这个高 —— **同行控件的中线因此自动重合**，
  不再需要手写 `Margin.Top` 去凑；
- 按钮最小宽 `ButtonMinWidth = 100`（够放 4 个汉字），文字更长才增长；
- 同一页内按钮宽度取齐（`EqualizeWidths`），超过 `MaxEqualizedWidth = 140` 的不参与，
  免得一个"电表：原有电表"把整页按钮都撑宽；
- 按钮永不参与拉伸；只有"标准档 100×34"和"紧凑档（卡片内）76×32"两档；
- 字体统一 `微软雅黑 9.75`。

### 逐项改动

| 编号 | 文件 | 改动 |
|---|---|---|
| UI-15 | `UI/UiStyle.cs`（新增） | 规范与助手集中一处 |
| UI-15 | `Forms/MainForm.cs` | 新增 `ApplyLayoutStandard()`，构造函数末尾统一套用：先规范全部按钮，再重排 6 条工具条，最后按页签取齐宽度 |
| UI-15 | `Forms/MainForm.Designer.cs` | 顶部加注释说明按钮几何值已由 `UiStyle` 接管；`cmbQuickMeter/cmbQuickMetric/cmbCompareMetric` 收窄并加 `DropDownWidth`（下拉列表仍按全宽显示） |
| UI-16 | `UI/UiStyle.cs`、`Forms/MainForm.cs` | `Apply` 去掉横向拉伸的停靠、只留一侧锚点；`btnReadInfo` 用 `WrapInToolbar` 套进停靠工具条；`dgvDeviceInfo` 从"四边锚定 + 写死 Bounds"改 `Dock=Fill` 并调到 z 序底层 |
| UI-17 | `UI/UiStyle.cs` | `Cell`/`Field`：1 行 N 列表格 + 只锚左，由表格负责垂直居中 |
| UI-17 | `Forms/MainForm.cs` | 删掉 `AddHeatPumpField` 里的假居中实现，改调 `UiStyle.Field` |
| UI-17 | `Thermostat/Forms/ThermostatMonitorControl.cs` | 删掉 70 行**从未被调用**的重复工具条实现（`CreateToolbar`/`CreateToolbarGroup`/`CreateField`/`CreateButton`） |
| UI-18 | `Forms/MainForm.cs` | 6 条工具条按"字段组 + 按钮组"重排（`flpDashboardToolbar`/`flpQuickTop`/`flpQuickPaging`/`flpCompareLeftTop`/`flpCompareRightTop`/`btnPanelParams`），动作按钮打包成一个条目，窄窗口时整组一起换行 |
| UI-19 | 温控器三条 Bar、`LineControllerControl`、`MeterOverviewControl`、`HeatPumpMonitorControl` | `AutoScaleMode.Font` → `Inherit`，整窗只由 `MainForm` 一处决定缩放系数 |
| UI-19 | `HeatPump/Forms/HeatPumpMonitorControl.cs` | 日期框 `Width` 148 → 190（原来靠 1.6 倍缩放才够宽，系数变 1 后会把日期切成 `2026-08-26 00:0`） |
| UI-19 | `Forms/LineControllerControl.Designer.cs` | 汇总卡片标题高度从写死 24 改 `AutoSize`，卡片最小高 84 → 88；`Segoe UI` → `UiStyle.MetricFont` |
| UI-20 | `HeatPump/Forms/HeatPumpDeviceEditorDialog.cs`、`HeatPumpModuleGroupEditorDialog.cs` | 绝对坐标改两列表单（`CreateFormGrid`/`AddFormRow`）+ 底部右对齐按钮行；字体统一 |
| UI-7 | —— | 工具条收敛到一行、汇总卡片降到 88px 后，1400×900 下线控器卡片区能完整显示一整行卡片（截图已确认） |
| UI-10 | 各处 | 按钮/输入框/标签统一 `UiStyle` 字体，`Segoe UI` 与宋体残留清掉 |
| UI-13 | `HeatPump/Forms/HeatPumpMonitorControl.Designer.cs` | 左列 `Absolute 228F` → `Percent 24F`，告警行 `Absolute 180F` → `Percent 30F`，两个分区标题 `Height=25` → `AutoSize`；状态文字单独占一行，不再把筛选条顶换行 |
| —— | `Forms/MainForm.cs`、`Forms/MeterOverviewControl.cs` | 卡片内按钮改紧凑档；"电表总览"卡片 300×190 → 320×220（与"概览仪表盘"同尺寸，原尺寸装不下内容的 208px） |

### 验证结果

| 项 | 方式 | 修前 | 修后 |
|---|---|---|---|
| 按钮尺寸种类 | 探针读 38 个按钮 `Bounds` | **17 种** | **4 种**（104×34 ×26、121×34 ×10、138×34、155×34） |
| 按钮高度种类 | 同上 | **8 种**（29~52） | **1 种**（34） |
| 字体种类 | 探针读 `Control.Font` | 2 种 | **1 种**（微软雅黑 9.75，70 个控件） |
| 同行中线偏差 | 探针按纵向重叠分行后算中线 | **15 处**，最大 11.5px | **0 处** |
| 子控件超出父容器 | 探针比 `Bounds` 与父 `ClientSize` | 5 处 | **0 处** |
| 工具条换行 | 探针分行 | 4 处（3 处拆散标签） | 2 处（均为整组换行：长说明文字、温控器三条 Bar），**不再拆散标签** |
| 被拉伸的按钮 | 探针读 `Bounds` | 读取设备信息 1324×38、遥控 160×50 | 138×34 / 121×34 |
| 对话框重叠 | 探针比同级 `Bounds` | 标签压文本框 8px | **0 处** |
| 电表卡片 | 探针播种 2 台电表后读 `Bounds` | —— | 两套实现都是 320×220，内容首选高 200 / 190，无裁切 |
| 线控器卡片 | `LayoutProbe` 注入 1 台控制器 + 5 模块 | 卡片区可见约 110px | 一整行 340×208 卡片完整可见 |
| 编译 | `dotnet build MeterAcquisitionPro.sln` | —— | 0 error，警告与基线一致（6 × CS8632 + MSB3277） |
| 测试 | `dotnet test TPEM.Tests` | 34 条 | **44 条全通过**（新增 10 条界面规范护栏） |
| 回归 | 12 个页签 + 关联对比子页签逐个截图 | —— | 无崩溃，各页控件排布正常 |

### 遗留与注意

1. **`MainForm` 的窗体级默认字体仍是宋体 9**，没有改成微软雅黑。原因：`AutoScaleMode.Font` 下改窗体字体
   会让 `AutoScaleDimensions`（8×15）与新字体度量不符，整窗按 1.25×1.4 重新缩放，布局会整体炸开；
   要改需连 `AutoScaleDimensions` 一起改，而那个值是机器/DPI 相关的，属于 UI-12（DPI 感知）范围。
   目前**没有可见控件继承到宋体**（探针核对：70 个按钮/输入框全是微软雅黑）。
2. 本机实测 `Graphics.DpiX = 120`（125% 缩放）但进程 DPI 不感知，`DeviceDpi` 报 96 ——
   文字实际按 120dpi 渲染（一个汉字约 19px），所以规范里的像素常量是按这个环境定的。
   做 UI-12 时要重新核对这批常量。
3. 温控器页三条子工具条在 1400 宽下仍折成 2 行（连接条 / 控制条 + 状态条）。这是有意的分组换行，
   不是错位；要一行放完需要再压缩控件宽度或缩短标签文案，涉及文案改动，未做。
4. 新发现 X-7：5 个源文件不在编译列表里（含 1079 行的 `HeatPump/Forms/MainForm.cs`），
   处理方式需产品/开发确认，本批未动。
5. `artifacts/UiAudit`、`artifacts/LayoutProbe` 都在未跟踪目录里，后续做 UI-6 / UI-8 / UI-9 / UI-11 / UI-12 / UI-14
   可以继续用；`UiAudit.exe <标签> [页签名]` 第二个参数会额外打印该页签的控件树并截图，排查遮挡很好用。

---

## 十三、第二轮审查实施记录（2026-09-17）

### 审查方式

| 手段 | 说明 |
|---|---|
| 分路并行审查 | 4 路独立审查：上位机（`MeterAcquisition`）、入库服务与数据库（`MeterIngestionWorker` + `sql`）、若依后端（Java）、若依前端（Vue）。每路都先读本清单已记录项，只报**清单未记录**的问题 |
| 基线实测 | `dotnet build MeterAcquisitionPro.sln -t:Rebuild`、`dotnet test TPEM.Tests`、`mvn -o -pl ruoyi-system -am compile`、`npm run build:prod` 全部实跑 |
| 仓库核查 | `git ls-files`、`git cat-file`、逐目录统计体积；xlsx 协议表两版逐格比对（见下方"更正"） |
| 未做 | 未连数据库、未执行迁移、未跑真机串口；网页端只做了"能静态定论"的修复 |

### 本轮已落地的改动

| 编号 | 文件 | 改动 |
|---|---|---|
| P4-1 | `Services/ModbusService.cs` | `Connect`/`Disconnect` 改为"锁内开关串口、出锁触发事件" |
| P4-1 | `Forms/MainForm.cs` | 两处 `PublishBoxRegistryAsync(...).GetAwaiter().GetResult()` → `RunGuardedAsync`（后台发送 + 兜底） |
| P4-2 | `Forms/MainForm.cs` | 新增 `ParseStopBitsFromString`；写参数用它；确认框补显示下拉项原文 |
| P4-3 | `Common/Utils.cs` | `Crc16Helper.Calculate/Validate` 增加带偏移的重载（原实现只能从下标 0 算） |
| P4-3 | `Services/ModbusService.cs` | 新增 `ValidateWriteResponse`（8 字节定长找帧 + 地址/功能码/回显/CRC 全校验）；两个写方法改用它 |
| P4-4 | `MeterIngestionWorker/Program.cs` | 新增 `ResetDebounceCountersOfUnhitItems`，把"连续命中"真正实现为"未命中即清零" |
| P4-5 | `MeterIngestionWorker/Program.cs` | `Task.WhenAll` 包 `catch (OperationCanceledException)`；异常分支补 `cts.Cancel()` |
| P4-6 | `MeterIngestionWorker/Program.cs` | `meter_status.last_collect_time` / `last_publish_time` 改 CASE 取较新者；`meter.last_seen_time` 加单调守卫 |
| W-1 | `MeterCardMapper.xml` | `selectQualityHistory` 补 `LEFT JOIN meter_status ms` |
| W-2 | `meter/history/index.vue` | `categoryInfo` 补 `quality`；getter 空值兜底；删重复 `trendRequestId`；新增 `$route.query` watch |
| W-3 | `meter/alarm-record/index.vue` | 累计电能字段名改 `forwardActiveEnergy` / `reverseActiveEnergy` |
| W-4 | `MeterCardServiceImpl.java` | `enrichCard` 补 `setReactivePowerKvar(toKilovars(...))` + `toKilovars` 助手 |
| W-5 | `MeterCardServiceImpl.java` | `applyStatus` 接收阈值表，PF/电压/频率全部走 `minOf/maxOf`；严重数值异常改用新状态码 `ABNORMAL`；抽出 `DEENERGIZED_VOLTAGE` |
| W-5 | 前端 5 处 | `meter-utils.js`、`meter-simulation.js`、`views/index.vue`（图例 + severity）、`exception-devices/index.vue`（severity）补 `ABNORMAL` |

### 验证结果

| 项 | 方式 | 结果 |
|---|---|---|
| C# 编译 | `dotnet build MeterAcquisitionPro.sln -t:Rebuild` | **0 error**；警告与基线一致（6 × CS8632 + MSB3277） |
| 单元测试 | `dotnet test TPEM.Tests` | **44 条全通过** |
| Java 编译 | `mvn -o -pl ruoyi-system -am compile` | BUILD SUCCESS（退出码 0） |
| 前端构建 | `npm run build:prod` | **DONE Build complete**（所有改动过的 .vue 语法通过） |
| 基线（改动前） | 同上三项 | 编译 0 error、测试 44 通过 —— 本轮改动未引入回归 |

> 注：`ruoyi-ui/package.json` 里的构建脚本名是 **`build:prod`**，不是 `build`。
> 此前记录里写的 `npm run build` 会直接报 `Missing script: "build"`。

### 对清单前几批几处说法的更正

1. **xlsx 协议表（原 X-3 澄清）—— 数据完整，但丢了内嵌图形**。
   逐格比对工作区版本与 `HEAD` 版本：12 张表、每张表的行数、非空值总数**完全一致**
   （18 个抽样值在两份里都能找到），所以"内容完整、不是被精简"的结论**对表格数据成立**。
   但工作区那份是 **WPS 重存**的（`xmlns:etc="http://www.wps.cn/..."`），比 `HEAD` 少了
   **12 个 drawing 对象 + 1 张内嵌 PNG**（`xl/media/image1.png`，32 KB），
   合计 48,866 字节 —— 与 80506 → 31809 的体积差（48,697 字节）基本相等，**体积差全部来自这里**。
   若那些图形载有信息（接线图/截图之类），值得打开看一眼。
2. **行数已过时**：`MainForm.cs` 现在是 **3588 行**（UI-11 记的是 4071）。
   另外 `MeterIngestionWorker/Program.cs` 已达 **2266 行** —— 同一个"上帝文件"问题，
   但 P2-5 只点了 `MainForm`，拆分时应一并考虑。
3. **X-3 的实际规模远大于原记录**：原文只点了 `.claude/buildcheck-adaptive-y/` 一个目录，
   实测有 **17 个 `buildcheck*` 目录、1153 个跟踪文件、合计 1575 MB** 构建产物在版本库里
   （`.git` 50.3 MB）。全新 clone 会在磁盘上写出 1.5 GB 的无用文件。
4. **X-1 补充一处凭据**：`sql/migrations/20260730_create_tpem_admin.sql` 硬编码了
   `tpem_admin` 的 bcrypt 口令哈希并授予 `role_id=1`（若依超管角色），与 PG/MQTT 口令同属 X-1 范围。
5. **X-7 核实无误**：磁盘上 81 个 `.cs` 中确实有 5 个不在 `csproj` 的 `Compile` 列表里
   （`HeatPump/Forms/` 下的 `ControllerEditorDialog`、`ControllerGroupEditorDialog`、
   `ModuleEditorDialog`、`MainForm.cs` 与 `MainForm.Designer.cs`）。
6. **`.claude/settings.local.json` 被跟踪**，且工作区改动里新增了
   `Bash(export PGPASSWORD=12345)` 字面量 —— 建议把它加进 `.gitignore`，别再让它进提交。

### 本轮未做的三件事（各有原因，不是遗漏）

1. **网页端权限注解（W-6）**：`sys_menu` 里没有电表各页的权限位，而前端菜单又是硬编码的
   `constantRoutes`。只加 `@PreAuthorize` 会让非管理员账号整块看不到电表模块 ——
   这是产品决定（谁该看到什么），需要先定权限位再一起改。
2. **网页端阈值真正打通（W-7）**：取决于"MySQL `ma` 库到底有没有 `meter_threshold` 表"
   和"这张表该由哪个数据源提供"，需要现场一条 SQL 定论后再决定是补 MySQL DDL 还是改数据源路由。
3. **图表时间范围不跟随（《任务说明》第 7 条）**：属于趋势查询链路（`LIMIT 240` 截断 + 缺分桶），
   改动会同时影响能耗与质量两条趋势接口，且要先按上面给的两步现场确认根因。

### 建议的后续批次顺序

1. **网页端止血批（W-6 ~ W-8）**：定权限位 → 补 `@PreAuthorize` 与菜单种子数据；
   现场核实 `meter_threshold` 的库归属后打通阈值；补 `is_alarm_enabled` 语义。
2. **上位机串口保命批（P4-7 ~ P4-12）**：切型号后的寄存器分派、离线超时入配置、
   关窗等热泵刷新、扫描后按钮状态、地址改动回写档案。
3. **数据可信补齐批（P4-13 ~ P4-20）**：热泵故障码两端接通、离线/恢复事件成对、
   失败消息重投、`sql/public.sql` 加警告或废弃、档案同步的空列表与唯一键冲突处理。
4. **仓库清理（X-1 / X-3 / X-8）**：`buildcheck*` 出库（需重写历史，属不可逆操作，单独一轮并先评审）、
   `skills` gitlink 处置、口令轮换与移出仓库。
5. **前端体验批（W-20 ~ W-30）**：模拟数据标识、定时器随 keep-alive 启停、
   首页入口职责、详情抽屉竞态、错误态与空态区分。

---

## 十四、第二批修复实施记录（2026-09-17 同日）

承十三节，继续按"高严重度 + 能静态定论 + 可验证"推进：本批完成
**W-8 / W-10 / W-11 / W-20 / W-21 / W-22 / W-23 / W-25 / W-27** 与
**P4-8 / P4-10 / P4-11 / P4-12 / P4-16 / P4-18**，另把 W-7 从"静默"改为"可见"。

### 网页端

| 编号 | 文件 | 改动 |
|---|---|---|
| W-8 | `MeterCardServiceImpl.java` | 新增 `isAlarmEnabled(...)`，风险判定六项全部先看阈值表的 `is_alarm_enabled`；表里没有该行时保持原判定（不能因为读不到配置就少报风险） |
| W-7 | `MeterCardServiceImpl.java` | 读不到阈值由"静默退回默认值"改为**明确的 WARN**（区分"查询异常"与"查询成功但无数据"两种原因），恢复时补一条 INFO；失败结果只缓存 **30 秒**而不是 5 分钟，不再让一次瞬断把兜底值锁死一整轮 |
| W-10 | `MeterCardController.java` | `/quality/page` 下标改用 `long` 计算（原 `int` 乘法可溢出为负数 → `subList` 抛异常 → 500）；`pageSize` 上限 200 |
| W-10 | `TableSupport.java` | 分页参数统一夹取（`pageNum ≥ 1`、`1 ≤ pageSize ≤ 500`），所有走 PageHelper 的接口（含 `/history/*/page`）一并受约束 |
| W-11 | `GlobalExceptionHandler.java` | 两个兜底分支不再回 `e.getMessage()`，改为固定文案；新增 `IllegalArgumentException` 专用分支 → 400 + 原始校验消息（例如"结束时间不能晚于当前时间"，这类消息是业务代码自己写的，可以给用户看） |
| W-20 | `layout/components/AppMain.vue` | 模拟模式下在内容区顶部常驻醒目横幅，说明"下面是演示数据"并给出关闭路径；进入每个页面时重读开关（localStorage 非响应式） |
| W-21 | `views/index.vue`、`meter/realtime/index.vue` | 定时器改为 `activated`/`deactivated` 启停（这两个页面被 keep-alive 缓存，原先只在 `beforeDestroy` 清理 → 离开后仍每 15 秒请求）；抽 `startRefreshTimer`/`stopRefreshTimer`，重复调用安全 |
| W-22 | `meter/topology/index.vue` | `loadTopology` 补 `catch`：给出错误提示、页面顶部错误横幅，并且在加载失败期间**拒绝进入编辑与保存** —— 否则会用空配置把现有区域覆盖掉 |
| W-23 | `meter/components/realtime-detail.vue` | 抽屉 `open()` 增加请求序号守卫 + 打开时先清空内容，避免快速切换设备时显示/覆盖成另一台设备的数据 |
| W-25 | `meter/energy/index.vue` | 去掉 `handleMeterChange` 里的手动 `loadAnalysis()`（路由 watch 已负责同一件事），不再重复发请求 |
| W-27 | `meter/realtime/index.vue` | 输入框改绑 `draft`，点搜索才合并进 `queryParams`，自动刷新不再按"未提交的输入值"过滤列表 |

### 上位机与入库服务

| 编号 | 文件 | 改动 |
|---|---|---|
| P4-8 | `Forms/MainForm.cs`、`App.config` | 新增 `OfflineTimeoutSeconds` 配置项（含说明），并把实际生效阈值改为 `max(配置值, 表数 × 2 秒)`，每个采集周期重算 —— 表多时一轮采集本身就超过 15 秒，原先会让排在后面的卡片周期性闪"设备掉线" |
| P4-10 | `Forms/MainForm.cs` | 自动扫描的 `finally` 改为按真实连接状态恢复"连接/断开"按钮（原无条件启用"连接"，再点会 `Close()+Open()` 打断采集） |
| P4-11 | `Forms/MainForm.cs` | 快速查询 `finally` 里在 `ToggleQuickControls(true)` 之后补一次 `UpdateQuickPagingState()`，翻页按钮不再是"能点但没反应" |
| P4-12 | `Forms/MainForm.cs` | 卡片改从站地址时立即重新发布所属箱的档案（原来只改内存对象：平台仍按旧地址归属历史与告警，刷新后卡片读数跳回旧表），并写审计记录 |
| P4-1 | `Forms/MainForm.cs` | 补掉第二处漏网的 `PublishBoxRegistryAsync(...).GetAwaiter().GetResult()`（`UpdateSecondConnectionUI`，同样是串口事件路径） |
| P4-16 | `sql/public.sql` | 文件头加显著警告：本 dump 缺 `uq_alarm_event_active`，用它恢复库会让告警写入持续报 42P10、整事务回滚；并写明正确做法（用迁移建库，或恢复后重放 001→004） |
| P4-18 | `MeterIngestionWorker/Program.cs` | `MarkLogFailed` 的空 catch 改为记 WARN：更新失败时 `mqtt_message_log` 会永远停在 `pending`，原先完全无痕 |

### 验证结果（第二批）

| 项 | 方式 | 结果 |
|---|---|---|
| C# 编译 | `dotnet build MeterAcquisitionPro.sln` | **0 error**；警告与基线一致（6 × CS8632 + MSB3277 ×108） |
| 单元测试 | `dotnet test TPEM.Tests` | **44 条全通过** |
| Java 编译 | `mvn -o -pl ruoyi-admin -am compile` | **BUILD SUCCESS**（覆盖 ruoyi-common / framework / system / admin 四个模块） |
| 前端构建 | `npm run build:prod` | **DONE Build complete** |

> **行为变化，需现场确认**：`TableSupport` 的分页上限为 500。
> 若现场有"一次导出/查看超过 500 行"的用法，需要改大这个常量或改走导出接口。

### 第二轮审查累计进度

- **已完成**：P4-1 ~ P4-6、P4-8、P4-10 ~ P4-12、P4-16、P4-18；
  W-1 ~ W-5、W-8、W-10、W-11、W-20 ~ W-23、W-25、W-27；
  **W-24、W-28 ~ W-45**（见十五节，2026-09-23）。
- **待现场定论后才能做**：W-7（`meter_threshold` 该建在 MySQL 还是改数据源路由；**表不存在已实测确认**）、
  P4-7（AMC96L-E4 是否支持原有电表寄存器）、《任务说明》第 7 条（趋势 `LIMIT 240` 与分桶，先按十三节的两步确认）。
- **待产品决定**：W-6（电表各页的权限位与站点隔离范围）、W-26（首页点正常设备该进哪个页面）、
  W-31（哪份库是网页端的权威数据源）、W-46（拓扑列表视图要不要支持改配电箱）。
- **尚未动**：P4-9、P4-13 ~ P4-15、P4-17、P4-19 ~ P4-24、W-9、W-12 ~ W-19、W-26、W-46。

---

## 十五、网页端第三批实施记录（2026-09-23）

### 审查方式

在修拓扑页悬浮球"收起后再点无反应"时顺带铺开：**两路并行审计**（一路只看 `views/meter/*` 页面，一路看组件/共享层/热泵/API/路由），
产出 W-31 ~ W-46；随后我自己复核了影响最大的几条（控制器注解、静态路由、`ma` 库实测、各页关键行），
再按"低风险 + 可静态定论"的顺序修 W-32 ~ W-45。

### 本轮已落地的改动

| 编号 | 文件 | 改动 |
|---|---|---|
| W-33 | `meter/quality/index.vue` | 三个筛选 `el-form-item` 补 `prop`（Element 的 `resetFields` 只认带 `prop` 的项，原来「重置」是空转）；顺带把 `showAll` 一并复位 |
| W-38 | `meter/quality/index.vue` | `showDetail` 只改路由、由 watcher 统一打开抽屉；路由值没变时（同一行再点一次）才直接打开 —— 原来两处都 open，同一台设备固定发两次详情请求 |
| W-24 | `meter/quality/index.vue` | 列表与统计拆成两个独立失败域（原来 `Promise.all`，统计接口一挂就连列表一起清空）；统计没拿到时 KPI 显示 `--` 而不是 0；列表失败时空态文案改为"加载失败，请点「搜索」重试" |
| W-32 | `meter/history/index.vue` | `loadDetails` 增加 `detailRequestId` 归属校验（与同页趋势一致），失败时不再静默清空而是给出"明细加载失败，请重试" |
| W-35 | `meter/energy/index.vue` | `energyKpis` 与图表数据改用 `toKilowatts`/`toMeterNumber`：缺失读数不再以 0 参与峰值/均值、不再在趋势图上画 0 kW 点；KPI 显示 `--` |
| W-39 | `meter/energy/index.vue` | `applyPreset` 不再顺带重拉全量设备卡片（设备列表只在 `mounted` 拉一次，且只在"本来没选设备"时才补一次分析）；`loadMeters` 失败不再清掉已选设备 |
| W-34 | `meter-utils.js`、`meter/exception-devices/index.vue`、`views/index.vue` | 新增 `getMeterSeverityRank`：`FAULT` 的等级 0 不再被 `\|\| 99` 当成未知；**同时消除首页与异常页两张互相矛盾的严重度表**（NODATA/VOLTAGE_BAD/PF_LOW 先后不一致） |
| W-37 | `meter/exception-devices/index.vue` | 合并 `dashboard + toolbar` 两组并按 `meterId` 去重，外部串口设备不再漏掉；清掉页内重复的 `formatNumber`/`statusType`/`getMeterStatusLabel` 包装 |
| W-36 | `heatpump/monitor/index.vue` | `refresh`/`loadHistory` 补 `catch`（原来每 15 秒一次未处理 Promise 拒绝）；新增 `loadFailed` 横幅；定时器改 `activated`/`deactivated` 启停；自动刷新不再弹整表 loading |
| W-40 | `heatpump/monitor/index.vue` | 筛选表单只在「实时状态」页签显示，告警页签给出"由后端汇总全部站点、不受筛选影响"的说明（后端 `/alarms` 仍不支持按站点筛选） |
| W-44 | `heatpump/monitor/index.vue` | 控制器列两个字段都为空时显示 `--`，不再是"地址 undefined" |
| W-41 | `meter/alarm-record/index.vue` | 删掉页内 3 位小数的 `formatNumber`，统一用 `formatMeterNumber`（2 位）；`statusType` 换成共享的 `getMeterStatusType`；位置文案换 `formatMeterLocation` |
| W-42 | `meter-utils.js` + 8 个页面 | 新增 `hasMeterValue` / `toMeterNumber` / `toKilowatts` / `formatMeterLocation`；位置文案在首页、实时、能耗、质量、拓扑、异常设备、告警记录、历史、详情抽屉统一；从站地址统一走 `formatMeterAddress` |
| W-43 | `api/system/meter.js`、`meter-utils.js` | 删掉重复的 `getEnergyTrend`（无调用点）与名不符实的 `formatMeterDateTime`（无调用点） |
| W-45 | `meter-utils.js`、`meter-device.vue` | 判空统一走 `hasMeterValue`（`null`/`undefined`/空串都算没有），空串不再被 `Number('') === 0` 当成 0；`isBelow/isAbove/isOutside` 与卡片上色同一口径 |
| W-30 | 实时 / 质量 / 历史 / 详情抽屉 | 失败与"无数据"分开：实时页失败保留上一次数据并显示提示（不再清空列表、分页器不再消失），质量页与历史页有各自的失败文案，详情抽屉失败给"详情加载失败"占位而不是一台"全是 `--` 的设备" |
| W-28 尾部 | `views/index.vue` | 首页负荷排行与柱状图改用 `toMeterNumber`（原来 `Number(null) === 0` 会把没采到读数的设备算成 0 kW 排进排行榜），`formatNumber` 统一走 `formatMeterNumber` |
| W-47 | `views/index.vue` | 定时器改调 `loadSnapshot({ silent: true })`：轮询不再置整表 loading（原来每 15 秒闪一次遮罩）；`loadSnapshot` 失败时保留上一次卡片，不再把首页清空。同类排查：`realtime`、`heatpump` 的轮询已是静默，`lock.vue` 的 1 秒定时器只是时钟，无此问题 |
| W-48 | `views/index.vue` | 补 `methods: { formatMeterLocation }`。这是本轮**自己引入的回归**：模板里用了它但没挂到实例上，表格一有数据渲染就抛 `TypeError`，Vue 中断 patch、组件冻在上一帧，于是 loading 遮罩永远不关 —— 用户看到的"首页一直在转圈"就是这个。另写了 `artifacts/db-cleanup/check-template-methods.py`（先剥掉 import 再判断是否暴露）扫描全部 `src/views/**/*.vue`，确认没有第二处 |

### 验证方式与结果

| 项 | 方式 | 结果 |
|---|---|---|
| 前端构建 | `npm run build:prod`（两次，覆盖全部改动后重跑） | **DONE Build complete**，日志无 error |
| 预览环境 | `ruoyi-ui/.harness` 扩展成 `?page=topology\|realtime\|energy\|quality\|history\|exceptions\|alarmRecord\|heatpump` 可单独挂载 | 编译通过（`DONE Compiled successfully`） |
| W-33 | 预览页真实点击：填「站点=模拟园区」→ 点「重置」 | 点前输入框与生效条件都是"模拟园区"，**点后输入框清空、条件回 `undefined`** |
| W-34 | 预览页读渲染顺序（模拟数据补了一台 FAULT） | 渲染为 **通信故障 → 无数据 → 电压异常 → 功率因数低**（旧代码 FAULT 会排最后） |
| W-35 | 预览页注入 `powerPoints=[null,5000,3000,""]` | 峰值 5 kW、平均 4 kW、**有效功率点 = 2**（旧代码会把两个缺失点算成 0，得到 4 个点与 2 kW 均值）；累计电能显示 `--` |
| W-36 | 预览页在后端不可用时打开热泵页 | 显示"数据刷新失败，下表是上一次成功获取的结果"，`loadFailed=true`，定时器在跑且不再抛未处理拒绝 |
| W-40 | 预览页切换页签 | 「实时状态」显示筛选表单；切到「活动告警」表单隐藏、说明文字在位 |
| W-32 | 预览页带 `meterId`+时间范围进历史页 | 趋势 24 点、明细 20 行，`detailRequestId` 递增到 1（说明新的守卫路径确实执行） |
| W-41 / W-42 | 预览页直接调用页面方法 | `formatMeterNumber(12.3456)="12.35"`、`(null)="--"`、`("")="--"`；`getMeterStatusType("FAULT")="danger"`、未知码 `"info"`；告警记录页渲染出"模拟园区 / 泵房配电箱"与 2 位小数读数 |
| W-48 | 预览页读**真实 DOM**（不是只看组件状态） | 修复前：`loading=false` 但遮罩可见、表格 0 行、改模板字段 DOM 不动（说明渲染被异常中断）；修复后：表格 4 行、位置列显示"模拟园区 / 泵房配电箱"、遮罩 `display:none`、根类名从 `energy-dashboard el-loading-parent--relative` 复原为 `energy-dashboard`、健康度计数 7 台全对上 |

### 过程中发现的三件事

1. **首页与异常设备页的严重度表本来就不一致**（两张表对 NODATA / VOLTAGE_BAD / PF_LOW 的先后互相矛盾），
   已合并为 `getMeterSeverityRank`；顺序采用异常设备页那张（故障 → 数值异常 → 无数据 → 电压 → 功率因数 → 等待采集），
   若现场更认可首页原来的顺序，改 `meterSeverityOrder` 一处即可。
2. **预览入口缺 RuoYi 的全局 `resetForm`**（真实应用由 `main.js` 注入到 `Vue.prototype`），
   第一次点「重置」的验证因此无效（页面里 `this.resetForm(...)` 直接抛错）；已在 `.harness/entry.js` 里照 main.js 补上，
   之后才得到有效结论。**以后用预览页验证涉及 `resetForm`/`parseTime` 这类全局方法的页面，先确认入口补了没有。**
3. **模拟数据补了一台通信故障设备**（`SIM-METER-007 泵房电表`）：原来 6 台只覆盖 OK/PF_LOW/VOLTAGE_BAD/NODATA，
   异常列表最该排在最前的 FAULT 反而没有样例，排序与配色都无法核对。
4. **模板里调用共享函数，必须在 `methods` 里暴露；漏挂的后果不是"显示空白"，而是整个组件渲染中断、冻在上一帧。**
   本轮 W-48 就是这么来的：首页模板用了 `formatMeterLocation`，只 import 没挂 `methods` → 表格一有数据渲染就抛
   `TypeError` → Vue 中断 patch → loading 遮罩永远不关。**只查组件状态（`loading === false`）会得出"没问题"的错误结论，
   必须读真实 DOM**（表格有没有行、遮罩的 `display`、根元素类名有没有 `el-loading-parent--relative` 残留）。
   已补 `artifacts/db-cleanup/check-template-methods.py` 全量扫描：**先剥掉 import 再判断是否暴露**，
   否则多行 import（每个名字独占一行）会被误判成"已暴露"而漏报。

### 本轮未做（各有原因）

1. **W-31 数据源归属**：`ma` 库实测停在 `2026-07-30 00:08:14`，入库写的是 PostgreSQL。
   这不是代码问题而是"哪份库是网页端权威数据源"的架构决定，定了才好动手（改数据源路由或给 MySQL 补同步）。
2. **W-6 权限**：`/meter` 七页仍是 `constantRoutes` 里的静态路由（无 `roles`/`permissions`），
   后端也只有 `/topology` 两个端点带 `@PreAuthorize`。补注解与补菜单种子数据必须一起做，属产品决定（谁该看到什么）。
3. **W-46 拓扑列表视图改配电箱**：属新增功能，且跨箱受 `uk_box_slave` 约束，要先定"撞地址怎么办"。
4. **W-42 剩余的状态文案**：前端「通信故障/无数据/等待采集」与后端 `statusText`「通信异常/设备掉线/等待数据」仍不一致，
   改文案会动到多处界面与图例，属措辞决定，未擅改。



